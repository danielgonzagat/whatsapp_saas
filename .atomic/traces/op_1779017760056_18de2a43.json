{
  "traceVersion": "1.0",
  "operationId": "op_1779017760056_18de2a43",
  "ts": "2026-05-17T11:36:00.056Z",
  "file": "docs/ai/atomic-os-benchmark/tools/refactor-scorecard.cjs",
  "repoRoot": "/Users/danielpenin/whatsapp_saas",
  "operation": "atomic_create_file",
  "operator": "atomic_create_file",
  "targetUnit": "text_span",
  "intention": "atomic_create_file",
  "fallback": false,
  "metrics": {
    "changedChars": 5462,
    "lineRewriteSurfaceChars": 0,
    "expansionFactorAvoided": 0,
    "bytesNet": 5462,
    "lineRewriteAvoided": false
  },
  "validation": {
    "language": "ts",
    "syntaxErrorsBefore": 0,
    "syntaxErrorsAfter": 0
  },
  "preview": false,
  "changed": true,
  "afterSha256": "6d91b9b094019698d276c56daf022d5441e5c7f581272bbf4b2f61c14bc221ce",
  "proposedSha256": "6d91b9b094019698d276c56daf022d5441e5c7f581272bbf4b2f61c14bc221ce",
  "rollback": {
    "available": true,
    "strategy": "explicit pre-edit snapshot (before-text retained by caller)"
  },
  "inlinePreview": "\u001b[2m--- docs/ai/atomic-os-benchmark/tools/refactor-scorecard.cjs (atomic char-level)\u001b[0m\n  \u001b[32m{+#!/usr/bin/env node\n  'use strict';\n  \n  const fs = require('node:fs');\n  const path = require('node:path');\n  const { spawnSync } = require('node:child_process');\n  \n  const DEFAULT_PROTECTED = [\n    'AGENTS.md',\n    'CLAUDE.md',\n    'CODEX.md',\n    'ops',\n    'scripts/ops',\n    '.github',\n    'docs/codacy',\n    'docs/design',\n    '.codacy.yml',\n    'package.json',\n    '.husky/pre-push',\n    'backend/eslint.config.mjs',\n    'frontend/eslint.config.mjs',\n    'worker/eslint.config.mjs',\n    'scripts/pulse/no-hardcoded-reality-audit.ts',\n  ];\n  \n  function usage() {\n    console.error('Usage: refactor-scorecard.cjs --worktree <abs> --target <path> [--scope-prefix <path>] [--max-target-lines <n>] [--max-file-lines <n>] [--spec <path>] [--json]');\n    process.exit(2);\n  }\n  \n  function parseArgs(argv) {\n    const out = { json: false, scopePrefix: 'backend/src/kloel/unified-agent' };\n    for (let index = 0; index < argv.length; index += 1) {\n      const arg = argv[index];\n      if (arg === '--worktree') {\n        out.worktree = argv[++index];\n      } else if (arg === '--target') {\n        out.target = argv[++index];\n      } else if (arg === '--scope-prefix') {\n        out.scopePrefix = argv[++index];\n      } else if (arg === '--max-target-lines') {\n        out.maxTargetLines = Number(argv[++index]);\n      } else if (arg === '--max-file-lines') {\n        out.maxFileLines = Number(argv[++index]);\n      } else if (arg === '--spec') {\n        out.spec = argv[++index];\n      } else if (arg === '--json') {\n        out.json = true;\n      } else {\n        usage();\n      }\n    }\n    if (!out.worktree || !out.target) usage();\n    if (!path.isAbsolute(out.worktree)) throw new Error('--worktree must be absolute');\n    if (!fs.existsSync(out.worktree)) throw new Error('worktree not found: ' + out.worktree);\n    return out;\n  }\n  \n  function runGit(worktree, args) {\n    const result = spawnSync('git', ['-C', worktree, ...args], { encoding: 'utf8' });\n    if (result.status !== 0) {\n      throw new Error('git ' + args.join(' ') + ' failed: ' + (result.stderr || result.stdout));\n    }\n    return result.stdout.split(/\\r?\\n/).map((line) => line.trim()).filter(Boolean);\n  }\n  \n  function uniq(values) {\n    return [...new Set(values)];\n  }\n  \n  function relPath(worktree, value) {\n    const normalized = path.isAbsolute(value) ? path.relative(worktree, value) : value;\n    return normalized.split(path.sep).join('/');\n  }\n  \n  function lineCount(absPath) {\n    const text = fs.readFileSync(absPath, 'utf8');\n    if (!text) return 0;\n    return text.endsWith('\\n') ? text.split('\\n').length - 1 : text.split('\\n').length;\n  }\n  \n  function traceCount(worktree) {\n    const dir = path.join(worktree, '.atomic', 'traces');\n    try {\n      return fs.readdirSync(dir).filter((name) => name.endsWith('.json')).length;\n    } catch (error) {\n      if (error && error.code === 'ENOENT') return 0;\n      throw error;\n    }\n  }\n  \n  function sourceChangedFiles(worktree, scopePrefix) {\n    const tracked = runGit(worktree, ['diff', '--name-only', '--']);\n    const untracked = runGit(worktree, ['ls-files', '--others', '--exclude-standard', '--']);\n    return uniq([...tracked, ...untracked])\n      .filter((fileName) => fileName.startsWith(scopePrefix) && fileName.endsWith('.ts'))\n      .sort();\n  }\n  \n  function protectedDiff(worktree) {\n    return runGit(worktree, ['diff', '--name-only', '--', ...DEFAULT_PROTECTED]);\n  }\n  \n  function main() {\n    const args = parseArgs(process.argv.slice(2));\n    const target = relPath(args.worktree, args.target);\n    const changedSources = sourceChangedFiles(args.worktree, args.scopePrefix);\n    const sourceMetrics = changedSources.map((fileName) => ({\n      file: fileName,\n      lines: lineCount(path.join(args.worktree, fileName)),\n    }));\n    const targetLines = lineCount(path.join(args.worktree, target));\n    const largestChangedSource = sourceMetrics.reduce(\n      (largest, item) => (item.lines > largest.lines ? item : largest),\n      { file: null, lines: 0 },\n    );\n    const overFileLimit = Number.isFinite(args.maxFileLines)\n      ? sourceMetrics.filter((item) => item.lines > args.maxFileLines)\n      : [];\n    const spec = args.spec ? relPath(args.worktree, args.spec) : null;\n    const specDiff = spec ? runGit(args.worktree, ['diff', '--name-only', '--', spec]) : [];\n    const protectedFiles = protectedDiff(args.worktree);\n    const result = {\n      ok: true,\n      target,\n      targetLines,\n      maxTargetLines: Number.isFinite(args.maxTargetLines) ? args.maxTargetLines : null,\n      targetLinePass: Number.isFinite(args.maxTargetLines) ? targetLines <= args.maxTargetLines : true,\n      changedSourceCount: sourceMetrics.length,\n      changedInventoryLines: sourceMetrics.reduce((total, item) => total + item.lines, 0),\n      largestChangedSource,\n      maxFileLines: Number.isFinite(args.maxFileLines) ? args.maxFileLines : null,\n      overFileLimit,\n      specDiff,\n      protectedDiff: protectedFiles,\n      traceCount: traceCount(args.worktree),\n    };\n    result.ok = result.targetLinePass && overFileLimit.length === 0 && specDiff.length === 0 && protectedFiles.length === 0;\n  \n    if (args.json) {\n      console.log(JSON.stringify(result, null, 2));\n    } else {\n      console.log('ok=' + result.ok);\n      console.log('target_lines=' + targetLines);\n      console.log('changed_inventory_lines=' + result.changedInventoryLines);\n      console.log('largest_changed_source=' + (largestChangedSource.file || '') + ':' + largestChangedSource.lines);\n      console.log('trace_count=' + result.traceCount);\n    }\n    process.exit(result.ok ? 0 : 1);\n  }\n  \n  try {\n    main();\n  } catch (error) {\n    console.error(error instanceof Error ? error.message : String(error));\n    process.exit(1);\n  }+}\u001b[0m\n  ",
  "preservedZones": [
    {
      "kind": "unchanged_context",
      "description": "Everything outside the modified zone is preserved byte-for-byte by the atomic operation."
    }
  ],
  "modifiedZones": [
    {
      "kind": "changed_span",
      "oldTextHash": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
      "newTextHash": "6d91b9b094019698d276c56daf022d5441e5c7f581272bbf4b2f61c14bc221ce",
      "description": "The operation changed the highlighted span shown in inlinePreview."
    }
  ],
  "movementZones": [],
  "semanticImpact": "unclassified_code_edit",
  "audit": {
    "whatChanged": "Operation `atomic_create_file` changed ~5462 character(s) in `docs/ai/atomic-os-benchmark/tools/refactor-scorecard.cjs` (sub-line atomic edit, expansion 0× — it did not rewrite surrounding code).",
    "whatPreserved": "Everything outside the highlighted [-removed-]{+added+} span in that one file is byte-identical. No other file was touched by this operation.",
    "howToValidate": "Read the atomicDiff above — it shows EXACTLY the changed characters, nothing else. Then exercise the affected behavior in the running product to confirm intent.",
    "notProven": "Runtime/product behavior is NOT proven by this tool. Structural validity ≠ correct behavior. Prove behavior by running the flow in the app.",
    "nonTouched": "Single-file, single-span: auth, payments, database, protected files, and every other source file are provably untouched by this operation.",
    "promiseClass": "structurally-validated",
    "zeroCodeTrust": 60,
    "trustCeilingReason": "ceiling < 75: an edit tool proves structure + exact scope, not product behavior. Reaching 75 (validate by explanation) or 100 (validate by the product) requires running the changed flow — do that in the app, not here."
  }
}