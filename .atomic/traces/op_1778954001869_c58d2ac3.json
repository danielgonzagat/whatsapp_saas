{
  "traceVersion": "1.0",
  "operationId": "op_1778954001869_c58d2ac3",
  "ts": "2026-05-16T17:53:21.869Z",
  "file": "scripts/mcp/atomic-edit/smoke.ts",
  "operation": "atomic_edit",
  "operator": "atomic_edit",
  "fallback": false,
  "metrics": {
    "changedChars": 2130,
    "lineRewriteSurfaceChars": 199,
    "expansionFactorAvoided": 0.09,
    "bytesNet": 1931,
    "lineRewriteAvoided": false
  },
  "validation": {
    "language": "ts",
    "syntaxErrorsBefore": 0,
    "syntaxErrorsAfter": 0
  },
  "afterSha256": "3839e1eb8c5a60fd868fb0a973fa98b52d79b9e92bc617e3518ddf5ac8829509",
  "rollback": {
    "available": true,
    "strategy": "explicit pre-edit snapshot (before-text retained by caller)"
  },
  "inlinePreview": "\u001b[2m--- scripts/mcp/atomic-edit/smoke.ts (atomic char-level)\u001b[0m\n      );\n      for (const f of [txAAbs, txBAbs]) if (fs.existsSync(f)) fs.unlinkSync(f);\n  \u001b[32m{+\n      // analyzer transaction: ESLint proposes fixes in dry-run mode, atomic-edit writes them.\n      const eslintRel = path.join('worker', `.smoke-eslint.${process.pid}.ts`);\n      const eslintAbs = path.join(repoRoot, eslintRel);\n      fs.writeFileSync(\n        eslintAbs,\n        'export function smoke(flag: boolean) {\\n  if (flag) return 1;\\n  return 0;\\n}\\n',\n      );\n      try {\n        const eslintTx = (await client.callTool({\n          name: 'atomic_apply_eslint_dry_run_fixes',\n          arguments: {\n            cwd: 'worker',\n            args: ['--fix-dry-run', '--format', 'json', path.basename(eslintRel)],\n            allowedPaths: [eslintRel],\n          },\n        })) as { content: { text: string }[]; isError?: boolean };\n        const eslintBody = JSON.parse(eslintTx.content.at(-1)?.text ?? '{}') as {\n          ok?: boolean;\n          filesWritten?: number;\n          traceRefs?: string[];\n        };\n        const eslintAfter = fs.readFileSync(eslintAbs, 'utf8');\n        check(\n          'eslint dry-run fixes are applied by atomic transaction',\n          eslintBody.ok === true &&\n            eslintBody.filesWritten === 1 &&\n            eslintAfter.includes('if (flag) {') &&\n            eslintAfter.includes('return 1;'),\n          eslintTx.content[0]?.text ?? '',\n        );\n        const firstTrace = eslintBody.traceRefs?.[0];\n        const traceAbs = firstTrace ? path.join(repoRoot, firstTrace) : '';\n        const traceBody = traceAbs && fs.existsSync(traceAbs) ? JSON.parse(fs.readFileSync(traceAbs, 'utf8')) : {};\n        check(\n          'eslint analyzer trace records preservation topology',\n          traceBody.targetUnit === 'eslint_dry_run_file_output' &&\n            traceBody.semanticImpact === 'behavior_preserving_lint_cleanup' &&\n            Array.isArray(traceBody.preservedZones) &&\n            traceBody.preservedZones.length >= 2,\n          JSON.stringify(traceBody),\n        );\n      } finally {\n        if (fs.existsSync(eslintAbs)) fs.unlinkSync(eslintAbs);\n      }+}\u001b[0m\n    } finally {\n      await client.close().catch(() => {});",
  "audit": {
    "whatChanged": "Operation `atomic_edit` changed ~2130 character(s) in `scripts/mcp/atomic-edit/smoke.ts` (sub-line atomic edit, expansion 0.09× — it did not rewrite surrounding code).",
    "whatPreserved": "Everything outside the highlighted [-removed-]{+added+} span in that one file is byte-identical. No other file was touched by this operation.",
    "howToValidate": "Read the atomicDiff above — it shows EXACTLY the changed characters, nothing else. Then exercise the affected behavior in the running product to confirm intent.",
    "notProven": "Runtime/product behavior is NOT proven by this tool. Structural validity ≠ correct behavior. Prove behavior by running the flow in the app.",
    "nonTouched": "Single-file, single-span: auth, payments, database, protected files, and every other source file are provably untouched by this operation.",
    "promiseClass": "structurally-validated",
    "zeroCodeTrust": 60,
    "trustCeilingReason": "ceiling < 75: an edit tool proves structure + exact scope, not product behavior. Reaching 75 (validate by explanation) or 100 (validate by the product) requires running the changed flow — do that in the app, not here."
  }
}