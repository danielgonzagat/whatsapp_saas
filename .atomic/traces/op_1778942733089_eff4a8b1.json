{
  "traceVersion": "1.0",
  "operationId": "op_1778942733089_eff4a8b1",
  "ts": "2026-05-16T14:45:33.089Z",
  "file": ".opencode/plugin/workspace-gates.ts",
  "operation": "atomic_edit",
  "operator": "atomic_edit",
  "fallback": false,
  "metrics": {
    "changedChars": 1396,
    "lineRewriteSurfaceChars": 59,
    "expansionFactorAvoided": 0.04,
    "bytesNet": 1337,
    "lineRewriteAvoided": false
  },
  "validation": {
    "language": "ts",
    "syntaxErrorsBefore": 0,
    "syntaxErrorsAfter": 0
  },
  "afterSha256": "168848651c7b8053b957f8cdaad99bba3ebc5bc697f37046529e45ed8dc3aa89",
  "rollback": {
    "available": true,
    "strategy": "explicit pre-edit snapshot (before-text retained by caller)"
  },
  "inlinePreview": "\u001b[2m--- .opencode/plugin/workspace-gates.ts (atomic char-level)\u001b[0m\n  }\n  \n  \u001b[32m{+// TUI-abolished law (hard enforcement): native code edits must be refused so\n  // OpenCode never renders a whole-line +/- diff for code. Reuses the SAME\n  // single-source hook as Claude Code (.claude/settings.json PreToolUse) and\n  // Codex (.codex/hooks.json): scripts/mcp/atomic-edit/atomic-only-hook.mjs.\n  // That hook always exits 0 and signals the decision as JSON on stdout, so the\n  // exit-code-based runHook() above cannot read it — parse stdout explicitly and\n  // abort on \"deny\". Steers every code mutation to mcp__atomic-edit__*.\n  function runAtomicGate(payload: object): { deny: boolean; reason: string } {\n    const scriptAbs = path.join(repoRoot, 'scripts/mcp/atomic-edit/atomic-only-hook.mjs');\n    if (!existsSync(scriptAbs)) return { deny: false, reason: '' };\n    const r = spawnSync('node', [scriptAbs], {\n      input: JSON.stringify(payload),\n      encoding: 'utf8',\n      cwd: repoRoot,\n    });\n    try {\n      const out = JSON.parse(r.stdout || '{}');\n      const decision = out?.hookSpecificOutput;\n      if (decision?.permissionDecision === 'deny') {\n        return {\n          deny: true,\n          reason: String(decision.permissionDecisionReason || 'native code edit banned (TUI-abolished)'),\n        };\n      }\n    } catch {\n      /* malformed stdout → fail-open here; the workspace gate still runs after */\n    }\n    return { deny: false, reason: '' };\n  }\n  +}\u001b[0m\n  const PreToolUsePlugin = {\n    'tool.execute.before': async (input: AnyInput, output: AnyOutput) => {",
  "audit": {
    "whatChanged": "Operation `atomic_edit` changed ~1396 character(s) in `.opencode/plugin/workspace-gates.ts` (sub-line atomic edit, expansion 0.04× — it did not rewrite surrounding code).",
    "whatPreserved": "Everything outside the highlighted [-removed-]{+added+} span in that one file is byte-identical. No other file was touched by this operation.",
    "howToValidate": "Read the atomicDiff above — it shows EXACTLY the changed characters, nothing else. Then exercise the affected behavior in the running product to confirm intent.",
    "notProven": "Runtime/product behavior is NOT proven by this tool. Structural validity ≠ correct behavior. Prove behavior by running the flow in the app.",
    "nonTouched": "Single-file, single-span: auth, payments, database, protected files, and every other source file are provably untouched by this operation.",
    "promiseClass": "structurally-validated",
    "zeroCodeTrust": 60,
    "trustCeilingReason": "ceiling < 75: an edit tool proves structure + exact scope, not product behavior. Reaching 75 (validate by explanation) or 100 (validate by the product) requires running the changed flow — do that in the app, not here."
  }
}