name: Legacy CI (disabled)

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado desativado; use ci-cd.yml"
