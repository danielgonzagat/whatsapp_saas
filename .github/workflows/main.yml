name: Legacy CI (disabled)

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Workflow legado desativado; use ci-cd.yml"
