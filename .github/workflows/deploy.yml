name: Legacy Deploy (disabled)

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Workflow legado desativado."
          echo "Use .github/workflows/deploy-staging.yml para staging e .github/workflows/deploy-production.yml para produção."
