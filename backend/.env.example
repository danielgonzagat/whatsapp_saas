# ============================================
# KLOEL BACKEND - Variáveis de Ambiente
# ============================================

# ============================================
# Database (PostgreSQL + pgvector)
# ============================================
DATABASE_URL=postgresql://postgres:password@localhost:5432/whatsapp_saas
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=whatsapp_saas

# ============================================
# Redis (Cache + Queues)
# ============================================
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# 🛡️ Rate Limit (Auth) + Abuso
# ============================================
# Os endpoints /auth/* possuem rate limit.
# - Recomendado em produção: configurar REDIS_URL (rate limit distribuído + filas/queues).
# - Se Redis não estiver configurado/indisponível, o backend usa fallback local (por processo)
#   e loga WARN claro (não quebra login, evita abuso óbvio, mas não escala entre instâncias).

# ============================================
# JWT Authentication
# ============================================
# IMPORTANTE: Altere para uma chave segura em produção!
JWT_SECRET=change-me-to-a-secure-random-string
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ============================================
# Auth Mode
# ============================================
# Set to "true" to make all authentication optional (dev/test only)
AUTH_OPTIONAL=false
# Set to "true" to require authentication for all endpoints
AUTH_REQUIRED=true

# ============================================
# Google Sign-In (GIS)
# ============================================
# Fluxo suportado: o frontend recebe a credential (ID token) do Google Identity Services
# e o backend valida esse token em /auth/oauth/google.
GOOGLE_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
# Opcional: espelhe o mesmo client id usado no frontend para evitar divergência
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
# Opcional: lista CSV para aceitar client IDs diferentes em produção/preview/local
GOOGLE_ALLOWED_CLIENT_IDS=
# Opcional neste fluxo específico (mantido para Calendar/code flow)
GOOGLE_CLIENT_SECRET=your-google-client-secret
ENCRYPTION_KEY=change-me-32-bytes-minimum
LEGAL_DPO_EMAIL=privacy@kloel.com
LEGAL_SUPPORT_EMAIL=ajuda@kloel.com
NEXT_PUBLIC_SITE_URL=https://kloel.com

# ============================================
# WAHA (WhatsApp HTTP API)
# ============================================
WHATSAPP_PROVIDER_DEFAULT=meta-cloud
WORKER_BROWSER_RUNTIME_URL=http://worker:3003
WHATSAPP_BROWSER_HEADLESS=true
WHATSAPP_BROWSER_PROFILE_DIR=/data/browser-sessions
WHATSAPP_CUA_PROVIDER=openai
WHATSAPP_CUA_MODE=hybrid
WHATSAPP_IDLE_INTERVAL_MS=45000
WHATSAPP_ACTIVE_INTERVAL_MS=4000
WHATSAPP_ACTIVE_TO_IDLE_MS=60000
WHATSAPP_CHECKPOINT_TTL_SECONDS=604800
WHATSAPP_TYPING_DELAY_MIN_MS=30
WHATSAPP_TYPING_DELAY_MAX_MS=80
SCREENCAST_WS_PORT=3004
SCREENCAST_QUALITY=70
SCREENCAST_MAX_WIDTH=1440
SCREENCAST_MAX_HEIGHT=900
SCREENCAST_EVERY_NTH_FRAME=1
SCREENCAST_SHARED_SECRET=change-me
SCREENCAST_MAX_VIEWERS_PER_WORKSPACE=4
SCREENCAST_TOKEN_TTL_SECONDS=120
WHATSAPP_LIVE_SCREEN_WRITE_INTERVAL_MS=250
WHATSAPP_FRAME_ARCHIVE_INTERVAL_MS=1000

# Instância WAHA Plus
# Use a URL pública do seu serviço WAHA (sem /api no final)
WAHA_API_URL=https://seu-waha.up.railway.app
WAHA_CATCHUP_INCLUDE_ZERO_UNREAD_ACTIVITY=true
WAHA_CATCHUP_FALLBACK_CHATS_PER_PASS=25
WAHA_CATCHUP_FIRST_RUN_LOOKBACK_MS=2592000000
WAHA_CATCHUP_SEND_SEEN_ON_IMPORT=false
WAHA_CATCHUP_MARK_READ_WITHOUT_REPLY=false
WAHA_WATCHDOG_CONNECTED_MAINTENANCE_INTERVAL_SECONDS=300
CIA_BOOTSTRAP_INCLUDE_ZERO_UNREAD_ACTIVITY=true
CIA_ENABLE_PROACTIVE_CYCLE=false
ALLOW_PROACTIVE_OUTREACH=false
# Alias legado aceito pelo runtime
WAHA_BASE_URL=https://seu-waha.up.railway.app

# Chave de API para autenticação
WAHA_API_KEY=your-waha-api-key
# Alias legado aceito pelo runtime
WAHA_API_TOKEN=your-waha-api-key

# Sessões por workspace: obrigatório para multi-tenant
WAHA_MULTISESSION=true
WAHA_USE_WORKSPACE_SESSION=true
WAHA_SINGLE_SESSION=false

# NOWEB store: obrigatório para catch-up/backfill estável em engine NOWEB
WAHA_NOWEB_STORE_ENABLED=true
WAHA_NOWEB_STORE_FULL_SYNC=true
# Compatibilidade com payloads legados do WAHA
WAHA_STORE_ENABLED=true
WAHA_STORE_FULL_SYNC=true

# Webhook global do WAHA -> backend
# Recomendado em produção para garantir inbound, status e catch-up automáticos
WHATSAPP_HOOK_URL=https://seu-backend.up.railway.app/webhooks/whatsapp-api
WHATSAPP_HOOK_EVENTS=session.status,message,message.any,message.ack
# Exemplo de header customizado no WAHA:
# WHATSAPP_HOOK_CUSTOM_HEADERS=X-Api-Key:my-webhook-secret

# Segredo validado pelo backend no webhook (x-api-key/x-webhook-secret)
WHATSAPP_API_WEBHOOK_SECRET=my-webhook-secret
# Permite subir sessão sem webhook apenas em cenários muito específicos de diagnóstico.
WAHA_ALLOW_SESSION_WITHOUT_WEBHOOK=false
# Permite URL interna/privada para webhook somente em ambientes controlados.
WAHA_ALLOW_INTERNAL_WEBHOOK_URL=false

# Health do worker para o backend consolidar readiness
WORKER_HEALTH_URL=http://worker:3003/health
WORKER_METRICS_TOKEN=change-me-worker-metrics-token
WORKER_FORCE_AVAILABLE=

# Controles de compliance do envio autônomo.
# `reactive` sempre responde a quem acabou de falar; `proactive` respeita essas flags.
ENFORCE_OPTIN=false
AUTOPILOT_ENFORCE_24H=false

# ============================================
# Stripe (Pagamentos)
# ============================================
# Obter em: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
# Production live mode requires explicit human-confirmed enablement:
# KLOEL_LIVE_MODE=confirmed
KLOEL_MARKETPLACE_FEE_PERCENT=9.9
KLOEL_INSTALLMENT_INTEREST_MONTHLY_PERCENT=3.99
FRAUD_BLOCK_THRESHOLD=0.8
FRAUD_REVIEW_THRESHOLD=0.5
FRAUD_REQUIRE_3DS_THRESHOLD=0.3
FRAUD_MISSING_IDENTIFIER_SCORE=0.4
FRAUD_HIGH_AMOUNT_SCORE=0.3
FRAUD_FOREIGN_BIN_SCORE=0.35
FRAUD_HIGH_AMOUNT_3DS_CENTS=100000
FRAUD_VELOCITY_WINDOW_MINUTES=10
FRAUD_VELOCITY_MAX_ATTEMPTS_PER_IP=5
FRAUD_VELOCITY_MAX_ATTEMPTS_PER_DEVICE=5
FRAUD_VELOCITY_MAX_ATTEMPTS_PER_EMAIL=4
FRAUD_VELOCITY_MAX_ATTEMPTS_PER_DOCUMENT=3

# Price IDs para cada plano
STRIPE_PRICE_STARTER=price_xxx
STRIPE_PRICE_PRO=price_xxx
STRIPE_PRICE_ENTERPRISE=price_xxx
PREPAID_WALLET_CARD_TOPUP_FEE_BPS=500
PREPAID_WALLET_CARD_TOPUP_FIXED_CENTS=100

# Trial configuration
TRIAL_DAYS=7
TRIAL_CREDITS_USD=10

# Multiple webhook secrets (CSV for multi-account setups)
STRIPE_WEBHOOK_SECRETS=whsec_xxx

# Modo mock (para desenvolvimento sem Stripe real)
BILLING_MOCK_MODE=true

# Feature flag: how payment failures behave
# "fail-closed" = reject all when unknown state
FF_PAYMENT__FAILCLOSEDUNKNOWNSTATE=true

# ============================================
# OpenAI (brain/writer split para Kloel/Autopilot)
# ============================================
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=
INTERNAL_API_KEY=
OPENAI_BRAIN_MODEL=gpt-5.4
OPENAI_BRAIN_FALLBACK_MODEL=gpt-4.1
OPENAI_WRITER_MODEL=gpt-5.4-nano-2026-03-17
OPENAI_WRITER_FALLBACK_MODEL=gpt-4.1
OPENAI_AUDIO_UNDERSTANDING_MODEL=gpt-tempo-real-1.5
OPENAI_AUDIO_UNDERSTANDING_FALLBACK_MODEL=gpt-4o-mini-transcribe
OPENAI_MODEL=gpt-5.4-nano-2026-03-17
OPENAI_FALLBACK_MODEL=gpt-4.1
OPENAI_TTS_VOICE=nova
OPENAI_TTS_SPEED=1.0
# LLM budget/spend controls
LLM_BUDGET_DEFAULT_CENTS=100
LLM_MAX_INPUT_CHARS=32000
LLM_MAX_COMPLETION_TOKENS=4096
# Think timeout for structured reasoning (ms)
KLOEL_THINK_TIMEOUT_MS=30000
# Heartbeat interval for streaming responses (ms)
KLOEL_STREAM_HEARTBEAT_MS=15000
# LLM stub mode (returns mock responses, dev only)
KLOEL_LLM_STUB=false
# Web search stub mode (returns mock results, dev only)
KLOEL_WEB_SEARCH_STUB=false
VOICE_RESPONSE_AUDIO_REQUIRED=false
CIA_INLINE_BACKLOG_FALLBACK_LIMIT=10
CIA_BOOTSTRAP_AUTO_CONTINUE=true
CIA_BOOTSTRAP_AUTO_CONTINUE_LIMIT=500

# ============================================
# Cloudflare R2 Storage
# ============================================
STORAGE_DRIVER=r2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=kloel-uploads
R2_ENDPOINT=https://ACCOUNT_ID.r2.cloudflarestorage.com
R2_PUBLIC_URL=

# ============================================
# Email (Resend + SMTP fallback)
# ============================================
RESEND_API_KEY=re_xxx
# Alternative: SendGrid
SENDGRID_API_KEY=SG.xxx
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=KLOEL <noreply@kloel.com>
EMAIL_FROM_NAME=Kloel
# Secret used to sign unsubscribe links (HMAC)
EMAIL_UNSUBSCRIBE_SECRET=change-me-unsubscribe-secret

# ============================================
# Frontend URL (para redirects)
# ============================================
FRONTEND_URL=http://localhost:3000

# ============================================
# Prisma Migrations (produção)
# ============================================
# Em produção, o backend executa migrations automaticamente no startup

# ============================================
# Datadog APM
# ============================================
DD_SERVICE=kloel-backend
DD_ENV=development
DD_VERSION=1.0.0
# Opcional: use quando o Agent não estiver no localhost:8126
# Ex.: backend em container Docker no macOS -> http://host.docker.internal:8126
# DD_TRACE_AGENT_URL=http://localhost:8126
# (Docker CMD e script start:prod).
# Se o schema não estiver pronto, endpoints de autenticação retornam 503
# com mensagem clara (em vez de falhar com erro genérico).
# ============================================
# Sentry (Monitoramento de erros - opcional)
# ============================================
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
METRICS_TOKEN=change-me-metrics-token
DIAG_TOKEN=change-me-diagnostic-token
OPS_WEBHOOK_URL=
DLQ_WEBHOOK_URL=

# ============================================
# Ambiente
# ============================================
NODE_ENV=development
PORT=3001

# ============================================
# Autopilot Limits
# ============================================
AUTOPILOT_CONTACT_DAILY_LIMIT=5
AUTOPILOT_WORKSPACE_DAILY_LIMIT=1000
AUTOPILOT_SILENCE_HOURS=24
AUTOPILOT_CYCLE_CRON=* * * * *
AUTOPILOT_CONTACT_DEBOUNCE_MS=2000
AUTOPILOT_PENDING_MESSAGE_LIMIT=12
AUTOPILOT_SHARENON_DIGIT_REPLY_LOCK_MS=60000
ENABLE_LEGACY_AUTOPILOT_SCANNER=false
AUTOPILOT_BYPASS_REACTIVE_COMPLIANCE=true
AUTOPILOT_BYPASS_REACTIVE_RATELIMITS=true

# Catch-up/backfill WAHA
WAHA_CATCHUP_LOOKBACK_MS=43200000
WAHA_CATCHUP_MAX_CHATS=200
WAHA_CATCHUP_MAX_MESSAGES_PER_CHAT=100
WAHA_CATCHUP_MAX_PAGES_PER_CHAT=10
WAHA_CATCHUP_MAX_PASSES=5

# ============================================
# Rate Limiting
# ============================================
THROTTLE_TTL=60
THROTTLE_LIMIT=100
# Set to "true" to disable all rate limiting (dev/test only)
RATE_LIMIT_DISABLED=false

# ============================================
# Additional Application URLs
# ============================================
# Public-facing backend URL (used for OAuth redirects, webhooks, etc.)
BACKEND_PUBLIC_URL=http://localhost:3001
# Main application URL (alias for FRONTEND_URL)
APP_URL=http://localhost:3000
# API URL alias (same as BACKEND_URL)
API_URL=http://localhost:3001
# Allowed origins for CORS (CSV, alias for CORS_ALLOWED_ORIGINS)
ALLOWED_ORIGINS=http://localhost:3000
# Checkout/payment page domain
CHECKOUT_DOMAIN=https://pay.kloel.com
# Set to "true" to stub checkout payment responses (dev/test)
CHECKOUT_PAYMENT_STUB=false

# ============================================
# Guest Chat (chat público sem login)
# ============================================
# Habilitado por padrão. Defina como "false" para desabilitar em produção.
GUEST_CHAT_ENABLED=true

# ============================================
# CORS - Origens extras (CSV)
# ============================================
# Adicione domínios extras separados por vírgula (ex.: Railway, custom domain).
# As origens padrão (kloel.com, vercel, localhost) já estão incluídas.
# CORS_ALLOWED_ORIGINS=https://myapp.up.railway.app,https://custom.domain.com

# Regex patterns para origens dinâmicas (CSV de regex, ex.: Vercel preview deploys).
# O padrão ^https://kloel-frontend-.*\.vercel\.app$ já está incluído por default.
# CORS_ALLOWED_ORIGIN_REGEX=^https://my-other-app-.*\.vercel\.app$

# ============================================
# Meta Platform (Facebook / Instagram / WhatsApp Cloud)
# ============================================
META_APP_ID=
META_APP_SECRET=
META_AUTH_APP_ID=
META_AUTH_APP_SECRET=
META_CONFIG_ID=
META_VERIFY_TOKEN=
META_GRAPH_API_VERSION=v21.0
NEXT_PUBLIC_META_APP_ID=
NEXT_PUBLIC_META_AUTH_APP_ID=
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
META_ACCESS_TOKEN=
META_PHONE_NUMBER_ID=
META_WABA_ID=

# ============================================
# Canvas (Polotno)
# ============================================
NEXT_PUBLIC_POLOTNO_KEY=your_polotno_key_from_polotno.com/cabinet

# ============================================
# Health/Readiness
# ============================================
# GET /health          -> liveness simples
# GET /health/system   -> health consolidado (DB, Redis, WAHA, worker, config)
# GET /health/ready    -> alias de readiness

# ============================================
# Admin module — adm.kloel.com (SP-0..2)
# ============================================
# Dedicated JWT secret for admin sessions. Must be distinct from
# AUTH_SECRET/JWT_SECRET so a compromise of the user JWT does NOT leak admin
# sessions (invariant I-ADMIN-4). Generate with: openssl rand -hex 32
ADMIN_JWT_SECRET=

# AES-256-GCM key for encrypting MFA secrets at rest (invariant I-ADMIN-3).
# 32-byte hex, distinct from ADMIN_JWT_SECRET. Generate with: openssl rand -hex 32
ADMIN_MFA_ENCRYPTION_KEY=

# Issuer label shown in authenticator apps (Google Authenticator, 1Password,
# Authy). Displayed next to the account email.
ADMIN_MFA_ISSUER=Kloel Admin

# Session lifetime for admin refresh tokens. Forces re-login after this
# many hours of inactivity.
ADMIN_SESSION_TTL_HOURS=8

# Access-token lifetime in minutes. Short by design — refresh token rotates
# on every refresh call.
ADMIN_ACCESS_TOKEN_TTL_MINUTES=15

# Comma-separated list of origins allowed for the admin surface. Used by the
# CORS config to accept requests from adm.kloel.com only.
ADMIN_ALLOWED_ORIGINS=https://adm.kloel.com

# Set to `false` to disable the idempotent OWNER seed on boot. Defaults to
# `true` — leave it on unless you are running an integration test that wants
# a clean slate.
ADMIN_SEED_OWNER_ENABLED=true

# ============================================
# Webhook Secrets (Payments / E-Commerce)
# ============================================
PAYMENT_WEBHOOK_SECRET=change-me-payment-webhook
SHOPIFY_WEBHOOK_SECRET=change-me-shopify-webhook
PAGHIPER_WEBHOOK_TOKEN=change-me-paghiper-token
WC_WEBHOOK_SECRET=change-me-woocommerce-webhook
HOOKS_WEBHOOK_SECRET=change-me-hooks-webhook

# ============================================
# Feature Flags (Fail-Closed / Fail-Open)
# ============================================
FF_AUTH__FAILCLOSEDRATELIMIT=true
FF_WHATSAPP__STRICTLOCK=false
KLOEL_LIVE_MODE=development

# ============================================
# Redis — Alternate Connection Formats
# ============================================
REDIS_FALLBACK_URL=
REDIS_HOSTNAME=
REDIS_USER=
REDIS_USERNAME=
REDIS_PASS=
REDIS_MODE=
# Railway auto-provided aliases (do not set manually)
REDISHOST=
REDISPORT=
REDISUSER=
REDISPASSWORD=

# ============================================
# Autopilot — Extended Controls
# ============================================
AUTOPILOT_ALERT_WEBHOOK=https://hooks.slack.com/xxx
AUTOPILOT_SHARED_REPLY_LOCK_MS=60000
AUTOPILOT_WINDOW_START=8
AUTOPILOT_WINDOW_END=22
AUTOPILOT_TEST_PHONE=+5511999999999
AUTOPILOT_RECLAIM_HUMAN_LOCK_ON_INBOUND=false
AUTOPILOT_INLINE_REACTIVE=true
AUTOPILOT_CYCLE_LIMIT=100
AUTOPILOT_BYPASS_HUMAN_LOCK=false
AUTOPILOT_QUEUE_WAITING_THRESHOLD=10
ENABLE_LEGACY_BACKEND_AUTOPILOT=false

# ============================================
# CIA — Conversational Intelligence Agent Runtime
# ============================================
CIA_BOOTSTRAP_IMMEDIATE_LIMIT=10
CIA_BOOTSTRAP_REMOTE_LOOKBACK_MS=86400000
CIA_CONTACT_CATALOG_LOOKBACK_DAYS=30
CIA_DAILY_MESSAGE_LIMIT=500
CIA_PRESENCE_HEARTBEAT_MS=30000
CIA_REMOTE_PENDING_MAX_AGE_MS=3600000
CIA_REMOTE_UNKNOWN_PENDING_MAX_AGE_MS=7200000
CIA_RUNTIME_STALE_RUN_MS=300000

# ============================================
# WhatsApp / WAHA — Extended Configuration
# ============================================
WAHA_URL=https://seu-waha.up.railway.app
WAHA_CATCHUP_FALLBACK_PAGES_PER_CHAT=3
WAHA_CATCHUP_MIN_TRIGGER_INTERVAL_SECONDS=60
WAHA_CATCHUP_SWEEP_LIMIT=50
WAHA_LID_MAP_CACHE_TTL_MS=86400000
WAHA_SELF_IDENTITY_TTL_MS=3600000
WAHA_WATCHDOG_RECONNECT_LOCK_TTL_SECONDS=60
WAHA_WATCHDOG_RUN_ON_STARTUP=true
WAHA_WEBHOOK_SECRET=change-me-waha-webhook
WHATSAPP_VERIFY_TOKEN=change-me-whatsapp-verify
WHATSAPP_ACTION_LOCK_MS=5000
WHATSAPP_ACTION_LOCK_TEST_ENFORCE=false

# ============================================
# Railway Deploy Metadata (auto-provided)
# ============================================
RAILWAY_GIT_COMMIT_SHA=
RAILWAY_PROJECT_ID=
RAILWAY_DEPLOYMENT_ID=
RAILWAY_ENVIRONMENT_ID=
RAILWAY_SERVICE_ID=
RAILWAY_PUBLIC_DOMAIN=
RAILWAY_REPLICA_ID=
RAILWAY_STATIC_URL=

# ============================================
# Storage / CDN
# ============================================
CDN_BASE_URL=https://cdn.kloel.com
MEDIA_BASE_URL=https://media.kloel.com

# ============================================
# Sentry — Extended Configuration
# ============================================
SENTRY_RELEASE=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.1

# ============================================
# Datadog APM — Extended Configuration
# ============================================
DD_API_KEY=
DATADOG_API_KEY=

# ============================================
# PULSE — System Observability & Health
# ============================================
PULSE_STALE_SWEEP_MS=300000
PULSE_RUNTIME_TOKEN=change-me-pulse-runtime
PULSE_FRONTEND_PRUNE_MS=60000
PULSE_BACKEND_HEARTBEAT_MS=60000
PULSE_WORKER_HEARTBEAT_MS=60000

# ============================================
# Knowledge Base (Kloel KB)
# ============================================
KB_FETCH_MAX_BYTES=5242880
KB_MAX_CHUNKS=100
KB_FETCH_TIMEOUT_MS=30000
KB_URL_ALLOWLIST=

# ============================================
# Mass Send / Campaigns
# ============================================
MASS_SEND_JITTER_MIN_MS=1000
MASS_SEND_JITTER_MAX_MS=5000

# ============================================
# E2E / Testing
# ============================================
E2E_TEST_MODE=false

# ============================================
# Calendar (Google Calendar Integration)
# ============================================
DEFAULT_CALENDAR_LINK=https://calendly.com/seu-link

# ============================================
# Audio
# ============================================
AUDIO_FETCH_ALLOWLIST=

# ============================================
# Observability (Webhooks)
# ============================================
AUDIT_WEBHOOK_URL=https://hooks.slack.com/xxx

# ============================================
# Swagger / API Docs
# ============================================
SWAGGER_BASIC_USER=admin
SWAGGER_BASIC_PASS=change-me-swagger

# ============================================
# Worker Health Monitoring
# ============================================
WORKER_HEALTH_CACHE_MS=30000
WORKER_HEALTH_TIMEOUT_MS=10000

# ============================================
# Meta — Additional Tokens
# ============================================
META_TOKEN=
META_WEBHOOK_VERIFY_TOKEN=
# Encryption key for provider tokens (Meta, etc.) — 32-byte hex
PROVIDER_SECRET_KEY=

# ============================================
# CI / Platform (auto-provided by build/deploy systems)
# ============================================
CI=
GITHUB_SHA=
VERCEL_GIT_COMMIT_SHA=

# ============================================
# CORS - Origens extras (CSV)
# ============================================
CORS_ALLOWED_ORIGINS=https://myapp.up.railway.app
CORS_ALLOWED_ORIGIN_REGEX=^https://(.*\\.)?kloel\\.com$
