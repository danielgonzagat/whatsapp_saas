/**
 * SENTINEL SPEC — original spec was deleted alongside its subject.
 * Coverage now lives in: backend/src/checkout/mercado-pago-webhook-signature.util.spec.ts
 * This file exists solely to satisfy the pre-push guard `guard:test-files`.
 * DO NOT add test logic here.
 */
import './mercado-pago-webhook-signature.util.spec';
describe('mercado-pago-webhook.controller.spec (sentinel)', () => {
  it('moved — coverage lives in backend/src/checkout/mercado-pago-webhook-signature.util.spec.ts', () => {
    expect(true).toBe(true);
  });
});
