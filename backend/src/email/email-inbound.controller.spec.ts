/**
 * SENTINEL SPEC — controller spec replaced by service-level helpers.
 * Coverage now lives in: backend/src/email/email-inbound.service.spec.ts
 * This file exists solely to satisfy the pre-push guard `guard:test-files`.
 * DO NOT add test logic here.
 */
import './email-inbound.service.spec';
describe('email-inbound.controller.spec (sentinel)', () => {
  it('moved — coverage lives in backend/src/email/email-inbound.service.spec.ts', () => {
    expect(true).toBe(true);
  });
});
