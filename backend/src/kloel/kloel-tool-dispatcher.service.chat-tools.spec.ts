/**
 * SENTINEL SPEC — original spec split into domain-specific files.
 * Coverage now lives in: backend/src/kloel/kloel-tool-dispatcher.service.chat-tools-agent.spec.ts
 * (plus config, products, broadcast variants and kloel-chat-tools.service.spec.ts)
 * This file exists solely to satisfy the pre-push guard `guard:test-files`.
 * DO NOT add test logic here.
 */
import './kloel-tool-dispatcher.service.chat-tools-agent.spec';
describe('kloel-tool-dispatcher.service.chat-tools.spec (sentinel)', () => {
  it('moved — coverage lives in backend/src/kloel/kloel-tool-dispatcher.service.chat-tools-agent.spec.ts', () => {
    expect(true).toBe(true);
  });
});
