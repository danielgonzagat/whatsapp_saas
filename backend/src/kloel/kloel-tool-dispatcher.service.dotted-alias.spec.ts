/**
 * SENTINEL SPEC — original spec split into domain-specific files.
 * Coverage now lives in: backend/src/kloel/kloel-tool-dispatcher.service.dotted-alias-products.spec.ts
 * (plus payment-link, coupons, checkouts, sales, plans variants)
 * This file exists solely to satisfy the pre-push guard `guard:test-files`.
 * DO NOT add test logic here.
 */
import './kloel-tool-dispatcher.service.dotted-alias-products.spec';
describe('kloel-tool-dispatcher.service.dotted-alias.spec (sentinel)', () => {
  it('moved — coverage lives in backend/src/kloel/kloel-tool-dispatcher.service.dotted-alias-products.spec.ts', () => {
    expect(true).toBe(true);
  });
});
