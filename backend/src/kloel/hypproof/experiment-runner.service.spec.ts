/**
 * SENTINEL SPEC — original spec replaced by combined hypproof suite.
 * Coverage now lives in: backend/src/kloel/hypproof/hypproof.spec.ts
 * This file exists solely to satisfy the pre-push guard `guard:test-files`.
 * DO NOT add test logic here.
 */
import './hypproof.spec';
describe('experiment-runner.service.spec (sentinel)', () => {
  it('moved — coverage lives in backend/src/kloel/hypproof/hypproof.spec.ts', () => {
    expect(true).toBe(true);
  });
});
