/**
 * PlanService — Re-exports the canonical PlanService from
 * backend/src/plans/plan.service.ts.
 *
 * All chat tools and UI controllers MUST use this same service.
 *
 * @see ../plans/plan.service.ts for the canonical implementation.
 */
export { PlanService } from '../plans/plan.service';
