/**
 * @deprecated Moved to backend/src/marketing/channels/whatsapp/providers/provider-registry-session.ts
 * as part of the omnicore canonicalization. Import from the new location instead.
 * This stub re-exports the public API for backwards compatibility and will be
 * removed in a future cleanup wave.
 */
export {
  readRecord,
  persistSessionSnapshot,
  startSession,
  getSessionStatus,
  getQrCode,
  type SessionDeps,
} from '../../marketing/channels/whatsapp/providers/provider-registry-session';
