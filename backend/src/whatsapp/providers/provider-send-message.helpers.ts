/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/providers/provider-send-message.helpers.ts`.
 * Import from the canonical marketing/channels path instead. This stub re-exports the
 * symbols so legacy callers keep building during the omnicore consolidation
 * (per ADR-0012 OmniCore Wave W3 — Wave 25).
 */
export * from '../../marketing/channels/whatsapp/providers/provider-send-message.helpers';
