/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/providers/waha-session.helpers.ts`.
 * Import from the canonical marketing/channels path instead. This stub re-exports the
 * symbols so legacy callers keep building during the omnicore consolidation.
 */
export type {
  ListSessionsHelperDeps,
  LogoutSessionHelperDeps,
} from '../../marketing/channels/whatsapp/providers/waha-session.helpers';
export {
  listSessionsHelper,
  logoutSessionHelper,
} from '../../marketing/channels/whatsapp/providers/waha-session.helpers';
