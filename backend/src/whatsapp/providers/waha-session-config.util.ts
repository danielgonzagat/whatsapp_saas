/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/providers/waha-session-config.util.ts`.
 * Update imports to the new path. This stub will be removed after the migration window.
 */
export {
  normalizeEventList,
  resolveWebhookDiagnosticsFromConfig,
  resolveStoreDiagnosticsFromConfig,
  resolveSessionConfigMismatch,
  extractLidMappingsPayload,
  type WahaSessionConfigShape,
} from '../../marketing/channels/whatsapp/providers/waha-session-config.util';
