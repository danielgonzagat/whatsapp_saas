/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/whatsapp-send-rate-guard.service.ts`.
 *
 * This stub remains for backwards-compatible imports during the omnicore
 * marketing-channels consolidation. New imports MUST target the new path:
 *
 *   import { WhatsappSendRateGuardService } from
 *     '../marketing/channels/whatsapp/whatsapp-send-rate-guard.service';
 *
 * Delete this file once no consumers reference `backend/src/whatsapp/whatsapp-send-rate-guard.service`.
 */
export { WhatsappSendRateGuardService } from '../marketing/channels/whatsapp/whatsapp-send-rate-guard.service';
