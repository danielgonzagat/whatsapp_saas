/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/whatsapp-watchdog-recovery.service.ts`.
 *
 * This stub remains for backwards-compatible imports during the omnicore
 * marketing-channels consolidation. New imports MUST target the new path:
 *
 *   import { WhatsAppWatchdogRecoveryService } from
 *     '../marketing/channels/whatsapp/whatsapp-watchdog-recovery.service';
 *
 * Delete this file once no consumers reference
 * `backend/src/whatsapp/whatsapp-watchdog-recovery.service`.
 */
export { WhatsAppWatchdogRecoveryService } from '../marketing/channels/whatsapp/whatsapp-watchdog-recovery.service';
