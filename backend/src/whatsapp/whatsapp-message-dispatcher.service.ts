/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/whatsapp-message-dispatcher.service.ts`.
 * Import from the new path. This re-export stub will be removed once all callers migrate.
 */
export { WhatsappMessageDispatcherService } from '../marketing/channels/whatsapp/whatsapp-message-dispatcher.service';
