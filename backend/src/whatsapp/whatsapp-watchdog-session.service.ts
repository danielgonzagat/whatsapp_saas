/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/whatsapp-watchdog-session.service.ts`.
 *
 * This stub remains for backwards-compatible imports during the omnicore
 * marketing-channels consolidation. New imports MUST target the new path:
 *
 *   import { WhatsAppWatchdogSessionService } from
 *     '../marketing/channels/whatsapp/whatsapp-watchdog-session.service';
 *
 * Delete this file once no consumers reference
 * `backend/src/whatsapp/whatsapp-watchdog-session.service`.
 */
export {
  WhatsAppWatchdogSessionService,
  type SessionHealth,
} from '../marketing/channels/whatsapp/whatsapp-watchdog-session.service';
