/**
 * @deprecated Moved to `backend/src/marketing/channels/whatsapp/whatsapp-watchdog.service.ts`.
 *
 * This stub remains for backwards-compatible imports during the omnicore
 * marketing-channels consolidation. New imports MUST target the new path:
 *
 *   import { WhatsAppWatchdogService } from
 *     '../marketing/channels/whatsapp/whatsapp-watchdog.service';
 *
 * Delete this file once no consumers reference `backend/src/whatsapp/whatsapp-watchdog.service`.
 */
export { WhatsAppWatchdogService } from '../marketing/channels/whatsapp/whatsapp-watchdog.service';
