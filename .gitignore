# Throwaway debug scripts (prefix _tmp_)
scripts/_tmp_*

# Logs
*.log
!.dr-test.log
!.backup-validation.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Build outputs
dist/
build/
.next/
.turbo/
# Dependencies
node_modules/
# Environment — block ALL .env variants except examples
.env
.env.*
!.env.example
!.env.reference
# Tokens / credentials
tokens/
backend/tokens/
*_data/
# Editor/OS
.DS_Store
Thumbs.db
.idea/
.vscode/
# Playwright / E2E artifacts
e2e/playwright-report/
e2e/test-results/
e2e/.e2e-auth*.json
e2e/.e2e-auth*.json.lock
# Per-workspace junit reports emitted for Codecov test analytics upload
backend/test-results/
frontend/test-results/
worker/test-results/
test-results/
screenshots/
# HUD orchestrator + findings engines runtime state
HUD_LAST_REFRESH.json
FINDINGS_AGGREGATE.json
/artifacts/
.claude/worktrees/
.railway/
.vercel
.env*.local
.agents/
# Kilo Code IDE local config (may contain MCP server secrets)
.kilo/
.gitnexus/
.gitnexus-*/
GITNEXUS_*.json
# Local Obsidian vault
Obsidian/
# Findings engines (Phase 1 mirror expansion)
artifacts/opencode-fleet/
.opencode-archive/
BLOCKER_RANK.json
# Wave A purge — generated coverage/runtime artifacts must never enter git
backend/coverage/
frontend/coverage/
frontend-admin/coverage/
worker/coverage/
coverage/
.tmp/
tmp/
.notif-*/
.eslint-seatbelt.tsv
e2e/playwright-cache/
# atomic-edit MCP compiled output (regenerated by launcher on staleness)
scripts/mcp/atomic-edit/dist/
# atomic-edit AtomicEditTrace audit logs (per-operation churn; aggregate via
# scripts/mcp/atomic-edit/audit-atomicity.mjs, do not version individual ops)
docs/ai/traces/
scripts/mcp/atomic-edit/.smoke-fixture.*
scripts/mcp/atomic-edit/.smoke-atomic-*-proof-*/
scripts/mcp/atomic-edit/atomic-*-gate-*/
.atomic/artifacts/
.atomic/prove/
.atomic/snapshots/
.codex-artifacts/
.codex-hook-tmp/
backend/artifacts/
# atomic-edit operation traces (per-call audit logs; aggregate via
.atomic/traces/
.atomic/macro-traces/
.atomic/bypass-ledger.jsonl
# claude code session locks
.claude/scheduled_tasks.lock
graphify-out/
tools/graphify-plus/
SESSION_STATE.md
tools/loop-runner/
tools/test-affected/
# runtime state moved from root
state/HUD_LAST_REFRESH.json
state/FINDINGS_AGGREGATE.json
# atomic-edit + kloel runtime state
.atomic-edit-locks/
.kloel/
# atomic-edit operation traces (per-call audit; aggregate via
.canon-fleet/

# codegraph build artifacts
.codegraph/
.world/
.task-graph/

# Node 22+ compile cache (ephemeral; can land under cwd of sandboxed exec)
node-compile-cache/
**/node-compile-cache/

# Jest cacheDirectory accidentally rooted under backend/scripts (haste-map + transform-cache)
backend/scripts/jest_dx/
# Editor/LSP caches + sandbox repro dirs that can land under cwd
typescript-language-server*/
xcrun_db
repro-tmpdir-write
# Throwaway codemods / transient atomic proof fixtures (PID-suffixed)
scripts/tmp-*
scripts/mcp/.red-*/
scripts/mcp/**/.proof-*/
scripts/mcp/**/.red-*/
# atomic-edit runtime: ledgers, heartbeats, broker sockets, probes, KG snapshots, HEAD
.atomic/*.json
.atomic/*.jsonl
.atomic/*.jpeg
.atomic/*.sock
.atomic/HEAD
.atomic/broker-proof-last.json
.atomic/*.stderr
.atomic/_*
.atomic/*.txt
# Deep-cleanup session scratch (process artifacts, not Kloel product)
.cleanup-scratch/
PLANO-DE-LIMPEZA.md
# Root-level jest cache (haste-map / transform-cache / perf-cache — regenerable)
jest_dx/
**/jest_dx/
# node v8 compile cache (uid-suffixed, regenerable)
v8-compile-cache-*/
# Agent scratch / research dumps (never source of truth)
.r_*.txt
.CONSOLIDATED*.txt
.FACTS.txt
.tabhooks.txt
claude_statusline_*
agent-audit-*/

# agent-cli tooling — untracked from GitHub, kept on disk locally (Kloel cleanup)
.kilo/
.opencode-prompts/
.hermes/
.omx/
.pr198-agents/
.world/

# agent-cli scripts/docs — untracked from GitHub, kept on disk (Kloel cleanup)
scripts/codemods/
scripts/decomp/
scripts/findings-engines/
scripts/canon-gates/
scripts/__parts__/
scripts/auth/
scripts/backup/
docs/atomic/
docs/ai/
.codex/

# more agent-cli untracked (Kloel cleanup batch 3)
.opencode/
opencode.json
intents/

.claude/
.agents/
.codex-traces/

# Separate atomic-os public-repo working clone — never commit into Kloel
.atomic-os-workdir/
.atomic-os-workdir
.tmp-kloel-graph-nav-trace.json
.tmp-kloel-graph-nav-clean-trace.json

# FASE-1 formal-proof toolchain (Z3 venv) and scratch — not committed
scripts/mcp/atomic-edit/.z3venv/
.z3-scratch/

# ECC capability-mining research clone — never commit into Kloel
.ecc-research/

.z3venv/
.elan/
.elan-init.sh

# Atomic exec/gate scratch — stdout/stderr captures and sandbox effect-roots (regenerable)
.atomic-*.err
.atomic-*.out
.atomic-scratch/
.atomic-test-tmp/

# Cascas de TMPDIR de builds na raiz — packs webpack ~468MB que o envelope não
# consegue deletar (rm manual pendente) + scratch transitório de sessões; ignorados
# para o worktree poder ficar limpo (frontend:build:clean exige porcelain vazio).
909073dea1f127d9cf0eb420813fc5b9/
5d406ffa3ef1e36be83c02c976c1772a/
dfpuXW1uFOqNEd6kFVZHE/
ecc-mcp-home-*/
ecc-mcp-health-*/
ecc-session-recordings/

# PID files de dev-boot do harness atomic — runtime puro, nunca versionar
.atomic-devboot/
