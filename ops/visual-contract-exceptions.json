[
  {
    "file": "frontend/src/app/s/[slug]/route.ts",
    "rule": "hex",
    "reason": "Server-rendered standalone HTML error page (a raw HTML string returned in a Response) for the public Kloel Sites slug route. It cannot import the React design tokens, so the brand colors (#0A0A0C / #E5E5E5 / #E85D30 / #B8B8B8) are baked inline into the static error document. Owner-approved.",
    "expires": "2026-12-31"
  }
]
