'use client';

// Canonical sources replaced the inline copies here.
// This shim keeps the module path stable for any future importer.
export { ProductCard, MetricCard } from './WhatsAppExperience.dashboard-cards';
export { ToneCard, FollowUpSwitch, MediaItem } from './WhatsAppExperience.wizard-cards';
