'use client';

// Canonical sources replaced the inline copies here.
// This shim keeps the module path stable for any future importer.
export {
  NonWahaProviderHint,
  ConnectedCelebration,
  ActivatedScreen,
} from './WhatsAppExperience.connection-panes';
export { InfoCard, FeedCard, ProductPerformanceCard } from './WhatsAppExperience.dashboard-cards';
