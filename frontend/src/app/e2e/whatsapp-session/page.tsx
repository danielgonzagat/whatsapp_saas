import { WhatsAppSessionHarness } from '../_components/whatsapp-session-harness';

/** Whats app session page. */
export default function WhatsAppSessionPage() {
  return <WhatsAppSessionHarness mode="session" />;
}
