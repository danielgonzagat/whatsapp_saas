/** Public loading. */
export default function PublicLoading() {
  return (
    <div
      aria-hidden="true"
      style={{
        minHeight: '100vh',
        background: '#0A0A0C',
      }}
    />
  );
}
