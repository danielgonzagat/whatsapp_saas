# Kloel Deprecation Map

> Tracks each symbol marked as deprecated, with its replacement and migration deadline.

| Deprecated symbol | Replacement | Deadline | Status |
|---|---|---|---|
| _(none yet — populate as canonicalization migrations land)_ | | | |
| #27 `BrainSpineAuditService` at `backend/src/brain/brain-spine-audit.service.ts` (ADR-0013 Wave M3 — cognitive spine audit moved out of legacy top-level `brain/` namespace into the canonical Mind/Observability cluster). | Canonical: `MindSpineAudit` at `backend/src/kloel/mind/observability/mind-spine-audit.service.ts` (implementation + spec moved; barrel `kloel/mind/observability/index.ts` already exported it). Legacy path `backend/src/brain/brain-spine-audit.service.ts` is now a 2-line `@deprecated` re-export shim that re-exports both `MindSpineAudit` and `MindSpineAudit as BrainSpineAuditService` for back-compat. `backend/src/admin/brain/brain-audit.controller.ts` already imported from canonical; the controller spec was updated to follow. Guard `scripts/ops/check-mind-canonical-imports.mjs` row 62 already lists `/brain/brain-spine-audit.service` → `/kloel/mind/observability`. | 2026-06-15 | COMPLETE 2026-05-27 — Wave 5 row #27 — implementation flipped from legacy→canonical. `backend/src/brain/` now contains only the re-export shim. `npx jest mind-spine-audit` passes (6 tests). Backend tsc clean. Gate check: 0 callers of the legacy path remain in non-alias source. |
| #28 `HiddenDataExtractorService` at `backend/src/ai-brain/hidden-data.service.ts` (ADR-0013 Wave M5 — first ai-brain → kloel/mind/knowledge physical move; rows #18–#22 of `MIND_SERVICES_CANONICAL.md`). | Canonical: `MindHiddenDataExtractor` at `backend/src/kloel/mind/knowledge/hidden-data.service.ts`, exported via the `kloel/mind/knowledge` barrel. Legacy `ai-brain/hidden-data.service.ts` is now an 18-line `@deprecated` re-export stub of the canonical class, keeping `HiddenDataExtractorService` name compatible during the alias window. `ai-brain.module.ts` already imports from the canonical path; `mind-hidden-data-extractor.service.ts` alias re-exports from local `./hidden-data.service`. Remaining ai-brain files (`agent-assist.service.ts`, `knowledge-base.service.ts`, `media-factory.service.ts`, `vector.service.ts`) stay legacy for future waves — only the lowest-blast-radius file (zero external callers, `@Injectable` w/ trivial DI on `ConfigService`) was moved in this batch. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 5 row #28 — `git mv` preserved rename history; internal imports rewritten for the new depth (`../../../logging/structured-logger`, `../../openai-wrapper`, `../../../lib/openai-models`); spec mock path adjusted to `../../openai-wrapper`. Backend `tsc -p tsconfig.build.json --noEmit` clean; `npx jest --runInBand src/ai-brain src/kloel/mind/knowledge` = 9 suites / 97 tests passed (incl. moved `hidden-data.service.spec.ts`). |
| #29 `kloel.message.created` (legacy event name from ADR-0013 §4 / Wave M6). | Canonical `mind.message.received`. Actual runtime emit string today is `message.received` (no `kloel.` prefix) — see Section B.1 of `EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md`. Recommended migration strategy: **Aliased canonical** — extend `BRAIN_EVENT_TAXONOMY` with `mind.message.received`, switch outbox emit at `omnichannel/channel-inbound-hook.service.ts:47,155` and `mind/perception/mind-perception.service.ts:75` to canonical, accept either name in SQL aggregators / decision-catalog / mind-runtime branches for 4 weeks, then drop legacy. **DO NOT** rename the webhook dispatch string at `inbox/inbox.service.ts:193` — that is a distinct external contract. | 2026-07-15 | INVESTIGATION COMPLETE — see EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md |
| #30 `kloel.action.executed` (legacy event name from ADR-0013 §4 / Wave M6). | Canonical `mind.action.executed`. Actual runtime emit strings today are `capability.executed` / `capability.failed` / `brain.capability.invoked` (no `kloel.` prefix) at `kloel/brain-runtime.service.ts:220,352` and `kloel/brain-capability-executor.service.ts:538` — see Section B.2. Recommended migration: **Aliased canonical** — add `mind.action.executed` to taxonomy, switch `brain-runtime.service.ts:220,352` to emit canonical name, update SQL filter in `mind/observability/mind-spine-audit.service.ts:66` to accept both. | 2026-07-15 | INVESTIGATION COMPLETE — see EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md |
| #31 `kloel.product.created` (legacy event name from ADR-0013 §4 / Wave M6). | Canonical `mind.product.observed`. Actual runtime emit string today is `product.created` (no `kloel.` prefix). Emitted via two paths: (1) `eventEmitter.emit('product.created', ...)` at `products/product.service.ts:107` — no @OnEvent listeners exist anywhere, effectively dead; (2) `brainSpine.recordCommercial({ eventType: 'product.created' })` at `products/product.service.ts:129` — outbox path consumed by mind-runtime. See Section B.3. Recommended migration: **Aliased canonical** — change ONLY the outbox eventType (Path B) to `mind.product.observed`; perception synthesizer at `kloel/mind-perception.service.ts:170` also updated. Path A emit string stays legacy until a future EventEmitter2 listener contract is decided. | 2026-07-15 | INVESTIGATION COMPLETE — see EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md |
| #32 `kloel.plan.created` (legacy event name from ADR-0013 §4 / Wave M6). | Canonical `mind.plan.observed`. Actual runtime emit string today is `plan.created` (no `kloel.` prefix) at `plans/plan.service.ts:120` (EventEmitter2, no listeners) and `plans/plan.service.ts:144` (outbox via `brainSpine.recordCommercial`). See Section B.4. Recommended migration: **Aliased canonical** — change outbox eventType (line 144) to `mind.plan.observed`, extend taxonomy, accept both names in aggregators for 4 weeks. | 2026-07-15 | INVESTIGATION COMPLETE — see EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md |
| #33 `MediaFactoryService` at `backend/src/ai-brain/media-factory.service.ts` (ADR-0013 Wave M5 — second ai-brain → kloel/mind/knowledge physical move; MIND_SERVICES_CANONICAL row #21 / Wave M2). NOTE: Wave 6 originally targeted `vector.service.ts` + `knowledge-base.service.ts` together (rows #20/#22), but `knowledge-base.service.ts` is 515 LOC (exceeds the per-task 200-LOC ceiling) so this batch pivoted to the smaller `media-factory.service.ts` candidate explicitly listed as the allowed fallback. The vector + knowledge-base pair stays legacy for a dedicated larger-scope wave. | Canonical: `MindMediaFactory` at `backend/src/kloel/mind/knowledge/media-factory.service.ts`, exported via the `kloel/mind/knowledge` barrel. Legacy `ai-brain/media-factory.service.ts` is now a 19-line `@deprecated` re-export stub of the canonical class, keeping `MediaFactoryService` name compatible during the alias window. `ai-brain.module.ts` import already flipped to the canonical path; `mind-media-factory.service.ts` alias now re-exports from the co-located `./media-factory.service`. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 6 row #33 — `git mv` preserved rename history for both the service and its spec; internal imports rewritten for the new depth (`../../../logging/structured-logger`, `../../openai-wrapper`, `../../../lib/openai-models`); spec mock path and named imports adjusted to match. Backend `tsc -p tsconfig.build.json --noEmit` clean; `npx jest --runInBand src/ai-brain src/kloel/mind/knowledge` = 9 suites / 97 tests passed (incl. moved `media-factory.service.spec.ts`); `node scripts/ops/check-mind-canonical-imports.mjs --report` = 0 legacy import sites. |
| #34 `KnowledgeBaseService` + `VectorService` co-move at `backend/src/ai-brain/{knowledge-base,vector}.service.ts` (ADR-0013 Wave M2 — third ai-brain → kloel/mind/knowledge physical move; MIND_SERVICES_CANONICAL rows #20 / #22). Co-moved together because `KnowledgeBaseService` depends on `VectorService` via constructor DI; co-located helper `knowledge-base.chunking.helpers.ts` was hauled along since it is consumed only by `KnowledgeBaseService` and was a sibling-only relative import. Wave 7 LOC budget (601 service LOC + 80 helper LOC) accommodates the pair after Waves 5 / 6 cleared #28 + #33. | Canonical: `MindKnowledgeBase` at `backend/src/kloel/mind/knowledge/knowledge-base.service.ts` and `MindVectorStore` at `backend/src/kloel/mind/knowledge/vector.service.ts`, both exported via the `kloel/mind/knowledge` barrel. Legacy `ai-brain/knowledge-base.service.ts` and `ai-brain/vector.service.ts` are now thin `@deprecated` re-export stubs of the canonical classes (also re-exporting `htmlToText` from the KB stub for the small handful of importers that already pull it directly). `ai-brain.module.ts` and `knowledge-base.controller.ts` were both flipped to the canonical paths. `mind-vector-store.service.ts` and `mind-knowledge-base.service.ts` alias re-exports now point at co-located `./vector.service` / `./knowledge-base.service` instead of the cross-tree `../../../ai-brain/...` shim. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 7 row #34 — `git mv` preserved rename history for all four files (`vector.service.ts`, `vector.service.spec.ts`, `knowledge-base.service.ts`, `knowledge-base.service.spec.ts`) plus the co-moved helper `knowledge-base.chunking.helpers.ts`; internal imports rewritten for the new depth (`../../../logging/structured-logger`, `../../../audit/audit.service`, `../../../common/trace-headers`, `../../../common/utils/url-validator`, `../../../prisma/prisma.service`, `../../../queue/queue`, `../../../wallet/...`, `../../../observability/ops-alert.service`, `../../../common/regex`); KB ↔ Vector cross-import stays `./vector.service` (still siblings); KB spec adjusted to `../../../queue/queue` for both import and `jest.mock` factory plus `../../../billing/...` / `../../../audit/...` / `../../../wallet/...` upstream paths. Backend `tsc -p tsconfig.build.json --noEmit` clean; `npx jest --runInBand src/ai-brain src/kloel/mind/knowledge` = 9 suites / 97 tests passed (incl. moved `vector.service.spec.ts` + `knowledge-base.service.spec.ts`); `node scripts/ops/check-mind-canonical-imports.mjs --report` = 0 legacy import sites. |
| #35 `AgentAssistService` at `backend/src/ai-brain/agent-assist.service.ts` (ADR-0013 Wave M2 row #22 — fourth and final ai-brain → kloel/mind/knowledge physical move). With Waves 5 / 6 / 7 already clearing `hidden-data.service.ts`, `media-factory.service.ts`, `knowledge-base.service.ts`, and `vector.service.ts`, this Wave 8 move finishes the `ai-brain/` → `kloel/mind/knowledge/` migration for the remaining 387-LOC `@Injectable()` (DI: `ConfigService`, `PrismaService`, `PlanLimitsService`, `WalletService`, optional `OpsAlertService`). The co-located helpers (`agent-assist.helpers.ts` + its 4 spec files) stay in `ai-brain/` for this wave — they remain a sibling-only relative import surface and their move is a separate sub-task. | Canonical: `MindKnowledgeAssist` at `backend/src/kloel/mind/knowledge/agent-assist.service.ts`, exported via the `kloel/mind/knowledge` barrel through `mind-knowledge-assist.service.ts`. Legacy `ai-brain/agent-assist.service.ts` is now a 12-line `@deprecated` re-export stub of the canonical class, keeping `AgentAssistService` name compatible during the alias window. `ai-brain.module.ts` and `ai-brain/knowledge-base.controller.ts` were both flipped to the canonical path. `mind-knowledge-assist.service.ts` alias now re-exports from co-located `./agent-assist.service` instead of the cross-tree `../../../ai-brain/agent-assist.service` shim. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 8 row #35 — `git mv` preserved rename history for both `agent-assist.service.ts` and `agent-assist.service.spec.ts`; internal imports rewritten for the new depth (`../../../logging/structured-logger`, `../../openai-wrapper`, `../../../lib/openai-models`, `../../../billing/plan-limits.service`, `../../../observability/ops-alert.service`, `../../../prisma/prisma.service`, `../../../wallet/wallet.service`, `../../../ai-brain/agent-assist.helpers`); spec mock path adjusted to `../../openai-wrapper` and PrismaService import to `../../../prisma/prisma.service`. Backend `tsc -p tsconfig.build.json --noEmit` clean; `npx jest --runInBand src/ai-brain src/kloel/mind/knowledge` = 9 suites / 97 tests passed (incl. moved `agent-assist.service.spec.ts` co-located with the service). |
| #40 `cia-runtime.port.ts` at `backend/src/cia/cia-runtime.port.ts` (ADR-0013 Wave M4 — first physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, per ADR-0006 framing of CIA as a learning adapter and the empty `kloel/mind/cia/index.ts` barrel already prepared upstream). Chosen as the lowest-blast-radius starting file in a 28-file folder: 23 LOC, pure types only (`Symbol` + 3 type aliases + DI port interface), zero `@Injectable`, zero runtime dependencies — the cleanest possible seed for the incremental cia/ → kloel/mind/cia/ migration. NOTE: the canonical file `backend/src/kloel/mind/cia/cia-runtime.port.ts` was already present at HEAD as byte-identical content from a prior preparation step; this Wave 9 batch finishes the migration by flipping callers and dropping a `@deprecated` stub at the legacy path so the alias is observable and the legacy file no longer carries live source. | Canonical: `backend/src/kloel/mind/cia/cia-runtime.port.ts` (re-exported via the `kloel/mind/cia` barrel alongside the existing CIA re-exports). Legacy `backend/src/cia/cia-runtime.port.ts` is now an 18-line `@deprecated` re-export shim. Five callers were flipped to the canonical path immediately: `cia/cia-runtime.abstract.ts`, `cia/cia.module.ts`, `webhooks/whatsapp-api-webhook.controller.ts`, `whatsapp/controllers/whatsapp-api.controller.ts`, `whatsapp/whatsapp-watchdog-recovery.service.ts`. Remaining 16 cia/ files (services + module + controller + spec/fixture set) stay legacy for future waves — this batch is intentionally tiny so the canonical home is established with zero risk before larger `@Injectable` services land. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 9 row #40 — legacy file flipped to `@deprecated` re-export of canonical; canonical file already present (byte-identical) at HEAD so no `git mv` was needed beyond the local move (rename detection picks it up via identical content); 5 call sites updated to canonical path. `kloel/mind/cia/index.ts` barrel extended with the new exports (`CIA_RUNTIME_SERVICE`, `CiaBacklogMode`, `CiaBacklogOptions`, `CiaRuntimePort`). Backend `tsc -p tsconfig.build.json --noEmit` clean; `npx jest --runInBand src/cia src/kloel/mind/cia src/webhooks/whatsapp-api-webhook src/whatsapp/controllers/whatsapp-api src/whatsapp/whatsapp-watchdog-recovery` = 15 suites / 158 tests passed. Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new port file falls inside the existing `kloel/mind/cia/` alias host regex. |
| #41 `cia-runtime.abstract.ts` at `backend/src/cia/cia-runtime.abstract.ts` (ADR-0013 Wave M4 — second physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, following Wave 9's port move at row #40). Chosen as the next-smallest unmoved file: 25 LOC, pure abstract class implementing `CiaRuntimePort`, zero `@Injectable`, zero runtime dependencies (only a type-only import of the already-canonical port). Sole external caller was `cia/cia.module.ts` (DI alias used as `WhatsappCiaRuntimeService` for cross-cluster binding to the concrete `CiaRuntimeService`). | Canonical: `backend/src/kloel/mind/cia/cia-runtime.abstract.ts` (re-exported via the `kloel/mind/cia` barrel as `WhatsappCiaRuntimeService` to make the cross-cluster DI alias observable from the canonical surface). Legacy `backend/src/cia/cia-runtime.abstract.ts` is now a 13-line `@deprecated` re-export shim. The single caller (`cia/cia.module.ts`) was flipped to the canonical path immediately. The 15 remaining cia/ files (services + module + controller + spec/fixture set) stay legacy for future waves — this batch is intentionally tiny so the canonical home keeps growing with zero risk before larger `@Injectable` services land. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 10 row #41 — `git mv` preserved rename history; internal type-only import rewritten for the new depth (`./cia-runtime.port` since the port is now a sibling in `kloel/mind/cia/`); legacy file replaced with thin `@deprecated` re-export shim. `kloel/mind/cia/index.ts` barrel extended with `WhatsappCiaRuntimeService` alias export. Backend `tsc -p tsconfig.build.json --noEmit` clean (no cia-related diagnostics; the pre-existing unrelated `waha-types` error is the only TSC output and is outside this wave's scope). `npx jest --runInBand src/cia src/kloel/mind/cia` = 12 suites / 133 tests passed. Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new abstract falls inside the existing `kloel/mind/cia/` alias host regex. |
| #42 `cia-cognitive-health.service.ts` at `backend/src/cia/cia-cognitive-health.service.ts` (ADR-0013 Wave M4 — third physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, following Waves 9 + 10 which moved the port + abstract). Chosen as the next-smallest unmoved file: 93 LOC, single `@Injectable` `CiaCognitiveHealthService` with trivial DI on `PrismaService`, `GoalFieldService`, `SpineEmitterService`, no controller wiring — only consumed by `kloel/mind/mind-bg.scheduler.ts` (optional DI) and `cia/cia.module.ts` (provider+export). UTP-CIA-COG-ESC-001 cognitive-health alert escalator into `kloelMemory`. | Canonical: `backend/src/kloel/mind/cia/cia-cognitive-health.service.ts` (re-exported via the `kloel/mind/cia` barrel sibling-style — barrel switched from cross-tree `../../../cia/cia-cognitive-health.service` to local `./cia-cognitive-health.service`). Legacy `backend/src/cia/cia-cognitive-health.service.ts` is now a 15-line `@deprecated` re-export shim. Both callers were flipped to the canonical path immediately: `cia/cia.module.ts` (now imports from `../kloel/mind/cia/cia-cognitive-health.service`) and `kloel/mind/mind-bg.scheduler.ts` (now imports from `./cia/cia-cognitive-health.service`); the `mind-bg.scheduler.spec.ts` type-only `import(...)` was also flipped to the local path. The remaining 14 cia/ files (services + module + controller + spec/fixture set) stay legacy for future waves. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 11 row #42 — `git mv` preserved rename history for both `cia-cognitive-health.service.ts` and `cia-cognitive-health.service.spec.ts`; internal imports rewritten for the new depth (`../../../prisma/prisma.service`, `../../goal-field/goal-field.service`, `../../spine/spine-emitter.service`, `../../goal-field/goal-field.types`); spec imports rewritten (`../../goal-field/goal-field.service`, `../mind.types`, `../../goal-field/goal-field.types`); legacy file replaced with thin `@deprecated` re-export shim. Two external callers flipped to canonical path + barrel switched to local sibling re-export. Backend `tsc -p tsconfig.build.json --noEmit` clean (EXIT=0, zero diagnostics). `npx jest --runInBand src/cia src/kloel/mind/cia src/kloel/mind/mind-bg` = 13 suites / 143 tests passed (incl. moved `cia-cognitive-health.service.spec.ts` and the `mind-bg.scheduler.spec.ts` covering the optional DI wiring). Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new service falls inside the existing `kloel/mind/cia/` alias host regex. |
| #43 `cia-autonomy-advisor.service.ts` at `backend/src/cia/cia-autonomy-advisor.service.ts` (ADR-0013 Wave M4 — fourth physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, following Waves 9 + 10 + 11 which moved the port + abstract + cognitive-health service). Chosen as the next-smallest unmoved non-module/non-controller file: 144 LOC, single `@Injectable` `CiaAutonomyAdvisorService` with trivial DI on `DecisionOutcomeService` (sibling under `kloel/`) and `CiaRuntimeStateService` (still-legacy `cia/`), zero controller wiring — advisor-only z-statistic analyzer that reads `decisionOutcome.findAllClosedSinceForWorkspace` per workspace and recommends INCREASE/DECREASE per decision type, optionally calling `runtimeState.updateWorkspaceAutonomy` when `CIA_AUTONOMY_AUTO_APPLY_ENABLED=true`. Only consumed via the `kloel/mind/cia/index.ts` barrel re-export and `cia/cia.module.ts` (provider+export). | Canonical: `backend/src/kloel/mind/cia/cia-autonomy-advisor.service.ts` (re-exported via the `kloel/mind/cia` barrel sibling-style — barrel switched from cross-tree `../../../cia/cia-autonomy-advisor.service` to local `./cia-autonomy-advisor.service`). Legacy `backend/src/cia/cia-autonomy-advisor.service.ts` is now a 17-line `@deprecated` re-export shim (re-exports both `CiaAutonomyAdvisorService` and the `AutonomyAdvice` type alias to keep the canonical export surface byte-equivalent during the alias window). The single external caller `cia/cia.module.ts` was flipped to the canonical path immediately (`'../kloel/mind/cia/cia-autonomy-advisor.service'`). The remaining 12 cia/ files (services + module + controller + spec/fixture set) stay legacy for future waves. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 12 row #43 — `git mv` preserved rename history for both `cia-autonomy-advisor.service.ts` and `cia-autonomy-advisor.service.spec.ts`; internal imports rewritten for the new depth (`../../../cia/cia-runtime-state.service` — still-legacy sibling — and `../../decision-outcome.service` under `kloel/`); spec imports rewritten identically; legacy file replaced with thin `@deprecated` re-export shim. One external caller (`cia/cia.module.ts`) flipped to canonical path + barrel switched to local sibling re-export. Backend `tsc -p tsconfig.build.json --noEmit` clean (EXIT=0, zero diagnostics). `npx jest --runInBand src/cia src/kloel/mind/cia` = 12 suites / 133 tests passed (incl. moved `cia-autonomy-advisor.service.spec.ts`). Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new service falls inside the existing `kloel/mind/cia/` alias host regex. |
| #44 `cia-send-helpers.service.ts` at `backend/src/cia/cia-send-helpers.service.ts` (ADR-0013 Wave M4 — fifth physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, following Waves 9 + 10 + 11 + 12 which moved the port + abstract + cognitive-health + autonomy-advisor). Chosen as the next-lowest-risk file in the cluster — 336 LOC, single `@Injectable` `CiaSendHelpersService` plus the exported constant `CIA_SHARED_REPLY_LOCK_MS` and `WHITESPACE_RE`. Leaf service with ZERO internal `./cia-*` imports (all deps point outside cia/: `Redis`, `ChannelTransportRegistry`, `AgentEventsService`, optional `OpsAlertService`/`SpineEmitterService`/`MindPolicyService`, plus `NON_DIGIT_RE`/`WHITESPACE_G_RE` regex helpers). 7 callers total — 4 specs + 2 peer cia/ services (`cia-inline-fallback.service`, `cia-remote-backlog.service`) + 1 cross-cluster import in `whatsapp/cia-remote-backlog.helpers.ts`; the legacy stub keeps all callers building with no caller-site flips required. | Canonical: `backend/src/kloel/mind/cia/cia-send-helpers.service.ts` (re-exported via the `kloel/mind/cia` barrel sibling-style — barrel switched from cross-tree `../../../cia/cia-send-helpers.service` to local `./cia-send-helpers.service`). Legacy `backend/src/cia/cia-send-helpers.service.ts` is now a 17-line `@deprecated` re-export shim (re-exports `CiaSendHelpersService`, `CIA_SHARED_REPLY_LOCK_MS`, and `WHITESPACE_RE` so the canonical export surface stays byte-equivalent during the alias window — `CIA_SHARED_REPLY_LOCK_MS` is consumed at runtime by `cia-inline-fallback.service.ts` and `cia-remote-backlog.service.ts`). No external caller required flipping — every consumer already imports via `'./cia-send-helpers.service'` (peers, specs, module) or `'../cia/cia-send-helpers.service'` (whatsapp/) and resolves through the shim transparently. The remaining 11 cia/ files (4 services + 4 specs + fixtures + module + controller) stay legacy for future waves. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 13 row #44 — `git mv` preserved rename history for both `cia-send-helpers.service.ts` and `cia-send-helpers.service.spec.ts`; internal imports rewritten for the new depth (`../../channel-transport.registry`, `../../../observability/ops-alert.service`, `../../../whatsapp/agent-events.service`, `../../../common/phone`, `../../../common/regex`, `../../spine/spine-emitter.service`, `../../mind-policy.service`); spec imports rewritten identically; legacy file replaced with thin `@deprecated` re-export shim covering all three named exports. Barrel switched to local sibling re-export. Backend `tsc -p tsconfig.build.json --noEmit` clean (EXIT=0, zero diagnostics). `npx jest --testPathPatterns="cia/" --no-coverage` = 12 suites / 133 tests passed (incl. moved `cia-send-helpers.service.spec.ts` with 32 tests); `npx jest --testPathPatterns="whatsapp/cia-" --no-coverage` = 6 suites / 94 tests passed (cross-cluster cia helpers under whatsapp/ still resolve via the legacy shim). Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new service falls inside the existing `kloel/mind/cia/` alias host regex. |
| #28b `cia-runtime-state.service.ts` at `backend/src/cia/cia-runtime-state.service.ts` (ADR-0013 Wave M4 — sixth physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, following Waves 9 + 10 + 11 + 12 + 13 which moved the port + abstract + cognitive-health + autonomy-advisor + send-helpers). Wave 14 row #28b. Chosen as the next-priority file in the cluster — 313 LOC, single `@Injectable` `CiaRuntimeStateService` with DI on `PrismaService`, `AgentEventsService`, and optional `OpsAlertService`. Persists workspace autonomy snapshots, autonomy run records, and contact-catalog refresh scheduling. 16 callers total — 1 sibling under `kloel/mind/cia/` (`cia-autonomy-advisor.service`), 9 peers/specs in `cia/` (runtime/inline-fallback/remote-backlog/bootstrap/backlog-run + module + fixtures + specs), 2 cross-cluster importers in `whatsapp/` (`cia-runtime.fixtures.ts`, `cia-backlog-run.helpers.ts`), and the kloel/mind/cia barrel. The legacy stub keeps all callers building with no caller-site flips required outside `kloel/mind/cia/` siblings. | Canonical: `backend/src/kloel/mind/cia/cia-runtime-state.service.ts` (re-exported via the `kloel/mind/cia` barrel sibling-style — barrel switched from cross-tree `../../../cia/cia-runtime-state.service` to local `./cia-runtime-state.service`). Legacy `backend/src/cia/cia-runtime-state.service.ts` is now a 13-line `@deprecated` re-export shim. Sibling `kloel/mind/cia/cia-autonomy-advisor.service.ts` (+ its spec) flipped from cross-tree `../../../cia/cia-runtime-state.service` to local `./cia-runtime-state.service`. The remaining 10 cia/ files (3 services + 4 specs + fixtures + module + controller) stay legacy for future waves. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 14 row #28b — `git mv` preserved rename history for both `cia-runtime-state.service.ts` and `cia-runtime-state.service.spec.ts`; internal imports rewritten for the new depth (`../../../common/prisma/prisma-json.util`, `../../../observability/ops-alert.service`, `../../../prisma/prisma.service`, `../../../queue/job-id.util`, `../../../queue/queue`, `../../../whatsapp/agent-events.service`, `../../../whatsapp/provider-settings.types`, `../../../common/string`); spec imports rewritten (`../../../prisma/prisma.service`, `../../../whatsapp/agent-events.service`, `../../../observability/ops-alert.service`, `../../../../test/helpers/prisma.mock`, `../../../queue/queue`); legacy file replaced with thin `@deprecated` re-export shim. Barrel switched to local sibling re-export. Sibling `cia-autonomy-advisor.service.ts` + spec also flipped to local sibling path. Backend `tsc -p tsconfig.build.json --noEmit` clean (EXIT=0, zero diagnostics). `npx jest --runInBand src/cia src/kloel/mind/cia` = 12 suites / 133 tests passed (incl. moved `cia-runtime-state.service.spec.ts`). Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new service falls inside the existing `kloel/mind/cia/` alias host regex. |
| #45 `cia-chat-filter.service.ts` at `backend/src/cia/cia-chat-filter.service.ts` (ADR-0013 Wave M4 — seventh physical move of the `backend/src/cia/` learning adapter cluster into `kloel/mind/cia/`, following Waves 9 + 10 + 11 + 12 + 13 + 14 which moved the port + abstract + cognitive-health + autonomy-advisor + send-helpers + runtime-state). Wave 15 row #45. 255 LOC, single `@Injectable` `CiaChatFilterService` — pure stateless helper for filtering/normalizing remote WhatsApp chat summaries (`WahaChatSummary`), zero DB / Redis / DI on internal services (depends only on `Logger` and the `WahaChatSummary` type from `whatsapp/providers/whatsapp-api.provider`). 18 callers total — 11 peers/specs in `cia/` (`cia-bootstrap.service`, `cia-backlog-run.service`, `cia-inline-fallback.service`, `cia-remote-backlog.service`, `cia-runtime.service.fixtures`, `cia.module` + 5 specs), 6 cross-cluster importers in `whatsapp/` (`cia-bootstrap.service.spec`, `cia-inline-fallback.service.spec`, `cia-inline-fallback.service.processing.spec`, `cia-remote-backlog.service.spec`, `cia-backlog-run.service.spec`, `cia-backlog-run.helpers`, `cia-remote-backlog.helpers`, `cia-chat-filter.service.spec`), and the kloel/mind/cia barrel. The legacy stub keeps all callers building with no caller-site flips required. | Canonical: `backend/src/kloel/mind/cia/cia-chat-filter.service.ts` (re-exported via the `kloel/mind/cia` barrel sibling-style — barrel switched from cross-tree `../../../cia/cia-chat-filter.service` to local `./cia-chat-filter.service`). Legacy `backend/src/cia/cia-chat-filter.service.ts` is now a 13-line `@deprecated` re-export shim. `cia/cia.module.ts` was flipped to the canonical path immediately (`'../kloel/mind/cia/cia-chat-filter.service'`). The remaining 9 cia/ files (3 services + 4 specs + fixtures + module + controller) stay legacy for future waves. | 2026-06-24 | MIGRATED 2026-05-27 — Wave 15 row #45 — `git mv` preserved rename history for both `cia-chat-filter.service.ts` and `cia-chat-filter.service.spec.ts`; internal import rewritten for the new depth (`../../../whatsapp/providers/whatsapp-api.provider` for the `WahaChatSummary` type); spec required no rewrites (only imports `./cia-chat-filter.service`); legacy file replaced with thin `@deprecated` re-export shim. Barrel switched to local sibling re-export. One external caller (`cia/cia.module.ts`) flipped to canonical path. Backend `tsc -p tsconfig.build.json --noEmit` clean (EXIT=0, zero diagnostics). `npx jest --runInBand --testPathPatterns="cia-chat-filter\|src/cia\|src/kloel/mind/cia"` = 13 suites / 167 tests passed (incl. moved `cia-chat-filter.service.spec.ts`); `npx jest --testPathPatterns="whatsapp/cia-" --no-coverage` = 6 suites / 94 tests passed (cross-cluster cia helpers under `whatsapp/` still resolve via the legacy shim transparently). Guard `scripts/ops/check-mind-canonical-imports.mjs` row 64 already covers cia/ paths; the new service falls inside the existing `kloel/mind/cia/` alias host regex. |
| #39 `normalizePhone` / `normalizePhoneDigits` / `formatPhone` — 6 legacy in-tree copies (canonicalization mission row #39 — ADR-0012 OmniCore consolidation): `backend/src/meta/meta-whatsapp.service.ts`, `backend/src/whatsapp/inbound-processor.helpers.ts`, `backend/src/marketing/channels/whatsapp/whatsapp-digits.util.ts`, `backend/src/whatsapp/whatsapp-normalization.util.ts`, `backend/src/whatsapp/whatsapp-catchup-history.service.ts`, `worker/providers/checkout-social-lead-enrichment.ts`. Each pre-existing copy stripped non-digits but diverged on: 10-digit floor vs none, leading `00` handling, `@c.us` stripping, return shape (`''` vs `null`). | Canonical: `backend/src/common/phone/phone-normalization.util.ts` exporting `NormalizedPhone` / `extractAsciiDigits` / `normalizePhone` / `extractPhoneFromChatId` / `phonesMatch` (~120 LOC, zero deps, 41-case Jest spec at `phone-normalization.util.spec.ts`). Worker boundary handled by byte-identical mirror at `worker/utils/phone-normalization.util.ts` + Vitest mirror spec at `worker/test/utils/phone-normalization.util.spec.ts`; drift between the two enforced by `scripts/ops/check-cross-boundary-utils-drift.mjs` (Wave 3D gate). All non-canonical helpers route through canonical via thin wrappers — no independent digit-strip implementations remain. | 2026-06-15 | COMPLETE 2026-05-27 — Wave 4A: 10 of 10 callers migrated. Final wave eliminated the last 5 legacy helpers — `whatsapp-catchup-history.service.ts::normalizePhone` (private method deleted, 6 call sites inline `whatsappDigits` from `common/phone`), `meta-whatsapp.service.ts::normalizePhone` (private method deleted, 3 call sites direct to `normalizeWhatsAppPhone` which now wraps canonical `extractAsciiDigits`), `inbound-processor.helpers.ts` (re-export `whatsappDigits as normalizePhone` removed, 2 importers now import `whatsappDigits as normalizePhone` from `common/phone` directly), `whatsapp-digits.util.ts::extractAsciiDigits` (rewritten as thin `unknown`-tolerant adapter delegating to canonical), `whatsapp-normalization.util.ts::extractPhoneFromChatId` (kept WhatsApp-namespaced because the canonical helper drops groups/LIDs/broadcasts whereas this surface keeps digits for ALL chat ids — documented divergence). `normalizeNumber` in `marketing/channels/whatsapp/whatsapp-service.helpers.ts` also rewritten as thin canonical wrapper. Backend tsc clean; 405 affected-suite tests pass (28 suites incl. autopilot/whatsapp-catchup/meta-whatsapp/checkout-social/unified-agent); worker 41/41 vitest pass; cross-boundary drift gate green (extractAsciiDigits/normalizePhone/extractPhoneFromChatId/phonesMatch all IDENTICAL). |

## Wave M5 physical-move progress (top-level kloel/mind-*.service.ts)

| Batch | Services moved | Sub-areas affected | Status |
|---|---|---|---|
| 1 | MindBanditService → policy/, MindBeliefService → inference/, MindGlobalPriorService → memory/ | policy, inference, memory | landed |
| 2 | MindCaseMemoryService → memory/, MindConceptService → memory/, MindPolicyService → policy/ | policy, memory | landed |
| 3 | MindPredictorService → inference/, MindSurpriseService → inference/, MindVerbalizerService → synthetic/ | inference, synthetic | landed |
| 4 | MindLiftReportService → observability/, MindObservabilityService → observability/, MindReportService → observability/ | observability | landed |
| 5 | mind-event-processor/mind-processor/mind-replay → runtime/ | runtime | landed |
| 6 | mind-guards/mind-guard-context-builder/mind-quality → policy/ | policy | landed |
| 7 | mind-simulator/mind-synthetic-generator/mind-workspace-state → synthetic, memory | synthetic, memory | landed |
| 8 | mind-perception → perception/ | perception | landed |

After Wave M5: top-level `kloel/mind-*.service.ts` files become @deprecated
re-exports for the 4-week alias window; physical code lives at
`kloel/mind/<sub-area>/`. Gate G4 (`check-mind-canonical-imports.mjs`)
will promote to `--strict` after the window closes, blocking pre-push
on any legacy import.
