# Kloel Deprecation Map

> Tracks each symbol marked as deprecated, with its replacement and migration deadline.

| Deprecated symbol | Replacement | Deadline | Status |
|---|---|---|---|
| Nine utility symbols duplicated across `backend/src/common/*` and `worker/*` (P1 dup #38): `forEachSequential`, `findFirstSequential`, `pollUntil`, `resolveRedisUrl`, `safeResolve`, `renderTemplate`, `toPrismaJsonValue`, `maskRedisUrl`, `RedisConfigurationError`. | **No consolidation in this PR.** Option B adopted as interim per `docs/architecture/CROSS_BOUNDARY_UTILS_DECISION.md`: parallel implementations preserved (separate deploy units — worker does NOT import backend at runtime), new gate `scripts/ops/check-cross-boundary-utils-drift.mjs` detects symbol-level drift, JSDoc cross-references added to the 3 behavior-equivalent `async-sequence.ts` pairs. Two pre-existing dedicated gates (`check-redis-resolver-sync.mjs`, `check-constants-sync.mjs`) already enforce file-level identity for the redis-resolver and sales-templates pairs. Two pairs are documented as already-divergent: `safeResolve` (cosmetic: error strings + backend's sibling `safeJoin`) and `toPrismaJsonValue` (worker has explicit null-short-circuit; backend factors into `prisma-json-scalar.util`). Option A (npm workspace `packages/shared/`) deferred to dedicated ADR. | 2026-07-31 | INTERIM 2026-05-27 — drift gate wired into `npm run canonical:check`, exit 0 soft / exit 1 `--strict`. Current state: 5 IDENTICAL, 2 WITHIN_TOLERANCE, 2 documented divergent. Full consolidation contingent on adopting npm workspaces (Option A — needs separate ADR + Railway build pipeline change). |
| `normalizePhone` / `normalizePhoneDigits` / `formatPhone` — 6 legacy in-tree copies (canonicalization mission row #39 — ADR-0012 OmniCore consolidation): `backend/src/meta/meta-whatsapp.service.ts`, `backend/src/whatsapp/inbound-processor.helpers.ts`, `backend/src/marketing/channels/whatsapp/whatsapp-digits.util.ts`, `backend/src/whatsapp/whatsapp-normalization.util.ts`, `backend/src/whatsapp/whatsapp-catchup-history.service.ts`, `worker/providers/checkout-social-lead-enrichment.ts`. Each pre-existing copy stripped non-digits but diverged on: 10-digit floor vs none, leading `00` handling, `@c.us` stripping, return shape (`''` vs `null`). | Canonical: `backend/src/common/phone/phone-normalization.util.ts` exporting `NormalizedPhone` / `extractAsciiDigits` / `normalizePhone` / `extractPhoneFromChatId` / `phonesMatch` (~120 LOC, zero deps, 41-case Jest spec at `phone-normalization.util.spec.ts`). Worker boundary handled by byte-identical mirror at `worker/utils/phone-normalization.util.ts` + Vitest mirror spec at `worker/test/utils/phone-normalization.util.spec.ts`; drift between the two enforced by `scripts/ops/check-cross-boundary-utils-drift.mjs` (Wave 3D gate). Worker callers delegate via thin adapter: `canonicalNormalizePhone(value)?.digits ?? null` preserves the existing digits-or-null DB column shape (`checkoutSocialLead.phone`, `contact.phone` unique key) while routing every input through the canonical validator. | 2026-06-15 | IN PROGRESS 2026-05-27 — 6 of 10 callers migrated (was 5/10 before Wave 3F). Latest: `worker/providers/checkout-social-lead-enrichment.ts` migrated to the worker mirror — local deprecated `normalizePhone(string \| null)` replaced with adapter over `canonicalNormalizePhone`; `@deprecated TODO(omnicore)` JSDoc removed. Worker tsc clean; 45/45 vitest pass (41 mirror cases + 4 existing enrichment cases) including the `phone: '5511999999999'` round-trip assertion. DEFERRED to follow-up: `backend/src/meta/meta-whatsapp.service.ts`, `backend/src/whatsapp/inbound-processor.helpers.ts`, `backend/src/marketing/channels/whatsapp/whatsapp-digits.util.ts`, `backend/src/whatsapp/whatsapp-catchup-history.service.ts` (each still carries `@deprecated TODO(omnicore)` JSDoc — 4 callers remaining). |
| `KloelLeadBrainService` at `backend/src/kloel/kloel-lead-brain.service.ts` (ADR-0013 Wave M1 — Mind unification, DEPRECATION_MAP row #20) | `LeadMindCoordinator` alias at `backend/src/kloel/mind/coordination/lead-mind-coordinator.service.ts` (pure ES re-export of same class reference — NestJS DI tokens stay identical via class-reference equality, migration is cosmetic). Physical move of implementation follows in separate PR after 4-week alias window. | 2026-06-24 | IN PROGRESS 2026-05-27 — 2 of 2 safe consumers migrated to `LeadMindCoordinator` via the `./mind/coordination` barrel: `backend/src/kloel/kloel.module.ts` (consolidated into existing barrel import) and `backend/src/kloel/kloel.service.ts`. Backend TSC clean (the only error remains the pre-existing unrelated `kloel-thinker.service.ts:32 TS6133`); 22/22 kloel-lead-brain.service.spec.ts + 24/24 kloel.service.spec.ts pass. Zero remaining `KloelLeadBrainService` symbol references outside the legacy impl + spec + alias re-export. |
