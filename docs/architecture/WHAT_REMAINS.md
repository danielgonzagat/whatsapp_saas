# WHAT_REMAINS — Canonicalization Wave Vision (Wave 25 snapshot)

> Generated by Wave 25 / subagent C on 2026-05-27.
> Purpose: refresh macro vision after a heavy day of file moves so subsequent
> waves know what is real, what is rubble, and which next moves carry the most
> leverage. **Investigation + planning only — no files were moved producing this
> document.**

---

## Macro Snapshot (2026-05-27 late session)

| Metric | Value | Source |
|---|---|---|
| Commits in last 24h | 182 | `git log --since="24 hours ago"` |
| Commits in last 36h | 402 | `git log --since="36 hours ago"` |
| OpenAPI routes | 591 across 63 tags | `tools/openapi/openapi-spec.json` |
| AsyncAPI channels | 122 | `tools/asyncapi/asyncapi-spec.json` |
| SARIF findings (backend) | 8 592 | `tools/sarif/manifest.json` |
| SARIF findings (frontend) | 5 | `tools/sarif/manifest.json` |
| SARIF findings (frontend-admin) | 16 | `tools/sarif/manifest.json` |
| SARIF findings (worker) | 0 | `tools/sarif/manifest.json` |
| Files in legacy `backend/src/whatsapp/` | 81 | `find` |
| Non-`@deprecated` files in `backend/src/whatsapp/` | 28 | header scan |
| Substantive (non-test/util/helper) leftovers | **3** | `whatsapp.module.ts`, `whatsapp-catchup.service.ts`, `whatsapp-catchup-orchestrator.service.ts` |
| Files in canonical `backend/src/marketing/channels/whatsapp/` | **124** | `find` |
| Legacy `whatsapp/whatsapp*` import sites (backend) | 16 (12 unique modules) | grep |
| Legacy `whatsapp/whatsapp*` import sites (frontend + worker) | 0 | grep |
| Files using legacy Brain\* names outside `brain-*.service.ts` impls | 7 references / 4 unique files | grep |

---

## What's Done

### WhatsApp dissolution (ADR-0012 OmniCore)
- **96/138** files moved to `backend/src/marketing/channels/whatsapp/` (124 files now home there; subtract earlier-existing channel-specific files).
- 53 files in the legacy `backend/src/whatsapp/` are now `@deprecated` re-export stubs.
- Today's run alone landed **20+ refactor(omnicore)** commits in the trailing 24h: account-agent suite, agent-events, inbound-processor, whatsapp.service, whatsapp-catchup-history, whatsapp-session, waha/wahaTransport providers, whatsapp-api.provider suite, waha-session-config providers, controllers (api/catalog/meta-compat).

### cia migration (ADR-0013 #28)
- **COMPLETE**: every file in `backend/src/cia/` is an `@deprecated` re-export stub pointing into `backend/src/kloel/mind/cia/`.
- Commit `0be9056f5` (today) finalized with the module move.
- Real owners: `backend/src/kloel/mind/cia/{cia,cia-runtime,cia-bootstrap,cia-backlog-run,cia-remote-backlog,cia-inline-fallback,cia-chat-filter,cia-send-helpers,cia-autonomy-advisor,cia-runtime-state,cia-cognitive-health}.service.ts` + `cia.module.ts` + `cia.controller.ts`.

### ai-brain migration
- All substantive services are `@deprecated` stubs: `agent-assist.service.ts`, `hidden-data.service.ts`, `media-factory.service.ts`, `vector.service.ts`, `knowledge-base.service.ts`.
- 6 real (non-stub) files remain: `ai-brain.module.ts`, `knowledge-base.controller{,.spec}.ts`, `agent-assist.helpers{,.spec,.charging.spec,.refund.spec}.ts` — these are seams (module + controller + helpers used by both new and legacy callers).

### brain migration
- **COMPLETE**: the lone `backend/src/brain/brain-spine-audit.service.ts` is a `@deprecated` stub.

### Brain → Mind alias map (ADR-0013 Wave M1, 4-week window)
- 7/7 substrate aliases present and verified `@deprecated`:
  - `BrainAutonomyService` → `MindAutonomyCoordinator`
  - `BrainCapabilityExecutorService` → `MindCapabilityExecutor`
  - `BrainCapabilityRegistryService` → `MindCapabilityRegistry`
  - `BrainCommercialGraphService` → `MindCommercialGraph`
  - `BrainEventSpineService` → `MindEventSpine`
  - `BrainRuntimeService` → `MindRuntime`
  - `WhatsAppBrainService` → `WhatsAppMindCoordinator`
- (The eighth "alias" — `BrainCapabilityRegistry` const file — is documentary, not a service.)

### P0 duplicates
- 4/4 resolved (canonical-duplicates ratchet baselined).

### Canonical gates wired into `package.json`
- `canonical:check:waha` — blocks any direct `waha*` import outside `marketing/channels/whatsapp/providers`.
- `canonical:check:brain` — blocks direct `Brain*` imports outside the alias stubs and impl files.
- `canonical:check:utils-drift` — detects cross-boundary util duplication drift.
- Plus `gate-asaas-ban`, `gate-event-taxonomy-namespace`, `gate-math-random`, `gate-prisma-any` in `scripts/ops/canonical/`.
- Composite: `npm run canonical:check`.

---

## What's Pending (priority ordered)

### 1. WhatsApp residual — 3 substantive non-stub files (HIGH leverage, single wave)
Only **three** real files remain at `backend/src/whatsapp/` top-level:
- `backend/src/whatsapp/whatsapp.module.ts` — the legacy module declaration, still imported by 8 outside modules (`app.module`, `health.module`, `meta.module`, `marketing.module`, `marketing-channels.module`, `mass-send.module`, `webhooks.module`, `cia.module`).
- `backend/src/whatsapp/whatsapp-catchup.service.ts` — imported by 4 outside files (cia-backlog-run, cia-bootstrap, cia-bootstrap.spec, whatsapp-api-webhook.controller).
- `backend/src/whatsapp/whatsapp-catchup-orchestrator.service.ts` — only consumed internally by catchup.service plus its own spec.

Plus the residual **test/helper/util orbit** (25 files: `cia-*.spec.ts`, `cia-runtime.fixtures.ts`, `cia-bootstrap.constants.ts`, `whatsapp-digits.util.spec.ts`, `whatsapp-catchup.*.spec.ts`, `whatsapp-catchup-orchestrator.helpers.ts`, `whatsapp-catchup.normalizers.ts`, `whatsapp-catchup.service.spec-helpers.ts`, `providers/provider-*.{ts,spec.ts}`, `worker-runtime.service{,.ts}.spec.ts`) — these move once the 3 substantive files move.

### 2. Brain consumer migration — 4 files import legacy Brain\* outside impl
- `backend/src/kloel/brain-capabilities.const.ts` — 2 doc refs (cosmetic, fix in passing).
- `backend/src/kloel/brain-capability-executor.substrate.helpers.ts` — 1 JSDoc ref (cosmetic).
- `backend/src/kloel/guest-chat.chat.helpers.ts` — **REAL CONSUMER**: imports `BrainEventSpineService`, uses it as a parameter type. Should switch to `MindEventSpine`.
- `backend/src/kloel/whatsapp-brain.service.ts` — itself the legacy alias stub; will be removed at end of ADR-0013 alias window.

### 3. Event taxonomy implementation
- `docs/architecture/EVENT_TAXONOMY.md` + `EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md` exist but enforcement is partial.
- `gate-event-taxonomy-namespace.mjs` exists; verify it's invoked from `canonical:check` and add a `:strict` mode mirroring the brain/waha pattern.
- AsyncAPI shows **122** channels — confirm every channel name matches the taxonomy namespace and add a contract sync gate.

### 4. Frontend canonicalization — still rough
- 0 legacy `whatsapp/whatsapp*` imports in frontend (good).
- BUT `docs/architecture/FRONTEND_DEDUP_AUDIT.md` exists for a reason: duplicated components/hooks across `frontend/src` and `frontend-admin`. Cross-reference with SARIF (5 + 16 findings).

### 5. Worker canonicalization — still rough
- 0 legacy whatsapp imports (good); 0 SARIF findings (excellent).
- But there's no canonical-domain gate enforcing where worker processors live (`worker/processors/*` vs scattered).

### 6. `whatsapp.module.ts` consolidation
- Once the 3 substantive files move, the module declaration could either:
  (a) become an `@deprecated` re-export of `MarketingChannelsModule` (preferred, alias-window pattern), or
  (b) be inlined into `marketing.module.ts`.
- 8 outside modules currently import `WhatsappModule` — they switch to `MarketingChannelsModule` (or to direct `MarketingModule` if the channel layering is acceptable).

### 7. Backend SARIF debt — 8 592 findings
- Out of scope for canonicalization, but worth re-baselining once moves stabilize to make sure new homes aren't carrying findings forward silently.

---

## Recommended Next Waves

### Wave 26 — finish `backend/src/whatsapp/` substantive moves
Files to move:
- `backend/src/whatsapp/whatsapp-catchup.service.ts` → `backend/src/marketing/channels/whatsapp/whatsapp-catchup.service.ts`
- `backend/src/whatsapp/whatsapp-catchup-orchestrator.service.ts` → `backend/src/marketing/channels/whatsapp/whatsapp-catchup-orchestrator.service.ts`
- Plus the orbital helpers/normalizers/specs: `whatsapp-catchup.normalizers.ts`, `whatsapp-catchup-orchestrator.helpers.ts`, `whatsapp-catchup.service.spec-helpers.ts`, all four `whatsapp-catchup*.spec*.ts`.
- Leave `@deprecated` re-export stubs at the old paths; update 4 outside import sites to canonical.

### Wave 27 — relocate the cia residual + worker-runtime
Files to move (all spec + fixture + constants):
- `backend/src/whatsapp/cia-backlog-run.helpers.ts` (paired with already-moved `cia-backlog-run.service.ts`)
- `backend/src/whatsapp/cia-backlog-run.service.spec.ts`
- `backend/src/whatsapp/cia-bootstrap.constants.ts`, `cia-bootstrap.service.spec.ts`
- `backend/src/whatsapp/cia-chat-filter.service.spec.ts`
- `backend/src/whatsapp/cia-inline-fallback.service.spec.ts`, `cia-inline-fallback.service.processing.spec.ts`
- `backend/src/whatsapp/cia-remote-backlog.helpers.ts`, `cia-remote-backlog.service.spec.ts`
- `backend/src/whatsapp/cia-runtime.fixtures.ts`
- → all into `backend/src/kloel/mind/cia/`
- `backend/src/whatsapp/worker-runtime.service.ts` (+ both spec files): decide canonical home — `worker/` or `backend/src/kloel/mind/runtime/`.
- `backend/src/whatsapp/whatsapp-digits.util.spec.ts` → co-locate with `whatsapp-digits.util.ts` (already in canonical home).

### Wave 28 — finish `whatsapp/providers/` cleanup
Files to move:
- `backend/src/whatsapp/providers/provider-registry.ts` + `.types.ts` + `.spec.ts`
- `backend/src/whatsapp/providers/provider-send-message.helpers.ts` + `.spec.ts`
- → `backend/src/marketing/channels/whatsapp/providers/`
- Goal: zero non-`@deprecated` files left at `backend/src/whatsapp/providers/`.

### Wave 29 — dissolve `whatsapp.module.ts`
- Switch all 8 outside imports of `WhatsappModule` to `MarketingChannelsModule`.
- Convert `whatsapp.module.ts` to an `@deprecated` alias re-exporting `MarketingChannelsModule`.
- (Defer hard removal until the 4-week ADR-0013 alias window expires.)

### Wave 30 — Brain consumer migration
- Rewrite `backend/src/kloel/guest-chat.chat.helpers.ts` to import `MindEventSpine` from `./mind/coordination/mind-event-spine.service`.
- Fix the 2 cosmetic JSDoc refs in `brain-capabilities.const.ts` + `brain-capability-executor.substrate.helpers.ts`.
- Tighten `canonical:check:brain` gate to `--strict` and run from pre-push.

### Wave 31 — Event taxonomy enforcement
- Audit all 122 AsyncAPI channels against `EVENT_TAXONOMY.md`.
- Wire `gate-event-taxonomy-namespace.mjs` into `canonical:check`.
- Add contract-sync gate: regenerate `asyncapi-spec.json` and diff in CI.

### Wave 32 — Frontend dedup pass
- Process `docs/architecture/FRONTEND_DEDUP_AUDIT.md` queue.
- Cross-check with `tools/sarif/frontend{,-admin}.sarif` (21 combined findings) — possibly the same root cause.

### Wave 33 — Worker canonical domain gate
- Define canonical worker processor layout.
- Add `scripts/ops/check-worker-processor-locations.mjs`.

### Wave 34 — SARIF re-baseline
- Snapshot the 8 592 backend findings at canonical-home paths.
- Identify and fix any findings that "moved" from legacy paths but weren't fixed.

---

## Files NOT to touch this round
- Anything under `docs/design/` (visual contract) — protected.
- `ops/*.json`, `scripts/ops/check-*.mjs`, `scripts/ops/lib/*.mjs` — protected.
- `.husky/pre-push`, `.github/workflows/ci-cd.yml`, root eslint configs — protected.
- `backend/src/lib/ai-models.ts` — protected.
- `CLAUDE.md`, `AGENTS.md` — protected.

---

## Reproduce these numbers

```sh
# files at legacy whatsapp/
find backend/src/whatsapp -type f -name '*.ts' | wc -l

# non-deprecated files at legacy whatsapp/
for f in $(find backend/src/whatsapp -type f -name '*.ts'); do
  head -3 "$f" | grep -q "@deprecated" || echo "$f"
done | wc -l

# substantive non-stubs (exclude tests/utils/helpers/types/constants/fixtures/normalizers)
for f in $(find backend/src/whatsapp -maxdepth 2 -type f -name '*.ts'); do
  head -3 "$f" | grep -q "@deprecated" && continue
  case "$f" in *.spec.ts|*helpers.ts|*normalizers.ts|*util.ts|*types.ts|*constants.ts|*fixtures.ts) continue;; esac
  echo "$f"
done

# canonical home file count
find backend/src/marketing/channels/whatsapp -type f -name '*.ts' | wc -l

# remaining legacy whatsapp/whatsapp* import sites in backend
grep -rEn "from ['\"].*whatsapp/whatsapp" backend/src worker --include='*.ts' \
  | grep -v "marketing/channels/whatsapp" | wc -l

# Brain* consumers outside impl files
grep -rEn "BrainAutonomyService|BrainCapabilityExecutorService|BrainCapabilityRegistryService|BrainCommercialGraphService|BrainEventSpineService|BrainRuntimeService|WhatsAppBrainService" backend/src --include='*.ts' \
  | grep -v "brain-.*\.service\.ts" | grep -v "mind-.*\.service\.ts" | grep -v ".spec.ts"

# OpenAPI route count
node -e "const s=require('./tools/openapi/openapi-spec.json'); console.log(Object.keys(s.paths||{}).length)"

# AsyncAPI channel count
node -e "const s=require('./tools/asyncapi/asyncapi-spec.json'); console.log(Object.keys(s.channels||{}).length)"

# SARIF totals
cat tools/sarif/manifest.json
```
