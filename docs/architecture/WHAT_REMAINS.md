# WHAT_REMAINS — Canonicalization Wave Vision (v2 / post-Wave 29 snapshot)

> Generated by Wave 30 / subagent B on 2026-05-27 evening.
> Purpose: refresh macro vision after Waves 26-29 landed (whatsapp/ residual
> drained, cia/ orbital moved, frontend dead exports pruned, mind.* event
> aliases live, brain consumer migration complete). Investigation +
> planning only — this document does not move files itself.

---

## Macro Snapshot (2026-05-27 ~19:42 BRT)

| Metric | Value | Source |
|---|---|---|
| Commits since 06:00 today | **135** | `git log --since="06:00"` |
| Commits canonicalization-related | 92 | grep on commit subjects |
| Files in legacy `backend/src/whatsapp/` | **3** | `find` (was 81 in v1) |
| Non-`@deprecated` files at `backend/src/whatsapp/` | **0** | header scan (was 28 in v1) |
| Substantive (non-stub) leftovers at `backend/src/whatsapp/` | **0** | every leftover is now an `@deprecated` re-export shim |
| Files in canonical `backend/src/marketing/channels/whatsapp/` | **138** | `find` (+14 since v1) |
| Files in legacy `backend/src/cia/` | **15** | every file is an `@deprecated` re-export stub |
| Files in canonical `backend/src/kloel/mind/cia/` | **33** | `find` |
| Files in canonical `backend/src/kloel/mind/` (all) | **127** | `find` |
| Files in `backend/src/ai-brain/` | **13** | mix of stubs + module seam |
| Files in `backend/src/brain/` | **1** | `brain-spine-audit.service.ts` (deprecated stub) |
| Brain\* consumers outside impl/aliases/specs | **5** (3 cosmetic JSDoc, 1 alias-self, 1 cosmetic comment) | grep |
| Legacy `whatsapp/whatsapp*` import sites that bypass canonical home | **3** (2× `whatsapp.module`, 1× `whatsapp-dispatch.adapter`) inside marketing/ itself — within canonical tree | grep |
| Cross-boundary legacy `whatsapp/<file>` imports (backend-wide) | 55 — all routed through `@deprecated` shims (provider-settings.types is the dominant one) | grep |
| OpenAPI routes | 591 across 63 tags | `tools/openapi/openapi-spec.json` |
| AsyncAPI channels | 122 | `tools/asyncapi/asyncapi-spec.json` |
| SARIF findings (backend) | 8 592 | `tools/sarif/manifest.json` |
| SARIF findings (frontend) | 5 | same |
| SARIF findings (frontend-admin) | 16 | same |
| SARIF findings (worker) | 0 | same |
| `npm run canonical:check` verdict | **OK overall** — every soft/strict gate green; 1 soft WARN on direct brain-* import (cosmetic, see Wave 30 below) | run today |

**Verdict:** the omnicore + mind unification has effectively landed. The
remaining residual is **bookkeeping** (deprecation-window shim removal,
emit-site rewiring, frontend dedup audit follow-through), not architecture.

---

## What's DONE Since v1 (Waves 26-29 + adjacent)

### Wave 26 — whatsapp-catchup family moved to canonical
Commit `47c57ee94` finished the catchup orbit:
- `whatsapp-catchup.service.ts` + orchestrator + helpers + normalizers + 4 spec
  files now live under `backend/src/marketing/channels/whatsapp/`.
- Concurrent commits (`aef6b8439`, `fe00d1a4c`) dropped **36 + 16 = 52 orphan
  stubs** that the catchup move left behind.

### Wave 27 — cia orbital relocated
Commit `c023adfcd` ("refactor(mind): move cia-* helpers and specs from
whatsapp/ to kloel/mind/cia") finished the orbital cleanup:
- `cia-backlog-run.helpers.ts`, `cia-bootstrap.constants.ts`,
  `cia-runtime.fixtures.ts`, `cia-remote-backlog.helpers.ts`, and **9 spec
  files** all moved into `backend/src/kloel/mind/cia/`.
- The canonical `mind/cia/` home now hosts 33 files (was effectively created
  during M4 with 12).

### Wave 28 — providers/ residual cleared + 2 orphan specs moved
Commits `96241bf9d` (provider-registry suite) and `47c57ee94` (orphan specs)
collectively reduced `backend/src/whatsapp/providers/` to **a single
`@deprecated` shim** (`provider-registry.ts`) and zero non-deprecated files.

### Wave 29 — WhatsappModule consolidation (Strategy A landed; deletion deferred)
- Commit `6478b0cee` moved `whatsapp.module.ts` into
  `backend/src/marketing/channels/whatsapp/`.
- Commit `3c900660e` documented the strategy in
  `docs/architecture/WHATSAPP_MODULE_CONSOLIDATION_PLAN.md`.
- All **8 outside importers** (app / health / meta / cia / marketing /
  marketing-channels / mass-send / webhooks) now point at
  `marketing/channels/whatsapp/whatsapp.module` — verified by grep.
- `backend/src/whatsapp/whatsapp.module.ts` itself remains as a one-line
  `@deprecated` re-export stub for the 4-week ADR-0013 alias window.

### Brain consumer migration (was Wave 30 in v1) — COMPLETED EARLY
Commit `2c668ceb3` migrated the final `BrainEventSpineService` consumer
(`backend/src/kloel/guest-chat.chat.helpers.ts`) to `MindEventSpine`. The
remaining 5 grep hits are all cosmetic JSDoc / class-name self-references in
the alias stubs themselves — no functional Brain\* consumers outside the
alias-stub layer remain.

### mind.* event taxonomy aliases landed
Commit `191b90fa6` ("feat(mind): introduce mind.* event aliases on
BRAIN_EVENT_TAXONOMY") adds the parallel mind.* event namespace alongside
the existing brain.* names. Emit-site rewiring is still pending (see below).

### Frontend whatsapp API surface audit + dead-export pruning
- `docs/architecture/FRONTEND_WHATSAPP_API_AUDIT.md` (commit `10de68fb9`)
  identified 29/35 exports of `frontend/src/lib/api/whatsapp.ts` as dead.
- Commit `69afa2531` deleted **28** of those dead exports
  (485 → 147 lines on `whatsapp.ts`). The lone remainder (`connectWhatsapp`)
  is now also gone (commit `10de68fb9`).

### Worker canonical audit document
Commit `fd51701fd` shipped the worker canonical-domain audit + plan
(no code changes yet — only the playbook). SARIF still shows 0 findings.

### Tactical kloel hardening (parallel agent)
A separate concurrent agent landed **24+** `fix(kloel)` commits routing tool
calls through dispatcher/receipts, blocking legacy fake-success paths, and
persisting state via memory services. Not directly canonicalization, but
they touched files that the canonicalization waves had moved and validated
each move stayed wired correctly.

---

## What's TRULY Remaining

### A. `backend/src/whatsapp/` residual — 3 files, all `@deprecated` stubs

```
backend/src/whatsapp/whatsapp.module.ts            (1-line stub)
backend/src/whatsapp/provider-settings.types.ts    (1-line stub, heavily depended-on — 10+ outside imports)
backend/src/whatsapp/providers/provider-registry.ts (re-export stub)
```

**Sunset plan:** keep stubs until 2026-06-24 (ADR-0012 W3 alias window),
then delete + update the ~55 cross-boundary import sites that still go
through them (mostly `provider-settings.types`).

### B. `backend/src/cia/` residual — 15 files, ALL `@deprecated` re-export stubs

Every file matches the pattern:
```ts
/**
 * @deprecated This module has moved to `backend/src/kloel/mind/cia/...`
 *   (canonical home per ADR-0013 Wave M4). Sunset 2026-06-24.
 */
export * from '../kloel/mind/cia/...';
```

The canonical home is `backend/src/kloel/mind/cia/` (33 files). Sunset on
the ADR-0013 alias window.

### C. Brain consumer migration — DONE on the runtime path; 4 cosmetic refs left

The 5 grep hits in the manifest break down as:
- 2 JSDoc `@see` refs in `brain-capabilities.const.ts` (cosmetic)
- 1 JSDoc class-name ref in `brain-capability-executor.substrate.helpers.ts` (cosmetic)
- 2 self-references inside `kloel/whatsapp-brain.service.ts` (the alias stub itself — class name `WhatsAppBrainService`)

None of these is a runtime consumer. The runtime path was closed by
`2c668ceb3`.

### D. Internal cross-import inside the canonical whatsapp/ tree still uses legacy `cia/`

Three files inside `backend/src/marketing/channels/whatsapp/` import from
the legacy `../../../cia/...` path (which works because the stubs re-export
correctly, but isn't ideal):

```
backend/src/marketing/channels/whatsapp/whatsapp-service.types.ts:3  → '../../../cia/cia-runtime.service'
backend/src/marketing/channels/whatsapp/cia-remote-backlog.helpers.ts:1 → '../../../cia/cia-chat-filter.service'
backend/src/marketing/channels/whatsapp/cia-remote-backlog.helpers.ts:2 → '../../../cia/cia-send-helpers.service'
backend/src/marketing/channels/whatsapp/whatsapp.module.ts:92        → require('../../../cia/cia-runtime.service')
```

These should be rewired to `../../../kloel/mind/cia/...` so the canonical
tree doesn't depend on alias stubs.

### E. Event taxonomy emit-site rewiring — still pending

`mind.*` event names are now declared (commit `191b90fa6`) but the actual
`emit(...)` call sites across the backend still use `brain.*` names.
122 AsyncAPI channels need a sweep against the taxonomy doc, and the
`gate-event-taxonomy-namespace.mjs` should be tightened from soft to strict
once emit-sites are clean.

### F. Frontend canonicalization — partial

- 28 dead exports deleted (this session).
- `docs/architecture/FRONTEND_DEDUP_AUDIT.md` queue still pending.
- Duplicate-pair consolidation across `frontend/src` ↔ `frontend-admin`
  (21 combined SARIF findings, both surfaces).

### G. Worker canonicalization — code pending

Audit doc (`fd51701fd`) landed; no code moves yet. Worker has 0 SARIF
findings, so risk is low. `worker/processors/*` layout still
unenforced — no `scripts/ops/check-worker-processor-locations.mjs`.

### H. `whatsapp.module.ts` final delete

The stub at `backend/src/whatsapp/whatsapp.module.ts` is safe to delete
once the 4-week ADR-0013 alias window expires (2026-06-24). All callers
already use the canonical path — verified by grep.

### I. Backend SARIF debt — 8 592 findings

Out of scope for canonicalization but worth re-baselining once items A-D
above resolve. The findings count has been stable across the day's
60+ refactor commits, suggesting no canonicalization-induced regressions.

---

## Recommended Next Waves

### Wave 30 — Rewire internal cia imports inside canonical whatsapp/ tree (item D)
Files to edit:
- `backend/src/marketing/channels/whatsapp/whatsapp-service.types.ts:3`
- `backend/src/marketing/channels/whatsapp/cia-remote-backlog.helpers.ts:1-2`
- `backend/src/marketing/channels/whatsapp/whatsapp.module.ts:92` (the `useExisting: require(...)` runtime ref)

Change `../../../cia/cia-*.service` → `../../../kloel/mind/cia/cia-*.service`
in 4 lines total. Low risk; canonical tree should not depend on legacy
alias stubs.

### Wave 31 — Event-taxonomy emit-site sweep (item E)
For every `emit(...)` / `on(...)` callsite that uses `brain.*` names,
add a parallel `mind.*` emit (dual-emit window) per the migration plan
in `docs/architecture/EVENT_TAXONOMY_KLOEL_TO_MIND_MIGRATION.md`. After
dual-emit lands, flip `gate-event-taxonomy-namespace.mjs` to strict.

### Wave 32 — Frontend dedup pass (item F)
Process `docs/architecture/FRONTEND_DEDUP_AUDIT.md` queue. Cross-check
each duplicate against `tools/sarif/frontend{,-admin}.sarif` for shared
root causes.

### Wave 33 — Worker canonical-domain gate (item G)
- Add `scripts/ops/check-worker-processor-locations.mjs`.
- Wire into `npm run canonical:check`.
- Per the audit doc, enforce one canonical path for processors and reject
  drift.

### Wave 34 — SARIF re-baseline (item I)
Snapshot the 8 592 backend findings at canonical-home paths so that
findings which "moved" from legacy paths but weren't fixed are surfaced.

### Wave 35 — ADR-0013 alias window expiry sweep (2026-06-24)
Plan to land in 4 weeks:
- Delete `backend/src/whatsapp/whatsapp.module.ts`
- Delete `backend/src/whatsapp/provider-settings.types.ts` (after rewiring
  ~55 cross-boundary import sites — small codemod)
- Delete `backend/src/whatsapp/providers/provider-registry.ts`
- Delete all 15 stub files in `backend/src/cia/`
- Delete all ai-brain/brain stubs that have reached their sunset date
- Final `rm -rf backend/src/whatsapp backend/src/cia backend/src/brain` once
  zero importers remain.

### Wave 36 — Brain alias-stub deletion (also 2026-06-24)
- Once all 7 Brain\* aliases reach sunset, delete the alias stubs and
  rename `WhatsAppBrainService` → `WhatsAppMindCoordinator` at call sites.

### Wave 37 — `gate-event-taxonomy-namespace.mjs --strict`
- After Wave 31 dual-emit window closes, flip the gate.

### Wave 38 — Final canonical:check tightening
- Convert every `WARN` (soft) gate to `FAIL` (strict).
- Run from pre-push; current state is `OK overall` with 1 soft WARN on
  brain imports (cosmetic JSDoc refs — fix in passing during Wave 36).

### Wave 39 — Public ARCHITECTURE_INDEX.md refresh
- The architecture-doc index has accumulated many new docs. Refresh the
  index, mark closed-out plans, and link the 8 MIND_M5_MOVE_BATCH_*.md
  reports as historical.

---

## Files NOT to touch this round
- Anything under `docs/design/` (visual contract) — protected.
- `ops/*.json`, `scripts/ops/check-*.mjs`, `scripts/ops/lib/*.mjs` — protected.
- `.husky/pre-push`, `.github/workflows/ci-cd.yml`, root eslint configs — protected.
- `backend/src/lib/ai-models.ts` — protected.
- `CLAUDE.md`, `AGENTS.md` — protected.

---

## Reproduce these numbers

```sh
# commits since 06:00 today
git log --since="06:00" --oneline | wc -l

# whatsapp/ residual
find backend/src/whatsapp -type f -name '*.ts' | wc -l
for f in $(find backend/src/whatsapp -type f -name '*.ts'); do
  head -3 "$f" | grep -q "@deprecated" || echo "$f"
done   # expect: zero output → all stubs

# cia/ residual (all should be stubs)
find backend/src/cia -type f -name '*.ts' | wc -l

# canonical homes
find backend/src/marketing/channels/whatsapp -type f -name '*.ts' | wc -l
find backend/src/kloel/mind -type f -name '*.ts' | wc -l
find backend/src/kloel/mind/cia -type f -name '*.ts' | wc -l

# Brain consumers outside aliases/impls/specs
grep -rEn "BrainAutonomyService|BrainCapabilityExecutorService|BrainCapabilityRegistryService|BrainCommercialGraphService|BrainEventSpineService|BrainRuntimeService|WhatsAppBrainService" backend/src --include='*.ts' \
  | grep -v "brain-.*\.service\.ts" | grep -v "mind-.*\.service\.ts" | grep -v ".spec.ts"

# internal canonical-tree imports that still hit legacy cia/
grep -rEn "from ['\"][^'\"]*\\.\\./cia/" backend/src/marketing/channels/whatsapp --include='*.ts'

# legacy whatsapp/ cross-boundary import sites still routed through stubs
grep -rEn "from ['\"][^'\"]*\\.\\./whatsapp/" backend/src --include='*.ts' \
  | grep -v "marketing/channels" | wc -l

# canonical:check verdict
npm run canonical:check 2>&1 | tail -15

# OpenAPI / AsyncAPI / SARIF
node -e "const s=require('./tools/openapi/openapi-spec.json'); console.log(Object.keys(s.paths||{}).length)"
node -e "const s=require('./tools/asyncapi/asyncapi-spec.json'); console.log(Object.keys(s.channels||{}).length)"
cat tools/sarif/manifest.json
```
