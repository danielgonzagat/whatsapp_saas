# Kloel CIA Vision Traceability Matrix

Generated: 2026-05-11

Statuses used: `[ENTREGUE_PROVADO]`, `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]`, `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]`, `[INVALIDADO_POR_DESCOBERTA]`.

| ID | Tese da visao | Significado operacional | Evidencia atual no repo | Gap | Wave responsavel | Criterio de aceite | Status |
|---|---|---|---|---|---|---|---|
| V01 | Kloel e Inteligencia Comercial Autonoma | Cinco canais ligados a um cerebro unificado | `backend/src/kloel/**`, `backend/src/cia/**`, `worker/processors/cia/**`, `worker/processors/autopilot/**`; W9 proves local UnifiedAgent inbound WhatsApp intent -> `send_message` action -> WhatsApp send mock -> `AutopilotEvent` | Prova ponta-a-ponta live multi-canal ainda depende de provedores/env | W6/W7/W9 | Inbound de canal real cria percepcao e acao rastreavel | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V02 | Substitui trabalho comercial operacional repetitivo | Atendimento, qualificacao, follow-up, recuperacao e fechamento | Autopilot worker em `worker/processors/autopilot/**`; Kloel services em `backend/src/kloel/**` | Falta golden path com venda real sandbox + follow-up/recover/close provados | W6/W7 | Fluxo automatizado comprovado com registros e ledger | `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]` |
| V03 | Cerebro neurossimbolico | Camada simbolica + LLM + orquestrador | `worker/processors/cia/brain.ts`, `brain.governor.ts`, `cognitive-state*`, `backend/src/kloel/unified-agent*`; W9 proves mocked LLM tool planning can dispatch a concrete outbound action through UnifiedAgentActions | Observability checklist precisa fechar 11 pontos e live policy/action trace | W6/W9 | Todos os 11 pontos mapeados com teste/log | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V04 | Mesma CIA no chat e nos canais | Chat interno e canais externos compartilham contexto | `ChatThread`, `ChatMessage`, `KloelMemory`; W6 bridge dispatches Omnichannel Instagram/Messenger inbound to `UnifiedAgentService`; W7 chat summary tool now reads paid checkout revenue and wallet balances from real tables | Precisa provar comando estrategico no chat alterando politica observavel em canal subsequente | W6/W7 | Comando no chat altera politica observavel no canal; pergunta operacional usa dados reais | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V05 | Marketing como porta de entrada da CIA para canais | Interface unica com cinco canais | `frontend/src/components/kloel/marketing/*MarketingTab.tsx`, `MarketingView.tsx`; W1 E2E abre os cinco canais oficiais | W1 wizard spine provado; operacao real dos canais ainda depende W2-W6 | W1/W5/W6 | E2E abre os 5 canais e persiste setup | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V06 | Wizard de 4 passos em todos os 5 canais | Conexao, Produtos, Arsenal, Configuracao | `OfficialMarketingChannelPage.tsx`, `GET/POST /marketing/connect/channel-setup`; W1 E2E 4/4 passou | W1 quatro passos provados; schema dedicado `ChannelSetup` nao existe e permanece governado | W1 | Visual + E2E por canal | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V07 | Passo 1 visivel no funil | Indicador 1-2-3-4 sem colisao | W1 Playwright cobre 1024px e 380px nos cinco canais oficiais | Prova local registrada; producao ainda precisa validacao final W9 | W1/W9 | Screenshot >=1024px e >=380px | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V08 | Conversas unificadas dos 5 canais | Inbox cross-canal | Inbox frontend/backend exist; W5 adds TikTok Omnichannel adapter and Facebook/TikTok filters | Email inbound adapter and real five-channel records still pending | W5 | `D-INBOX` com registros reais | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V09 | Identidade reconciliada cross-canal | Contato unico quando ha sinal forte | `worker/processors/autopilot/identity-resolve.ts`, `identity.ts` | Prova cross-canal ainda pendente | W5 | Teste com sinais fortes e sem invencao | `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]` |
| V10 | Meta sem erro de URL | WhatsApp/Instagram/Facebook conectam via OAuth/Embedded Signup | W2 code-side patch: channel-specific Meta Config IDs/scopes, public callback reachable, invalid webhook verify rejected, unsigned webhook POST rejected when `META_APP_SECRET` is configured; W8 production startup now fails if `META_APP_SECRET` is absent | Graph token smoke/env inventory/dashboard App Domains and Redirect URIs still external-blocked in this shell | W2 | 11 passos Meta registrados | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V11 | TikTok conectado dentro do limite da plataforma | OAuth/status/webhook/polling conforme aprovacao | W3 hardens TikTok webhook signatures and backend disconnect; W5 routes TikTok webhook payloads with workspace to Omnichannel/inbox/UnifiedAgent perception; W8 production startup now fails if `TIKTOK_CLIENT_SECRET` is absent | App review/env externo pendente; frontend disconnect and live OAuth smoke pending | W3/W5 | OAuth sandbox/live real ou bloqueio externo documentado | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V12 | Email nao e toggle | Mailbox do cliente via Gmail/Microsoft/IMAP+SMTP | EMAIL-0 accepted: existing Email is toggle + campaign + shared-sender; EMAIL-1 added `MailboxConnection`, provider/status enums, and mailbox token crypto; EMAIL-2 adds Gmail OAuth URL, signed public callback, encrypted token storage, and mailbox status; EMAIL-3 adds manual Gmail inbound sync to Omnichannel; EMAIL-4 adds basic Gmail outbound through the customer mailbox with unsubscribe headers; EMAIL-5/6 routes CIA `send_message` for `channel=email` through Gmail mailbox; EMAIL-7 adds Microsoft OAuth URL/callback/encrypted token storage/status overlay; EMAIL-8 adds IMAP+SMTP validation and encrypted credential storage; EMAIL-9 adds opt-out suppression before proactive Gmail sends; EMAIL-10 adds mailbox metrics without PII tags for Gmail/Microsoft/IMAP connection and Gmail sync/send; W8 requires `EMAIL_INBOUND_SECRET` for public email marketing webhook reconciliation in production and production startup now fails if `EMAIL_INBOUND_SECRET`/`EMAIL_TOKEN_ENCRYPTION_KEY` are absent | Gmail/Microsoft/IMAP live smoke, Microsoft Graph inbound/outbound, IMAP polling/SMTP send, worker/push sync, alerting e broader compliance | W4 EMAIL-10 | Gmail/Microsoft/IMAP inbound+outbound cliente | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V13 | CIA le todas inbound | Webhook/polling/IDLE entrega percepcao ao cerebro | WhatsApp reaches UnifiedAgent; W6 dispatches Omnichannel Instagram/Messenger inbound to UnifiedAgent; W5 adds TikTok webhook adapter into the same bridge; EMAIL-3 routes Gmail messages through Omnichannel; EMAIL-5/6 gives Email a channel-aware `send_message` outbound path; W9 propagates request `correlationId` into generic webhook, finance webhook, and outbound webhook queue jobs, and worker `startJob()` now preserves that upstream correlation in lifecycle logs | Live Gmail smoke, Microsoft/IMAP inbound, worker/push sync, and full outbound multicanal proof pending | W4/W5/W6/W9 | `I1` por canal + trace webhook to job to worker log | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V14 | CIA responde conforme politica | Tom/agressividade/horario afetam politica | `set_sales_policy` now persists owner strategic policy in `Workspace.providerSettings.autopilot.salesPolicy`; `UnifiedAgentContextService` injects active policy into the CIA system prompt | Live channel behavior diff still needs provider smoke; horario-specific enforcement not yet proved | W6/W9 | `I3`/politica consultada em log | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V15 | CIA toma iniciativa proativa | Follow-up, recuperacao, oferta, pos-venda | `worker/processors/autopilot/followup.ts`, `execution-planner.ts`, `cart-recovery.service.ts`; W9 proves local inbound-to-outbound `send_message` dispatch through UnifiedAgent, while high-volume proactive sends remain approval-gated | Precisa prova sandbox sem disparo real indevido | W6/W7/W9 | Proativo em conta teste com ledger | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V16 | Produtos alimentam conhecimento | Produto vira contexto consultavel | `Product`, `ProductAIConfig`, `kloel-product-context-formatter.ts` | Precisa teste chat/canal consultando produto real | W7 | Produto cadastrado aparece na resposta da CIA | `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]` |
| V17 | Cliente conversa com CIA no chat interno | Mensagens persistidas, streaming, ferramentas | `ChatThread`, `ChatMessage`, `kloel-thread.service.ts`, frontend chat components; W7 `get_dashboard_summary` now includes real paid sales and wallet context for owner questions | Precisa smoke de reload/stream/ferramenta e conversa real perguntando "como foi o dia" | W6/W7 | `D-CHAT` + resumo operacional real | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V18 | Comandos estrategicos alteram comportamento | Chat atualiza politica da CIA | `backend/src/kloel/kloel-chat-tools.service.ts`, `backend/src/kloel/kloel-tool-dispatcher.service.ts`, and `backend/src/kloel/unified-agent-context.service.ts` now prove chat tool -> persisted sales policy -> prompt context | Full external-channel before/after conversation still pending provider/test-account smoke | W6/W9 | Antes/depois em conversa de teste | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V19 | Vendas/Carteira/Relatorios reais | Checkout, wallet, reports reconciliados | `backend/src/checkout/**`, `backend/src/billing/**`, `backend/src/reports/**`, wallet models; W7 adds `checkout-paid-effects/wallet.ts` so `CheckoutOrder` paid updates credit `KloelWallet` with idempotent transaction/ledger writes; W7 extends chat summary to paid orders/revenue and wallet balances; W9 adds PrismaService proof that direct and transactional `CheckoutOrder` PAID transitions trigger social conversion, CAPI, affiliate, wallet credit, and WhatsApp post-payment effects; W9 aligns boleto UI/routes/copy with the current card/PIX-only backend rail and adds a billing mock-mode regression spec | Checkout-to-wallet and chat summary code paths are locally tested; sandbox checkout payment smoke and report/chat screenshots remain pending | W7/W9 | Checkout sandbox pago + wallet + report + chat summary | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V20 | Aprovacao humana alto risco | ApprovalRequest visivel | `ApprovalRequest` model exists; W8 queues high-risk `create_campaign` and `change_plan` chat tool calls as `ApprovalRequest` instead of executing immediately; `GET /kloel/approvals/pending` lists open workspace approvals; `POST /kloel/approvals/:id/{approve,reject,adjust}` records owner decisions; owner chat dashboard renders pending approvals from the real API; approved `kloel_tool:create_campaign` and `kloel_tool:change_plan` requests execute the original tool and finish as `COMPLETED`; the `change_plan` owner prompt now includes the actual requested `newPlan`; legacy Connect payout route now opens a payout ApprovalRequest instead of executing Stripe payout directly; email campaign send now creates `email_campaign:send` approval before enqueueing provider delivery and `EmailMarketingService.enqueueSend()` now defensively requires that approval; direct email send now creates `marketing_email:direct_send` approval before any provider send; Meta Ads status mutation now creates `meta_ads:campaign_status` approval before Graph API mutation; legacy wallet withdrawal now creates `wallet:withdrawal` approval before debiting available balance; legacy WhatsApp mass campaign start now creates `whatsapp_campaign:start` approval before enqueueing the mass-send queue; owner sale refund now creates `sale:refund` approval before calling Stripe refunds | Admin backoffice refund remains classified as manual IAM/audit operation without second-approval gate; browser UI approval smokes pending | W8 | `I4` com aprovar/rejeitar/ajustar | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V21 | Anuncios com regras reais | Regras persistem e worker executa | `backend/src/kloel/ad-rules*`, `backend/src/integrations/*ads*`, `AnunciosView.tsx` | Execucao provider real pendente; current rule engine proof is persistence/evaluation/logging, not provider-side action | W8/Trilho B | Regra executada contra sandbox/provider | `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]` |
| V22 | Admin com IAM, audit log, LGPD | `adm` com IAM/audit/compliance | `backend/src/admin/**`, `backend/src/gdpr/**`, AdminChat models; `frontend-admin` has buildable routes for audit/compliance/IAM/client/account/admin modules and uses the Next 16 `proxy.ts` convention; W8 OpenCode admin/compliance audit accepted; `/gdpr/delete` and `/gdpr/export` compatibility routes now delegate to the canonical `GdprService` request workflows instead of weaker inline behavior; admin role/status changes now revoke active sessions transactionally and audit the revoked count; GDPR export/delete now covers owner `ChatMessage` rows tied to the user and workspace; GDPR delete now unlinks the deleting user from assigned conversations and authored messages while preserving retained commercial records | Live `adm.kloel.com` browser smoke and broader retention classification for financial/business records remain pending | W8 | Login admin + audit + LGPD | `[ENTREGUE_PENDENTE_BLOQUEIO_EXTERNO]` |
| V23 | Sem fake | UI endpoint persistencia worker reais | Regras globais existem; muitas superficies reais; W8 hardens several public/high-risk paths with real approval, GDPR, admin build, and webhook-secret behavior instead of mock success paths; W9 removes implemented-channel `soon` labels, fills owner chat quick actions, replaces `#` auth legal links, makes checkout success status truthful, persists guest chat through Redis, aligns boleto UI/routes/copy with backend card/PIX support, proves billing mock mode cannot activate in production, aligns the owner smart-payment modal with the backend Pix response contract, and removes active boleto thank-you/reminder settings from product/plan tabs | Ainda precisa varredura anti-fake completa por rota/fluxo, browser smoke e provider live proof; Batch 18 OpenCode read-only audit was rejected as incomplete | Todas | Nenhuma tela mock em fluxo provado | `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]` |
| V24 | Design Terminator preservado | Void/Ember/Sora/raio<=6/SVG-only/zero gradient/zero emoji | Design rule in `CLAUDE.md`; marketing components exist | Validacao visual ainda pendente | W1/W9 | Visual contract + review | `[BACKLOG_GOVERNADO_PARA_PROXIMA_EXECUCAO]` |
