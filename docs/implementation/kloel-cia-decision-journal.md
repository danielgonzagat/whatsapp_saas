# Kloel CIA Decision Journal

Generated: 2026-05-11

| Data       | Decisao                                                                                                                          | Alternativas consideradas                                                                                                                             | Motivo                                                                                                                                                                                                                                                                                                                   | ID-visao referenciado | Risco                                                                                                                                                                                | Plano de rollback                                                                                                                                                                   |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-11 | Created `feat/kloel-cia-convergence` from current dirty `chore/purga-total-debt` HEAD instead of switching to `main`             | Switch/pull `main`; stash dirty worktree; create branch from current HEAD                                                                             | Worktree had extensive uncommitted human/agent work including protected files; switching/stashing would risk disrupting accumulated work                                                                                                                                                                                 | V01-V24               | Branch base is not clean `main`; future PR strategy must reconcile                                                                                                                   | No destructive rollback; keep docs isolated in `docs/implementation/**`                                                                                                             |
| 2026-05-11 | Treat `scripts/decomp/opencode-subagent-delegation-rules.md` as `PULSE_ONLY_RULE`                                                | Apply it globally; ignore it                                                                                                                          | Title/content explicitly scope it to PULSE auditor debt and `scripts/pulse/no-hardcoded-reality-audit.ts`; global use would misroute Email/Marketing work                                                                                                                                                                | V23                   | Non-PULSE subagents might miss useful safety language                                                                                                                                | Use global AGENTS/CODEX/CLAUDE plus domain docs instead                                                                                                                             |
| 2026-05-11 | Treat `opencode-fleet.mjs` as observable/auditable, not literally interactive                                                    | Require TTY live sessions for every domain; use Codex subagents; use shell background logs                                                            | Runner captures prompt/stdout/stderr/exit/summary artifacts and is the repo tool; PULSE live-interactive rule remains PULSE-only                                                                                                                                                                                         | V23                   | Non-interactive model may fail without live steering                                                                                                                                 | Reject subagent output unless artifacts and diff review exist                                                                                                                       |
| 2026-05-11 | Register env provider inventory as blocked instead of printing or guessing values                                                | Print Railway/Vercel vars; infer live config from code                                                                                                | `RAILWAY_TOKEN` and `VERCEL_TOKEN` absent in current shell; secrets must not be exposed                                                                                                                                                                                                                                  | V10, V12, V19         | Live env drift remains unknown                                                                                                                                                       | Re-run env inventory when tokens are available through secret manager                                                                                                               |
| 2026-05-11 | Persist W1 channel setup in `Workspace.providerSettings.marketingChannelSetup` instead of creating a new ChannelSetup schema now | Create new Prisma models immediately; keep UI local-only                                                                                              | The repo already uses `Workspace.providerSettings` for channel provider/setup state and the W1 goal was a smallest safe persisted wizard spine, not a schema migration                                                                                                                                                   | V05, V06, V07, V23    | ProviderSettings JSON can become less queryable than dedicated models                                                                                                                | Replace with schema-backed models later through a migration while preserving the public `connect/channel-setup` API                                                                 |
| 2026-05-11 | Split Meta OAuth scopes/config IDs per channel before dashboard validation                                                       | Keep a single config/scope bundle for WhatsApp, Instagram, and Facebook; defer all W2 work to external dashboard                                      | The generated URL was requesting cross-channel scopes and one config fallback; code-side determinism is required before classifying residual failures as Meta dashboard/env blockers                                                                                                                                     | V10, V13, V23         | Meta dashboard may still require exact config/scope alignment not visible from code                                                                                                  | Revert channel-specific selection if provider evidence proves the single-bundle model is required                                                                                   |
| 2026-05-11 | Enforce missing Meta webhook signature when `META_APP_SECRET` is configured                                                      | Preserve previous behavior accepting unsigned requests when header is absent; require signature unconditionally even without configured secret        | With a configured secret, accepting missing signatures is unsafe; without the env, local/test portal probes can still be classified as env/governance debt instead of silently pretending S4 passes                                                                                                                      | V10, V13, V23         | Production S4 still depends on proving `META_APP_SECRET` exists in Railway                                                                                                           | Revert only if Meta provider behavior proves signed callbacks cannot be delivered, then register an external/security exception explicitly                                          |
| 2026-05-11 | Dispatch Omnichannel non-WhatsApp inbound to `UnifiedAgentService` with `executeTools: false`                                    | Leave Instagram/Messenger at inbox-only; call full unified agent tool execution for all channels immediately                                          | This proves a shared brain perception/decision path without sending WhatsApp-only actions to Instagram/Messenger identifiers before a channel-aware outbound dispatcher exists                                                                                                                                           | V04, V13, V23         | Non-WhatsApp channels get brain analysis but no autonomous outbound yet                                                                                                              | Remove `maybeDispatchToUnifiedAgent()` and `executeTools` handling, or replace it with a true channel dispatcher when W6 outbound is implemented                                    |
| 2026-05-11 | Represent Email mailbox credentials with explicit OAuth and IMAP/SMTP fields in `MailboxConnection`                              | Store a single generic token blob; delay schema until OAuth implementation                                                                            | EMAIL-1 must support Gmail/Microsoft OAuth plus IMAP+SMTP and encrypted at-rest credentials without schema churn in EMAIL-2/7/8                                                                                                                                                                                          | V12, V13, V15         | Migration not generated/applied yet; fields may need provider-specific refinement                                                                                                    | Adjust schema before migration generation; no production DB mutation has occurred                                                                                                   |
| 2026-05-11 | Route only workspace-bound TikTok webhook payloads into Omnichannel                                                              | Keep TikTok audit-only; infer workspace from global/default config; block all TikTok webhook routing until live payload shape is known                | Without live TikTok app approval, the only safe code-side adapter is one that requires an explicit workspace signal in the payload and refuses to invent ownership                                                                                                                                                       | V08, V11, V13, V23    | Real TikTok payloads may require account lookup rather than payload workspace fields                                                                                                 | Replace workspace extraction with provider-account lookup after live TikTok webhook payload evidence is available                                                                   |
| 2026-05-11 | Implement Gmail mailbox OAuth as a Marketing-owned service plus public signed-state callback                                     | Reuse customer login Google OAuth; keep callback behind JWT; finish only UI toggle                                                                    | Mailbox OAuth needs restricted Gmail scopes, refresh token storage, and a provider callback that can complete without a JWT bearer token while still proving workspace ownership through signed state                                                                                                                    | V12, V13, V23         | Live Google consent/redirect URI configuration remains external; callback redirects can only be smoke-tested against real Google once env is configured                              | Remove `MailboxGmailOAuthService`, callback controller, controller endpoints, and mailbox status overlay; no production DB mutation occurred                                        |
| 2026-05-11 | Deliver Gmail inbound first as authenticated manual sync before Pub/Sub/worker polling                                           | Build Pub/Sub and polling worker immediately; wait for live Google project before coding inbound; use provider-owned campaign webhooks                | EMAIL-3 can prove mailbox-to-Omnichannel normalization and idempotency locally without requiring Google Pub/Sub setup or a real mailbox, while leaving the live near-real-time worker as the next slice                                                                                                                  | V12, V13, V23         | Manual sync is not the final latency target; production still needs scheduled/push sync and live Gmail smoke                                                                         | Remove `syncLatestInbox()`, the sync endpoint, and related tests; no production provider mutation occurred                                                                          |
| 2026-05-11 | Implement Gmail outbound through Gmail API before generic Email dispatcher integration                                           | Keep using Kloel sender for tests; wait for full CIA outbound dispatcher; send via SMTP abstraction only                                              | V12 requires messages to leave the customer's mailbox; the smallest proof is Gmail `users/me/messages/send` using the connected mailbox token and unsubscribe headers                                                                                                                                                    | V12, V15, V23         | Current endpoint is a test/send primitive, not yet wired into CIA outbound action routing                                                                                            | Remove `sendMessageFromMailbox()`, send-test endpoint, and tests; no production email was sent by this execution                                                                    |
| 2026-05-11 | Route Unified Agent `send_message` by channel instead of letting Email use WhatsApp tooling                                      | Keep `executeTools:false` for Email forever; create a parallel Email agent; make all send_message tools call Gmail                                    | The same CIA should own channel decisions, but outbound must use the channel protocol. A channel-aware branch in the existing messaging action service preserves WhatsApp behavior and enables Gmail mailbox sends when context says `channel=email`                                                                     | V04, V12, V13, V23    | Email tool execution is still disabled for Omnichannel inbound until live safety is proven; proactive/bulk approval policy remains pending                                           | Remove the email branch in `UnifiedAgentActionsMessagingService.actionSendMessage()` and the reactive deliveryMode context in Omnichannel                                           |
| 2026-05-11 | Credit checkout-paid wallet effects from the Prisma paid-update hook with app-level idempotency                                  | Inject `WalletService` into checkout payment services; add a DB unique constraint before code; leave wallet reconciliation manual                     | The existing post-payment effects hook already centralizes `CheckoutOrder` PAID side effects. Adding a small helper there closes the code-side Golden Path without broad DI churn or an unreviewed production migration. Idempotency is enforced by `checkout:<order.id>` reference plus optimistic wallet update tests. | V19, V23              | DB-level uniqueness for wallet transaction references is still a hardening gap; live sandbox payment smoke is still external-blocked                                                 | Remove `backend/src/prisma/checkout-paid-effects/wallet.ts`, its export, the PrismaService call/method, and focused tests; no production DB mutation was executed                   |
| 2026-05-11 | Extend the existing dashboard-summary chat tools instead of creating a separate sales-summary tool                               | Create a new `get_sales_summary` tool; rely on reports UI only; answer from LLM context without a tool                                                | The Golden Path question "como foi o dia" already routes naturally through `get_dashboard_summary`. Adding paid checkout and wallet fields there gives the same CIA a real operational view without expanding the tool surface prematurely.                                                                              | V04, V17, V19, V23    | Two tool services currently implement dashboard summary, so both had to be patched to avoid divergent answers                                                                        | Remove the paid checkout/wallet fields from both summary methods and revert the focused chat-tools spec                                                                             |
| 2026-05-11 | Implement Microsoft mailbox OAuth as a provider-specific service mirroring Gmail instead of genericizing both providers now      | Refactor Gmail into an abstract OAuth mailbox base first; wait for live Azure credentials; implement Graph inbound/outbound before any OAuth endpoint | EMAIL-7 needed code-side Microsoft connection parity without destabilizing the already validated Gmail path. A provider-specific service reuses the same `MailboxConnection` schema and token crypto while keeping Graph API differences isolated for later inbound/outbound slices.                                     | V12, V13, V23         | Some auth/state logic is duplicated with Gmail and should be extracted only after Microsoft live smoke proves shape stability                                                        | Remove Microsoft service, callback controller, controller endpoints/status overlay, module registration, and focused tests; no provider call or production DB mutation was executed |
| 2026-05-11 | Implement IMAP+SMTP validation with Node built-ins instead of adding a mail client dependency                                    | Install `imapflow`/`nodemailer`; store credentials without validation; defer IMAP+SMTP until live account exists                                      | `backend/package.json` is protected governance, so adding dependencies is not allowed without human approval. A small built-in `net`/`tls` validator gives immediate connection proof and encrypted credential storage while keeping live polling/send work for the next governed slice.                                 | V12, V13, V23         | Built-in protocol validation is minimal and does not implement STARTTLS upgrade or production polling; live provider differences may require a dedicated library later with approval | Remove `MailboxImapSmtpService`, endpoint/status overlay changes, module registration, and focused tests; no real credentials were used                                             |
| 2026-05-11 | Use existing Contact opt-out state as the first mailbox suppression source                                                       | Create a new EmailSuppression model; only append unsubscribe footer; rely on provider bounce webhooks later                                           | The repo already has LGPD unsubscribe flow setting `Contact.optIn=false`. EMAIL-9 can reduce immediate risk by preventing proactive Gmail sends to opted-out contacts without adding a migration or duplicate suppression table.                                                                                         | V12, V15, V23         | Bounce/complaint suppression and non-Gmail mailbox sends still need additional policy wiring                                                                                         | Remove `isSuppressedRecipient()` and the proactive suppression branch/test in `MailboxGmailOAuthService`                                                                            |
| 2026-05-11 | Queue high-risk campaign creation from the owner chat as `ApprovalRequest` instead of executing immediately                      | Execute `create_campaign` directly; block the tool entirely; build a full approval UI before any backend guard                                        | Campaign creation can lead to mass outbound messaging, so the backend dispatcher must fail closed before UI completion. Reusing `ApprovalRequest` gives durable review state without a new schema.                                                                                                                       | V20, V23              | Approval execution/adjust UI is not implemented in this slice; only the unsafe direct execution path is blocked                                                                      | Revert `requestHighRiskApproval()` and the `create_campaign` switch branch, then remove focused dispatcher tests                                                                    |
