# Round 050 Invalidated

Status: invalidated, not benchmark evidence.

Reason: this round was created/launched by a stale external `codex exec`
launcher, not by the official OpenCode A/B loop. It did not use the controlled
`opencode-*` prompt/event artifacts.

Benchmark policy: no result from this round can be used to claim normal or
atomic superiority.
