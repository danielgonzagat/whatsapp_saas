{
  "file": "backend/src/kloel/unified-agent-tool-call-processing.helpers.ts",
  "newText": "import { StructuredLogger } from '../logging/structured-logger';\nimport { forEachSequential } from '../common/async-sequence';\nimport { isAllowedTool, parseToolArgs } from './unified-agent-tool-parser.helpers';\nimport { executeToolAction, type ExecuteToolActionDeps } from './unified-agent-tool-router.helpers';\nimport type { ActionEntry } from './unified-agent.types';\nimport type { UnifiedAgentActionsService } from './unified-agent-actions.service';\n\ntype UnknownRecord = Record<string, unknown>;\n\ninterface ToolCallLike {\n  type: string;\n  function: {\n    name: string;\n    arguments?: string;\n  };\n}\n\nexport async function processUnifiedAgentToolCalls(\n  deps: {\n    logger: StructuredLogger;\n    actions: UnifiedAgentActionsService;\n    toolRouterDeps: Omit<ExecuteToolActionDeps, 'logger' | 'actions'>;\n  },\n  params: {\n    workspaceId: string;\n    contactId: string;\n    phone: string;\n    allowedTools?: string[];\n    executeTools: boolean;\n    context?: UnknownRecord;\n    toolCalls: ToolCallLike[];\n  },\n): Promise<ActionEntry[]> {\n  const actionsList: ActionEntry[] = [];\n\n  if (!params.executeTools) {\n    return actionsList;\n  }\n\n  await forEachSequential(params.toolCalls, async (toolCall) => {\n    if (toolCall.type !== 'function') {\n      return;\n    }\n    const toolName = toolCall.function.name;\n    if (!isAllowedTool(toolName, params.allowedTools)) {\n      deps.logger.warn(\n        `Blocked disallowed agent tool call: workspaceId=${params.workspaceId} tool=${toolName}`,\n      );\n      const blockedResult = { blocked: true, reason: 'capability_not_allowed' };\n      actionsList.push({ tool: toolName, args: {}, result: blockedResult });\n      await deps.actions.logAutopilotEvent(\n        params.workspaceId,\n        params.contactId,\n        toolName,\n        {},\n        blockedResult,\n      );\n      return;\n    }\n    const parseResult = parseToolArgs(toolCall.function.arguments);\n    if (parseResult.error) {\n      deps.logger.warn(`Failed to parse tool args for ${toolName}`);\n    }\n    const toolArgs = parseResult.args;\n    const result = await executeToolAction(\n      { logger: deps.logger, actions: deps.actions, ...deps.toolRouterDeps },\n      params.workspaceId,\n      params.contactId,\n      params.phone,\n      toolName,\n      toolArgs,\n      params.context,\n    );\n    actionsList.push({ tool: toolName, args: toolArgs, result });\n    await deps.actions.logAutopilotEvent(\n      params.workspaceId,\n      params.contactId,\n      toolName,\n      toolArgs,\n      result,\n    );\n  });\n\n  return actionsList;\n}\n"
}
