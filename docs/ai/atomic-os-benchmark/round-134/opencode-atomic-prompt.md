You are the ATOMIC OpenCode lane in A/B Round 134.

Worktree: `/Users/danielpenin/kloel-ab-worktrees/kloel-ab134-atomic-20260518121336`

Atomic OS only. No native file read/write/edit tools. Execute the compiled
fast-path first:

```sh
bash "/Users/danielpenin/whatsapp_saas/docs/ai/atomic-os-benchmark/round-134/atomic-fastpath.sh"
```

Then report: preprompt exit, macro validation, trace count, native-file-tool
violations and residual risk. Acceptance: seven-helper split; helpers no
`this.`; public facade methods preserved, including processMessage; service delegates full processMessage orchestration into the incoming helper,
predecided and tool-call processing live outside the service; no suppressions; no protected/governance
edits.
