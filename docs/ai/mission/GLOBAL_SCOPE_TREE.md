# Global Scope Tree

Estados permitidos: nao_iniciado / em_progresso / validado / producao / monitorado.

| Area | Estado | Evidencia atual | Proximo delta |
| --- | --- | --- | --- |
| Anatomia do workspace | em_progresso | Obsidian mirror inclui `.pulse/**`; drift delta de `AGENTS.md` reconciliado; `--validate` verde com 5.592 OK em 2026-05-16 12:43. | Auditar clusters restantes sem trocar a patologia flagship enquanto houver PULSE/grafo pendente. |
| Saude PULSE | em_progresso | Runtime probes health 4/4 reexecutado em 2026-05-16 12:10; production-final com trace explicito concluiu `parity-and-vision` em budget 600s, mas foi encerrado em `scan:perfectness`; `execution-harness` isolado levou 130.482ms; dois handoffs OpenCode confirmam `NOT_READY`/`NOT_CERTIFIED`. | Reexecutar production-final com trace explicito e budget 15-20min ou instrumentar/splitar `scan:perfectness` por modulo. |
| Performance | nao_iniciado | Sem medicao global recente neste ledger. | Definir baseline medido. |
| Conectividade funcional | em_progresso | PULSE agora visivel no grafo; GitNexus reindexado fresco em HEAD `565b0f84...`; DeepSeek V4 Pro validado no GitNexus por direct smoke e wiki smoke. | Usar GitNexus+DeepSeek como fonte cognitiva para validacoes read-only e provar uma integracao de produto nomeada. |
| Inteligencia comercial | nao_iniciado | Sem mercado de entrada N4+ neste ledger. | Declarar mercado flagship apos estabilidade anatomica. |
| Delegacao | em_progresso | Micro-ondas OpenCode rodaram com handoffs aceitos; escala local 20-50 segue bloqueada por RAM/swap e lease topology; OpenCode atomic-only foi validado; o loop A/B converte derrotas em ferramentas. Rounds 063/064 fecharam o tier de extracao simples; round 068 fechou extracao dupla; round 077 fechou extracao de metodos de classe simples; round 080 fechou dependencia de instancia; round 082 fechou extracao mista single-target; round 084 fechou extracao multi-modulo; round 087 fechou router bounded; round 088 fechou router cluster; round 093 validou vitoria operacional forte; round 094 rejeitou ATOMIC por falha funcional de escaped multiline replacement; round 095 rejeitou ambos; round 096 validou ATOMIC funcional contra NORMAL idle/no-op; round 097 foi rejeitado por perda dos worktrees antes da validacao externa; round 098 confirmou ATOMIC funcional contra NORMAL idle/no-task; round 099 confirmou ATOMIC no tier router + runtime-context cluster; round 100 confirmou vitoria operacional do ATOMIC, mas nao zero-loss; rounds 101/102 foram rejeitados por regressoes de ferramenta/politica; round 103 aceitou ATOMIC funcional/operacional mas ainda nao zero-loss; round 104 rejeitou getter `routerDeps`; round 105 rejeitou ambos; round 106 aceitou vitoria forte do ATOMIC apos parser dependency-aware; round 107 confirmou estabilidade local; round 108 rejeitou a primeira escala multi-modulo por import inutil no runtime helper; rounds 109/110 confirmaram vitoria forte do ATOMIC na mesma complexidade; round 111 aceitou a primeira vitoria forte no tier tres helpers; round 112 repetiu a vitoria forte no mesmo tier; rounds 113/114 confirmaram ATOMIC funcional no tier quatro helpers, mas NORMAL ficou incompleto; round 115 obteve baseline NORMAL completo e ATOMIC venceu operacao, mas perdeu shape agregado; round 116 validou o budget gate e rejeitou ATOMIC por `823/817` lines e `732/730` churn; round 117 corrigiu shape atomico para `809/817` e `718/730`, mas NORMAL ficou incompleto; round 118 obteve baseline NORMAL completo e ATOMIC venceu zero-loss; round 119 escalou para cinco helpers e ATOMIC venceu operacao/trace, mas perdeu input tokens e total Kloel lines; round 120 repetiu e fechou zero-loss no tier cinco helpers; round 121 escalou para seis helpers e ATOMIC venceu forte, mas perdeu input tokens por output de preprompt grande; round 122 repetiu seis helpers com preprompt compacto e ATOMIC venceu zero-loss, incluindo input tokens; round 123 escalou para sete helpers e ATOMIC venceu todas as metricas comparativas materiais, mas nao fechou preprompt limpo por budget absoluto fixo; round 124 validou a politica advisory com ATOMIC limpo, mas NORMAL ficou em timeout/typecheck tocado; round 125 obteve baseline NORMAL completo e rejeitou ATOMIC por residuo `toolRouterDeps`; round 126 obteve baseline NORMAL completo e rejeitou ATOMIC por validacao intermediaria final demais; round 127 obteve baseline NORMAL completo e rejeitou ATOMIC por estado cacheado `toolRouterDeps` no service; round 128 obteve baseline NORMAL funcional e rejeitou ATOMIC por replacement ambiguo `expectedCount: 2` que deixou estado parcial. | Round 129 deve repetir a mesma complexidade com `expectedCount` expandido em ocorrencias atomicas sequenciais; nao escalar ate ATOMIC passar o contrato funcional contra baseline completo. |
| Delegacao / Atomic OS A-B | em_progresso | Round 133 segue como ultima vitoria funcional aceita: ATOMIC passou final validation, `atomicModeClean=true` e venceu tempo/eventos/comandos/tokens/trace. Round 134 repetiu o tier, mas foi rejeitado como prova limpa: NORMAL falhou topologia final e ATOMIC foi bloqueado por backend typecheck vermelho fora dos arquivos do benchmark. | Reconciliar baseline typecheck ou separar gate task-scoped de divida global antes de nova escalada; publicar tooling/ledger/veredito acumulado no PR. |
| Prova real | em_progresso | Health probes contra `api.kloel.com` 4/4; Obsidian mirror N4 anatomico. | Health probe permanece prova estreita; subir provas de produto para N4+ onde depender de usuario/fluxo real. |
| Estabilidade | em_progresso | Mirror validate verde; worktree ainda sujo e branch behind 19; protegidos aparecem sujos mas nao foram tocados nesta reconciliacao. | Separar mudancas minhas das preexistentes e validar gates focados. |
| Seguranca/governanca | em_progresso | Arquivos protegidos detectados como sujos mas nao tocados nesta onda. | Nao editar protegidos; auditar antes de qualquer publish. |
| Release | nao_iniciado | Nao ha condicao global de parada. | Bloqueado ate build/test/PULSE e escopo N4+. |
| Observabilidade pos-release | nao_iniciado | Sem evidencia neste ledger. | Definir sinais e monitores apos estabilizacao. |

## Deltas De Delegacao

- 2026-05-16 20:19: `ORCH-ATOMIC-AB-BENCH-007` adicionou `atomic_rename_property_key` ao repo principal apos A/B OpenCode NORMAL vs ATOMIC. Validacao principal: `node --check` advanced/server/smoke, `node scripts/mcp/atomic-edit/build.mjs`, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `125 passed, 0 failed`, auditor `--strict-current-topology` = `pass=true`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check` limpo. Escala de complexidade permanece bloqueada porque a vitoria atomica ainda nao foi ampla: ATOMIC venceu rastreabilidade/proximidade tecnica, NORMAL venceu cobertura live inicial.
- 2026-05-16 20:45: `ORCH-ATOMIC-AB-BENCH-008` adicionou `atomic_add_await_to_call` ao repo principal apos A/B OpenCode NORMAL vs ATOMIC. Validacao principal: `node --check` advanced/server/smoke, `node scripts/mcp/atomic-edit/build.mjs`, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `135 passed, 0 failed`, auditor `--strict-current-topology` = `pass=true`, `traces=17`, `fallback_rate=0`, `coarse_unjustified=0`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check` limpo. Escala de complexidade permanece bloqueada porque ATOMIC ainda nao venceu em velocidade e a lapidacao revelou necessidade de insercao semanticamente ancorada.
- 2026-05-16 21:06: `ORCH-ATOMIC-AB-BENCH-009` adicionou `atomic_insert_after_anchor` ao repo principal apos A/B OpenCode NORMAL vs ATOMIC. Validacao principal: `node --check` server/smoke, `node scripts/mcp/atomic-edit/build.mjs`, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `145 passed, 0 failed`, auditor `--strict-current-topology` desde 2026-05-17T00:00:00.000Z = `pass=true`, `traces=18`, `fallback_rate=0`, `coarse_unjustified=0`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check` limpo. Escala de complexidade permanece bloqueada porque NORMAL venceu em tempo, escopo e cobertura; proxima rodada deve corrigir enforcement de escopo e template de smoke do ATOMIC.
- 2026-05-16 21:33: `ORCH-ATOMIC-AB-BENCH-010` adicionou `worker-scope-check` ao repo principal apos A/B OpenCode NORMAL vs ATOMIC. Validacao principal: `node --check scripts/mcp/atomic-edit/worker-scope-check.mjs`, `node --check scripts/mcp/atomic-edit/smoke.ts`, `node scripts/mcp/atomic-edit/build.mjs`, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `168 passed, 0 failed`, auditor `--strict-current-topology` desde 2026-05-17T00:00:00.000Z = `pass=true`, `traces=27`, `fallback_rate=0`, `coarse_unjustified=0`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check` limpo. Escala de complexidade permanece bloqueada porque ATOMIC falhou funcionalmente em 11 casos do bloco novo e venceu apenas rastreabilidade.
- 2026-05-16 21:55: `ORCH-ATOMIC-AB-BENCH-011` estabilizou `atomic_create_file` no repo principal apos A/B OpenCode NORMAL vs ATOMIC. Validacao principal: `node --check scripts/mcp/atomic-edit/server.ts`, `node --check scripts/mcp/atomic-edit/smoke.ts`, `node scripts/mcp/atomic-edit/build.mjs`, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `184 passed, 0 failed`, auditor `--strict-current-topology` desde 2026-05-17T00:00:00.000Z = `pass=true`, `traces=41`, `fallback_rate=0`, `coarse_unjustified=0`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check` limpo. Escala de complexidade permanece bloqueada porque ATOMIC venceu em rastreabilidade/topologia, mas NORMAL venceu em cobertura funcional da tarefa e ambos falharam self-termination.
- 2026-05-16 22:15: `ORCH-ATOMIC-AB-BENCH-012` adicionou `atomic_insert_before_anchor` e fez `worker-scope-check` ignorar `.atomic` como artefato de prova. Validacao principal: `node --check` server/smoke/worker-scope-check, `node scripts/mcp/atomic-edit/build.mjs`, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `195 passed, 0 failed`, auditor `--strict-current-topology` desde 2026-05-17T00:00:00.000Z = `pass=true`, `traces=58`, `fallback_rate=0`, `coarse_unjustified=0`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check` limpo, `worker-scope-check` no worktree NORMAL AB12 = `ok=true`. Escala de complexidade permanece bloqueada porque ATOMIC venceu rastreabilidade/topologia, mas NORMAL venceu cobertura funcional e autoencerramento ainda falhou.
- 2026-05-16 22:46: `ORCH-ATOMIC-AB-BENCH-013B` adicionou `atomic_replace_between_anchors` ao repo principal apos A/B OpenCode NORMAL vs ATOMIC. NORMAL foi bloqueado corretamente pelo hook atomic-only (`BLOCKED_BY_ATOMIC_ONLY_HOOK`, zero arquivos alterados). ATOMIC entregou `server.ts` + `smoke.ts`, e a validacao principal retornou `node --check` server/smoke verde, `node scripts/mcp/atomic-edit/build.mjs` OK, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `207 passed, 0 failed`, auditor `--strict-current-topology` desde 2026-05-17T01:16:00.000Z = `pass=true`, `fallback_rate=0`, `coarse_unjustified=0`, `previewHonestyPass=true`, `currentTopologyCoverage=1`, `git diff --check -- scripts/mcp/atomic-edit` limpo. Escala de complexidade permanece bloqueada porque esta rodada provou enforcement e vitoria funcional do ATOMIC, mas nao comparou contra um NORMAL capaz de concluir a mesma tarefa.
- 2026-05-16 23:21: `ORCH-ATOMIC-AB-BENCH-014` adicionou `atomic_replace_text_in_anchor_region` ao repo principal apos A/B OpenCode NORMAL vs ATOMIC. NORMAL venceu a rodada em produtividade, cobertura e validacao de worktree; ATOMIC entregou apenas `server.ts` parcial e foi rejeitado como entrega final. A versao principal e hibrida: algoritmo de match nao sobreposto do ATOMIC + smoke funcional do NORMAL + lapida de mensagens de erro. Validacao principal: `node --check` server/smoke passou, `node scripts/mcp/atomic-edit/build.mjs` passou, `npx tsx scripts/mcp/atomic-edit/smoke.ts` = `226 passed, 0 failed`, auditor `--strict-current-topology` desde 2026-05-17T02:00:00.000Z = `pass=true`, `currentTopologyCoverage=1`, `previewHonestyPass=true`, `git diff --check -- scripts/mcp/atomic-edit/server.ts scripts/mcp/atomic-edit/smoke.ts` limpo. Escala de complexidade permanece bloqueada porque ATOMIC ainda nao venceu NORMAL quando ambos podem competir na mesma tarefa.
- 2026-05-16 23:27: `ORCH-ATOMIC-AB-BENCH-045` repetiu refactor medio multi-arquivo de `backend/src/kloel/unified-agent.service.ts` apos escala de complexidade. Ambos lanes passaram Jest `13/13`, backend typecheck e diff-check. ATOMIC corrigiu wrong-root trace (`trace-isolation-check`: `ok=true`, `worktreeTraceCount=14`, `matchingTraceIds=[]`) e teve facade menor (`197` vs `345` linhas), zero file_change nativo e 14 traces; NORMAL venceu duracao, eventos, comandos, input/output/reasoning tokens e largest helper. Ferramentas de benchmark atualizadas: `round-audit.cjs` parseia `*_exit`, `atomic-call.cjs --help` sai 0, `trace-isolation-check.cjs` adicionado. Escala de complexidade permanece bloqueada ate ATOMIC vencer este tier tambem em economia operacional.
- 2026-05-17 08:31: `ORCH-ATOMIC-AB-BENCH-055` repetiu uma extracao bounded apos corrigir isolamento com `--dir`. Rounds 046-054 foram invalidados por contaminacao/travamento/isolamento quebrado; round 055 foi valido. Ambos lanes passaram Jest `13/13`, backend typecheck e diff-check. NORMAL venceu eventos/comandos/tokens/failed commands/acabamento; ATOMIC venceu traceabilidade e recusa de stale `expectedSha256`. Ferramentas atualizadas: watchdog com `--dir`, `atomic-call.cjs` aceita path relativo worktree-safe, `atomic_add_import` preserva quote style e build copia `worker-scope-check.mjs`. Smoke principal `node scripts/mcp/atomic-edit/smoke.mjs` = `226 passed, 0 failed`. Escala de complexidade segue bloqueada; repetir round 056 na mesma tarefa.
- 2026-05-17 08:56: `ORCH-ATOMIC-AB-BENCH-056` repetiu a mesma extracao bounded. Ambos lanes passaram Jest `13/13`, backend typecheck e diff-check; service final empatou em `712` linhas. NORMAL venceu economia operacional (`31` vs `70` eventos, `5` vs `10` comandos, input `56,279` vs `57,578`, output `2,219` vs `3,606`, reasoning `470` vs `4,573`). ATOMIC venceu traceabilidade (`trace_isolation_exit=0`, `worktree_dot_atomic_traces=1`, `worktree_docs_ai_traces=4`), mas falhou disciplina `atomicModeClean=false` por 5 `read` nativos e 1 shell hash read em codigo. Ferramentas atualizadas: `round-audit.cjs` detecta violacoes native-file/native-shell e `opencode-round-watchdog.cjs` sincroniza a toolchain atomica atual para o worktree ATOMIC antes do launch. Escala de complexidade segue bloqueada; round 057 deve repetir ate ATOMIC obter `atomicModeClean=true` e vencer/empatar economia.
- 2026-05-17 09:17: `ORCH-ATOMIC-AB-BENCH-057` repetiu a extracao bounded e obteve `atomicModeClean=true`. Ambos lanes passaram Jest `13/13`, backend typecheck e diff-check. NORMAL ainda venceu economia principal (`52` vs `55` eventos, input `53,679` vs `58,455`, reasoning `951` vs `7,773`, service `712` vs `713` linhas); ATOMIC venceu traceability, zero native file tools, output tokens e comandos shell. Ferramentas atualizadas: `atomic-call.cjs` normaliza `filePath/specifier/action`; `round-audit.cjs` ignora heredoc JSON como falso shell read. Incidente de ENOSPC resolvido com remocao de 125 worktrees geradas antigas. Escala de complexidade segue bloqueada; round 058 deve repetir a mesma tarefa ate ATOMIC vencer/empatar economia alem da disciplina atomica.
- 2026-05-17 09:34: `ORCH-ATOMIC-AB-BENCH-058` foi rejeitado como prova por `max_timeout` e escopo expandido nos dois lanes, apesar de Jest/typecheck/diff-check verdes. NORMAL tocou 6 arquivos Kloel e churn `628`; ATOMIC tocou 5 arquivos e churn `648`, com `13` traces, mas `atomicModeClean=false` por shell read/pipeline. Ferramentas atualizadas: `round-audit.cjs` mede touched-file/source-churn e detecta shell reads/pipelines em tooling; `atomic-call.cjs` adiciona aliases contextuais e normaliza `atomic_create_file`. Escala de complexidade segue bloqueada; round 059 precisa impor exatamente dois arquivos tocados, zero timeout e zero shell/pipeline no ATOMIC.
- 2026-05-17 09:52: `ORCH-ATOMIC-AB-BENCH-059` repetiu a mesma extracao bounded com escopo estrito. Ambos lanes passaram Jest `13/13`, backend typecheck e diff-check; ambos tocaram exatamente 2 arquivos. ATOMIC obteve `atomicModeClean=true`, venceu eventos (`29` vs `53`), input/output/reasoning tokens, source churn (`26` vs `27`) e trace isolation (`.atomic/traces=3`, `matchingTraceIds=[]`). NORMAL ainda venceu shell commands (`9` vs `13`) e service line count por 1 linha. Ferramenta atualizada: `atomic-call.cjs batch` para reduzir overhead de comandos. Escala de complexidade segue bloqueada; round 060 deve repetir o tier ate ATOMIC vencer/empatar comandos shell e acabamento final preservando as demais vitorias.
- 2026-05-17 10:00: `ORCH-ATOMIC-AB-BENCH-060` foi rejeitado porque ATOMIC fez apenas leitura batch e caiu em idle timeout (`SIGTERM`) sem mutar codigo; NORMAL concluiu e validou. Ferramenta atualizada: `atomic-call.cjs batch` agora parseia outputs JSON em objetos para reduzir carga cognitiva. Escala de complexidade segue bloqueada; round 061 deve repetir o mesmo tier com batch parseado.
- 2026-05-17 10:13: `ORCH-ATOMIC-AB-BENCH-061` validou `extract_symbol_to_file`: ambos lanes verdes, ATOMIC `atomicModeClean=true`, trace isolation verde, menos eventos/input/output/source churn, mas NORMAL ainda venceu shell commands, reasoning e 1 linha de service. Escala de complexidade segue bloqueada; round 062 deve remover preflights do ATOMIC e testar operador alto nivel como primeira acao.
- 2026-05-17 10:20: `ORCH-ATOMIC-AB-BENCH-062` obteve a primeira vitoria operacional quase-total do ATOMIC no tier atual: venceu eventos, shell commands, input/output/reasoning tokens, source churn, trace e disciplina atomica; perdeu apenas service line count por 1 linha. `extract_symbol_to_file` foi lapidado para compactar o gap e probe descartavel confirmou service `712` linhas. Escala de complexidade segue bloqueada ate round 063 confirmar zero derrotas.
- 2026-05-17 10:34: `ORCH-ATOMIC-AB-BENCH-063` confirmou a primeira rodada zero-loss do ATOMIC no tier atual. Ambos lanes passaram Jest `13/13`, backend typecheck, diff-check, protected diff e scan de suppressions. ATOMIC venceu eventos (`14` vs `34`), shell commands (`6` vs `7`), input/output/reasoning tokens e trace; empatou service `712`, touched files `2` e source churn `27`; perdeu nada. `extract_symbol_to_file` foi lapidado com validacao embutida e probe descartavel confirmou `ok=true`, todos os checks verdes e diff `1/26`. Escala de complexidade segue bloqueada ate a margem ficar maior tambem em comandos/input; round 064 repete o mesmo tier com prova embutida.
- 2026-05-17 10:43: `ORCH-ATOMIC-AB-BENCH-064` fechou o tier atual para escalada. ATOMIC repetiu a tarefa com uma unica chamada `extract_symbol_to_file validate:true`, e validacao externa independente confirmou Jest `13/13`, backend typecheck, diff-check, protected diff, scan limpo e trace isolation `ok=true`. ATOMIC venceu eventos (`6` vs `27`), shell commands (`1` vs `5`), input/output/reasoning tokens e trace; empatou service/churn/files; perdeu nada. Proxima rodada deve aumentar um degrau de complexidade.
- 2026-05-17 10:53: `ORCH-ATOMIC-AB-BENCH-065` escalou para extracao dupla (`isAllowedTool` + `formatPromptValue`). Ambos lanes passaram Jest `13/13`, backend typecheck, diff-check, protected diff e scan. ATOMIC venceu eventos (`6` vs `24`), shell commands (`1` vs `5`), input/output/reasoning, source churn (`30` vs `31`) e trace, mas NORMAL venceu service line count por 1 linha (`708` vs `709`). `extract_symbols_to_file` foi lapidado para compactar gap antes de `const`; repetir round 066 sem escalar.
- 2026-05-17 11:05: `ORCH-ATOMIC-AB-BENCH-066` repetiu a extracao dupla mas foi rejeitado como vitoria limpa. Ambos lanes tinham shape correto, porem backend typecheck falhou por Prisma Client compartilhado stale; ATOMIC tambem revelou que `extract_symbols_to_file` precisava ser idempotente sobre sucesso parcial apos timeout durante validacao embutida. Ferramentas atualizadas: retry idempotente e Prisma Client local regenerado.
- 2026-05-17 11:18: `ORCH-ATOMIC-AB-BENCH-067` validou a extracao dupla com gates externos verdes. ATOMIC venceu eventos (`10` vs `44`), comandos (`2` vs `7`), input/output/reasoning e trace, mas perdeu failed commands (`1` vs `0`) por JSON shell-escaped. Ferramentas atualizadas: parser de JSON shell-escaped no `atomic-call.cjs` e failed-command score no auditor.
- 2026-05-17 11:25: `ORCH-ATOMIC-AB-BENCH-068` fechou o tier de extracao dupla com zero derrotas medidas. ATOMIC venceu eventos (`6` vs `42`), comandos (`1` vs `7`), input (`51,002` vs `55,832`), output (`395` vs `2,175`), reasoning (`194` vs `843`) e trace; empatou failed commands `0/0`, service `708`, helper `29`, touched files `2` e source churn `31`. Proxima acao: escalar complexidade no round 069.
- 2026-05-17 11:50: `ORCH-ATOMIC-AB-BENCH-069` rejeitou a primeira escalada para extracao de metodos de classe. NORMAL venceu eventos (`36` vs `79`), comandos (`6` vs `22`), failed commands (`1` vs `3`), input/output/reasoning, service (`725` vs `727`) e acabamento; ATOMIC venceu source churn (`30` vs `32`) e trace. Proxima acao: adicionar operador macro e repetir round 070 sem escalar.
- 2026-05-17 14:10: `ORCH-ATOMIC-AB-BENCH-077` fechou o tier de extracao de metodos de classe. NORMAL atingiu `max_timeout`; ATOMIC completou exit `0`, manteve `atomicModeClean=true`, venceu primeira acao (`6.103ms` vs `20.774ms`), tempo total (`57.247ms` vs `577.539ms`), eventos (`3` vs `100`), comandos (`1` vs `14`), failed commands (`0` vs `1`), input/output/reasoning tokens e trace (`10` vs `0`), empatando apenas touched files/source churn/service/helper. Proxima acao: escalar complexidade controladamente.
- 2026-05-17 14:31: `ORCH-ATOMIC-AB-BENCH-078` rejeitou a escalada para metodos de classe com dependencia de instancia. NORMAL passou o aceite funcional (`13/13` Jest, helper sem `this.`, private methods removidos). ATOMIC venceu eventos (`3` vs `78`), primeira acao, tempo total, comandos, tokens, trace e source churn, mas falhou Jest (`8 failed, 5 passed`) porque o helper manteve `this.agentRuntime`. Proxima acao: atualizar o operador com parametro explicito/body replacements e repetir o mesmo tier.
- 2026-05-17 14:49: `ORCH-ATOMIC-AB-BENCH-079` repetiu a escalada rejeitada com o operador atualizado. Ambos lanes passaram Jest `13/13`, diff-check, protected diff, suppression scan, helper sem `this.` e private-method scan; typecheck global falhou nos dois por ruido Google Ads/Prisma sem erro `src/kloel`. ATOMIC venceu todas as metricas operacionais medidas: eventos `3` vs `98`, primeira acao `6.939ms` vs `22.533ms`, tempo total `56.641ms` vs `386.740ms`, comandos `1` vs `11`, failed commands `0` vs `1`, input/output/reasoning `53.610/105/98` vs `67.401/5.601/2.215`, service/helper/churn `701/40/86` vs `704/49/100`, traces `12` vs `0`; `atomicModeClean=true`. Proxima acao: repetir uma vez no Round 080 antes de escalar.
- 2026-05-17 15:02: `ORCH-ATOMIC-AB-BENCH-080` confirmou o tier de dependencia de instancia com zero perdas medidas. Auditor corrigido: `functionalPass=true`, `taskFunctionalPass=true`, `globalFunctionalPass=false`, `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `92`, primeira acao `6.122ms` vs `21.380ms`, tempo total `58.938ms` vs `380.512ms`, comandos `1` vs `13`, failed commands `0` vs `1`, input/output/reasoning `53.587/168/129` vs `82.302/5.419/3.380`, service/helper/churn `701/40/86` vs `704/49/100`, traces `12` vs `0`; `atomicModeClean=true`. Proxima acao: escalar para extracao mista no Round 081.
- 2026-05-17 15:16: `ORCH-ATOMIC-AB-BENCH-081` escalou para extracao mista de cinco metodos. Ambos lanes passaram aceite focado; auditor `functionalPass=true`, `taskFunctionalPass=true`, `globalFunctionalPass=false`, `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `100`, primeira acao `5.386ms` vs `17.360ms`, tempo total `60.741ms` vs `371.223ms`, comandos `1` vs `13`, failed commands `0` vs `5`, input/output/reasoning `54.405/101/285` vs `82.722/5.798/2.071`, service/churn `690/116` vs `693/134`, traces `19` vs `0`; `atomicModeClean=true`. Proxima acao: repetir o tier no Round 082 antes do salto para router.
- 2026-05-17 15:28: `ORCH-ATOMIC-AB-BENCH-082` confirmou o tier misto single-target. Ambos lanes passaram aceite focado; auditor `functionalPass=true`, `taskFunctionalPass=true`, `globalFunctionalPass=false`, `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `99`, primeira acao `4.909ms` vs `19.520ms`, tempo total `61.403ms` vs `442.439ms`, comandos `1` vs `13`, failed commands `0` vs `1`, input/output/reasoning `54.377/112/296` vs `74.125/5.902/3.282`, service/churn `690/116` vs `692/132`, traces `19` vs `0`; `atomicModeClean=true`. Proxima acao: escalar para multi-modulo no Round 083.
- 2026-05-17 15:49: `ORCH-ATOMIC-AB-BENCH-083` escalou para extracao multi-modulo. Ambos lanes passaram aceite focado; auditor `functionalPass=true`, `taskFunctionalPass=true`, `globalFunctionalPass=false`, `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `188`, primeira acao `5.222ms` vs `22.469ms`, tempo total `68.738ms` vs `857.071ms`, comandos `1` vs `25`, failed commands `0` vs `3`, input/output/reasoning `54.959/185/386` vs `75.502/11.080/9.250`, source churn `118` vs `136`, traces `22` vs `0`; `atomicModeClean=true`. NORMAL venceu apenas service line count por 1 linha (`688` vs `689`). Proxima acao: repetir Round 084 no mesmo tier antes de escalar.
- 2026-05-17 16:07: `ORCH-ATOMIC-AB-BENCH-084` confirmou o tier multi-modulo apos reparo do gap terminal. Ambos lanes passaram aceite focado; auditor `functionalPass=true`, `taskFunctionalPass=true`, `globalFunctionalPass=false`, `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `107`, primeira acao `5.203ms` vs `20.598ms`, tempo total `60.055ms` vs `499.020ms`, comandos `1` vs `13`, input/output/reasoning `55.031/106/243` vs `85.304/6.181/4.888`, service `688` vs `692`, source churn `119` vs `132`, traces `22` vs `0`; empatou failed commands `0/0` e touched files `3/3`; `atomicModeClean=true`. Proxima acao: escalar um degrau bounded no Round 085.
- 2026-05-17 16:35: `ORCH-ATOMIC-AB-BENCH-085` escalou para router bounded (`executeToolAction`). Ambos lanes passaram Jest focado `13/13` e checks de diff/protected/suppression/helper-this/private-method, com typecheck global ruidoso fora de `src/kloel`. NORMAL falhou scope preservation ao remover tambem `num` e `buildAgentToolEnvelope`; ATOMIC preservou escopo e venceu eventos `3` vs `136`, primeira acao `5.006ms` vs `20.699ms`, tempo total `53.732ms` vs `704.773ms`, comandos `1` vs `16`, failed commands `0` vs `5`, input/output/reasoning `52.895/180/173` vs `81.616/9.885/6.869`, total Kloel lines `792` vs `801`, source churn `445` vs `492`, traces `7` vs `0`; `atomicModeClean=true`. Proxima acao: repetir a mesma dificuldade no Round 086 com gate explicito de preservacao de escopo.
- 2026-05-17 17:05: `ORCH-ATOMIC-AB-BENCH-086` repetiu router bounded com baseline equivalente. Ambos lanes passaram aceite focado e scope preservation; `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `112`, primeira acao `5.221ms` vs `31.586ms`, tempo total `65.755ms` vs `748.290ms`, comandos `1` vs `13`, input/output/reasoning `53.003/126/455` vs `68.965/9.492/7.449`, total Kloel lines `792` vs `847`, source churn `445` vs `498`, traces `7` vs `0`; `atomicModeClean=true`. NORMAL venceu apenas `serviceLines` (`565` vs `584`). Ferramentas atualizadas: `requiredTextChecks` e `postRemovalReplacements`; probe descartavel reduziu shape atomico para `570` service lines / `791` total lines. Proxima acao: Round 087 na mesma dificuldade com callsite compacto para zerar a ultima perda.
- 2026-05-17 17:16: `ORCH-ATOMIC-AB-BENCH-087` fechou o tier router bounded com ATOMIC zero-loss. Ambos lanes passaram aceite focado e scope preservation; `sharedTypecheckNoiseOnly=true`. ATOMIC venceu service lines `562` vs `585`, total Kloel lines `783` vs `796`, source churn `432` vs `453`, eventos `3` vs `114`, primeira acao `7.438ms` vs `24.601ms`, tempo total `65.986ms` vs `811.633ms`, comandos `1` vs `14`, failed commands `0` vs `5`, input/output/reasoning `53.093/116/175` vs `72.417/10.141/11.206`, traces `8` vs `0`; `atomicModeClean=true`. Proxima acao: escalar um degrau controlado no Round 088.
- 2026-05-17 17:35: `ORCH-ATOMIC-AB-BENCH-088` fechou o tier router cluster com ATOMIC zero-loss. Ambos lanes passaram aceite focado, router export/absence scan e residual-scope scan; `sharedTypecheckNoiseOnly=true`. ATOMIC venceu service lines `544` vs `568`, helper `232` vs `234`, total Kloel lines `776` vs `802`, source churn `459` vs `497`, eventos `3` vs `112`, primeira acao `6.217ms` vs `23.804ms`, tempo total `73.333ms` vs `652.667ms`, comandos `1` vs `12`, failed commands `0` vs `1`, input/output/reasoning `55.827/201/522` vs `73.895/11.225/5.874`, traces `15` vs `0`; `atomicModeClean=true`. Proxima acao: escalar um degrau controlado no Round 089.
- 2026-05-17 17:53: `ORCH-ATOMIC-AB-BENCH-089` venceu funcionalmente o tier router+classifier, mas nao fechou zero-loss. NORMAL atingiu `max_timeout`; ATOMIC completou exit `0`; ambos passaram aceite externo focado e `sharedTypecheckNoiseOnly=true`. ATOMIC venceu eventos `3` vs `136`, primeira acao `5.478ms` vs `19.864ms`, tempo total `70.511ms` vs `885.733ms`, comandos `1` vs `19`, failed commands `0` vs `5`, tokens, helper/total/churn e traces `18` vs `0`; NORMAL venceu lint extra por menos erros (`5` vs `15`). Ferramenta atualizada: `atomic-call.cjs` ganhou `formatWithEslint` via `atomic_apply_eslint_dry_run_fixes`. Proxima acao: repetir a mesma dificuldade no Round 090.
- 2026-05-17 19:13: `ORCH-ATOMIC-AB-BENCH-093` repetiu a dificuldade router cluster + `actionSucceeded`. Ambos lanes passaram aceite focado externo; NORMAL ficou funcional mas atingiu `max_timeout`; ATOMIC completou, `atomicModeClean=true`, venceu eventos `3` vs `128`, primeira acao `5.309ms` vs `27.596ms`, tempo total `157.529ms` vs `900.843ms`, comandos `1` vs `14`, failed commands `0` vs `5`, input/output/reasoning `59.624/77/25` vs `83.286/10.371/13.311` e traces `22` vs `0`; NORMAL ainda venceu `serviceLines` `536` vs `548` e `sourceChurn` `487` vs `494`. Proxima acao: repetir Round 094 sem escalar, compactando shape final.
- 2026-05-17 19:51: `ORCH-ATOMIC-AB-BENCH-094` rejeitou a vitoria atomica. NORMAL passou aceite focado com `typecheckKloelErrors=0`; ATOMIC venceu eventos/comandos/tokens/traces e recusou uma escrita sintaticamente invalida, mas falhou Jest `12/13`, lint, `typecheckKloelErrors=4` e deixou estado parcial. Ferramentas atualizadas: decode opt-in de replacement text, `atomic_add_import typeOnly` e auditor task-functional por lane. Reconciliacao: `round-115-codex-*`/`round-116-codex-*` nao governam este loop OpenCode. Proxima acao: Round 095 na mesma dificuldade, sem escalar.
- 2026-05-18 07:26: `ORCH-ATOMIC-AB-BENCH-118` fechou o tier quatro helpers com vitoria comparavel zero-loss do ATOMIC. Ambos lanes completaram e passaram focused Jest `13/13`, focused ESLint `0`, touched Kloel typecheck errors `0`, diff/protected/suppression/helper/private/public scans verdes. ATOMIC venceu eventos `3` vs `154`, primeira acao `5.054s` vs `17.856s`, agent time `202.582s` vs `1,019.334s`, comandos `1` vs `9`, failed commands `0` vs `3`, input/output/reasoning `75.220/106/245` vs `98.317/15.017/11.616`, service lines `456` vs `468`, total Kloel lines `809` vs `825`, source churn `718` vs `746`, traces `46` vs `0`; `atomicModeClean=true`. Proxima acao: escalar um degrau controlado no Round 119.

## Condicao Para 100%

Nao atendida. Para fechar, todas as areas acima precisam estar em `producao` ou `monitorado`, com evidencia N4+ por area, ledger reconciliado com realidade, zero regressao em build/test/PULSE e zero foco ativo inconcluso.
