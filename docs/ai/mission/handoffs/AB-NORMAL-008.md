# AB-NORMAL-008 Handoff

- Status: accepted_as_input
- Modo: OpenCode NORMAL / DeepSeek V4 Pro / atomic-edit proibido.
- Workspace: `/tmp/kloel-opencode-ab8-20260516-2022-normal`
- Objetivo recebido: implementar `atomic_add_await_to_call`, operador MCP para envolver uma chamada com `await` preservando callee, argumentos e texto da chamada.
- Arquivos lidos: `AGENTS.md`, `scripts/decomp/opencode-subagent-delegation-rules.md`, `scripts/mcp/atomic-edit/advanced.ts`, `server.ts`, `smoke.ts`.
- Arquivos alterados: `scripts/mcp/atomic-edit/advanced.ts`, `server.ts`, `smoke.ts`.
- Hipotese inicial: helper semantico em `advanced.ts` + tool MCP em `server.ts` + smoke live seriam suficientes para provar a topologia de wrapper preservando expressao.
- Decisao tomada: aceitar como insumo valido e benchmark de velocidade; nao aceitar como patch final puro porque nao gera trace MCP e tem prova de preservacao mais fraca.
- Testes executados pelo orquestrador:
  - `node --check scripts/mcp/atomic-edit/advanced.ts`
  - `node --check scripts/mcp/atomic-edit/server.ts`
  - `node --check scripts/mcp/atomic-edit/smoke.ts`
  - `node scripts/mcp/atomic-edit/build.mjs`
  - `npx tsx scripts/mcp/atomic-edit/smoke.ts` -> 131 passed, 0 failed.
  - `node scripts/mcp/atomic-edit/audit-atomicity.mjs --since=2026-05-16T23:22:00.000Z --strict-current-topology --json` -> pass true.
  - `git diff --check -- scripts/mcp/atomic-edit`
- Evidencia antes/depois: antes nao havia ferramenta `atomic_add_await_to_call`; depois o worktree NORMAL tinha tool registrada e smoke verde.
- Benchmark:
  - Venceu ATOMIC em tempo de conclusao (~6m47s).
  - Perdeu em rastreabilidade de mutacao e cobertura semantica fina.
- Risco residual: sem traces MCP, a prova visual/comportamental depende de diff e smoke, nao de mapa de preservacao.
- Recomendacao para proximo worker: preservar velocidade, mas testar tambem contextos semanticamente invalidos que sintaxe isolada nao recusa.
