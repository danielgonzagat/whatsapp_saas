# AB-ATOMIC-008 Handoff

- Status: accepted_as_input_with_lapida_needed
- Modo: OpenCode ATOMIC / DeepSeek V4 Pro / somente MCP atomic-edit para mutacao de codigo.
- Workspace: `/tmp/kloel-opencode-ab8-20260516-2022-atomic`
- Objetivo recebido: implementar a mesma ferramenta `atomic_add_await_to_call` usando apenas o modo atomico.
- Arquivos lidos: `AGENTS.md`, `scripts/decomp/opencode-subagent-delegation-rules.md`, config OpenCode atomic-only, `scripts/mcp/atomic-edit/advanced.ts`, `server.ts`, `smoke.ts`.
- Arquivos alterados: `scripts/mcp/atomic-edit/advanced.ts`, `server.ts`, `smoke.ts`, via MCP atomic-edit.
- Hipotese inicial: `wrap_expression_keep_call` deveria preservar o texto da chamada e alterar apenas o wrapper `await`.
- Decisao tomada: aceitar como melhor insumo tecnico e prova do runtime atomic-only; nao aceitar como patch final puro porque perdeu em tempo e nao recusou contexto nao-async.
- Testes executados pelo orquestrador:
  - `node --check scripts/mcp/atomic-edit/advanced.ts`
  - `node --check scripts/mcp/atomic-edit/server.ts`
  - `node --check scripts/mcp/atomic-edit/smoke.ts`
  - `node scripts/mcp/atomic-edit/build.mjs`
  - `npx tsx scripts/mcp/atomic-edit/smoke.ts` -> 134 passed, 0 failed.
  - `node scripts/mcp/atomic-edit/audit-atomicity.mjs --since=2026-05-16T23:22:00.000Z --strict-current-topology --json` -> pass true.
  - `find .atomic/traces -type f` -> 22 traces.
  - `git diff --check -- scripts/mcp/atomic-edit`
- Evidencia antes/depois: antes nao havia ferramenta de wrapper await; depois o worktree ATOMIC tinha helper, tool, smoke live, selector de desambiguacao e detalhe `callText` preservado.
- Benchmark:
  - Venceu NORMAL em rastreabilidade, prova de ferramenta atomica e cobertura semantica.
  - Perdeu NORMAL em velocidade.
- Risco residual: o modo atomico ainda depende demais de insercoes por coordenada quando a intencao real e "inserir depois de um simbolo/bloco conhecido"; esse drift apareceu na lapidacao principal e precisa virar a proxima rodada.
- Recomendacao para proximo worker: criar/usar primitiva de insercao semanticamente ancorada para listas, tool registries e blocos de teste, reduzindo uso de `insert_at` por linha.
