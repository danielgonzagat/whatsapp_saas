global:
  resolve_timeout: 5m

route:
  receiver: "ops-webhook"
  group_wait: 30s
  group_interval: 2m
  repeat_interval: 4h

receivers:
  - name: "ops-webhook"
    webhook_configs:
      - url: "${ALERT_WEBHOOK_URL}"
        send_resolved: true

templates: []
