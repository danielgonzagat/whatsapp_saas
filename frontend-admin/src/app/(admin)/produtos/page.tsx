import { HonestPlaceholder } from '@/components/admin/honest-placeholder';

export default function ProdutosPage() {
  return (
    <HonestPlaceholder
      module="Produtos"
      plannedSubProject="SP-5"
      description="Catálogo completo de todos os produtos da plataforma, com fila de moderação, métricas por produto, e ações admin (aprovar, rejeitar, pausar, desativar)."
    />
  );
}
