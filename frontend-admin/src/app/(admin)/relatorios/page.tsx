import { HonestPlaceholder } from '@/components/admin/honest-placeholder';

export default function RelatoriosPage() {
  return (
    <HonestPlaceholder
      module="Relatórios & Analytics"
      plannedSubProject="SP-10"
      description="Relatórios pré-construídos (vendas, chargebacks, cohort, P&L), custom report builder, funnel de checkout, exports CSV/PDF e agendamento."
    />
  );
}
