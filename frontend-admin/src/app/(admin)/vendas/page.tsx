import { HonestPlaceholder } from '@/components/admin/honest-placeholder';

export default function VendasPage() {
  return (
    <HonestPlaceholder
      module="Vendas"
      plannedSubProject="SP-6"
      description="Volume consolidado de todas as vendas da plataforma, com filtros avançados (por produtor, afiliado, produto, método, gateway, status), drill-down e export."
    />
  );
}
