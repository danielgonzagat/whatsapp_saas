import { HonestPlaceholder } from '@/components/admin/honest-placeholder';

export default function ClientesPage() {
  return (
    <HonestPlaceholder
      module="Clientes"
      plannedSubProject="SP-12"
      description="Configuração individual por cliente: taxas customizadas, planos, limites, health score e comunicação direta."
    />
  );
}
