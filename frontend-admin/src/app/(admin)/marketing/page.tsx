import { HonestPlaceholder } from '@/components/admin/honest-placeholder';

export default function MarketingPage() {
  return (
    <HonestPlaceholder
      module="Marketing"
      plannedSubProject="SP-13"
      description="Panorama global de canais e performance — WhatsApp, Instagram, Facebook, Email, TikTok — com volumes agregados, taxa de conversão e rankings."
    />
  );
}
