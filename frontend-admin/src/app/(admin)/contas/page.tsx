import { HonestPlaceholder } from '@/components/admin/honest-placeholder';

export default function ContasPage() {
  return (
    <HonestPlaceholder
      module="Contas"
      plannedSubProject="SP-4"
      description="Gestão de produtores, afiliados e compradores: fila de KYC, perfil completo, ações admin (suspender, bloquear, desbloquear, congelar saldo), histórico e comunicações."
    />
  );
}
