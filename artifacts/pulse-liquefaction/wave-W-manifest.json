{
  "runId": "pulse-liquefy-W-2026-05-03T20-20-21-015",
  "concurrency": 10,
  "timeoutSec": 0,
  "dir": "/Users/danielpenin/whatsapp_saas-onda0",
  "skipPermissions": true,
  "tasks": [
    {
      "id": "liquefy-__companions__-autopilot-processor-companion",
      "title": "Liquefy scripts/pulse/__companions__/autopilot-processor.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/autopilot-processor.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **5292 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/autopilot-processor.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 583,\n  \"hardcoded_literal_surface_risk\": 1598,\n  \"hardcoded_numeric_surface_risk\": 465,\n  \"hardcoded_predeclared_authority_context_risk\": 18,\n  \"hardcoded_numeric_decision_literal_risk\": 140,\n  \"hardcoded_fixed_literal_decision_risk\": 94,\n  \"hardcoded_boolean_surface_risk\": 309,\n  \"hardcoded_fixed_boolean_decision_risk\": 26,\n  \"hardcoded_gate_profile_threshold_risk\": 8,\n  \"hardcoded_replacement_cheat_risk\": 2051\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/autopilot-processor.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/autopilot-processor.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/autopilot-processor.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/autopilot-processor.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/autopilot-processor.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/autopilot-processor.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/autopilot-processor.companion.ts` (do `git add scripts/pulse/__companions__/autopilot-processor.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/autopilot-processor.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/autopilot-processor.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/autopilot-processor.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-convergence-plan",
      "title": "Liquefy scripts/pulse/convergence-plan.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/convergence-plan.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **2933 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/convergence-plan.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 2933\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/convergence-plan.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/convergence-plan.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/convergence-plan.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/convergence-plan'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/convergence-plan.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/convergence-plan.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/convergence-plan.ts` (do `git add scripts/pulse/convergence-plan.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/convergence-plan.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/convergence-plan.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/convergence-plan.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-property-tester",
      "title": "Liquefy scripts/pulse/property-tester.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/property-tester.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **2219 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/property-tester.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 321,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"hardcoded_boolean_surface_risk\": 33,\n  \"hardcoded_const_declaration_risk\": 1,\n  \"hardcoded_numeric_surface_risk\": 74,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_fixed_literal_decision_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 1782\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/property-tester.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/property-tester.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/property-tester.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/property-tester'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/property-tester.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/property-tester.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/property-tester.ts` (do `git add scripts/pulse/property-tester.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/property-tester.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/property-tester.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/property-tester.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-no-hardcoded-reality-spec",
      "title": "Liquefy scripts/pulse/__tests__/no-hardcoded-reality.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/no-hardcoded-reality.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **2023 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/no-hardcoded-reality.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 95,\n  \"hardcoded_literal_surface_risk\": 664,\n  \"hardcoded_numeric_surface_risk\": 80,\n  \"hardcoded_boolean_surface_risk\": 101,\n  \"hardcoded_predeclared_authority_context_risk\": 34,\n  \"hardcoded_numeric_decision_literal_risk\": 8,\n  \"hardcoded_path_decision_risk\": 4,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_fixed_literal_decision_risk\": 3,\n  \"hardcoded_sql_reality_table_risk\": 1,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"fixed_flow_id_collection\": 1,\n  \"hardcoded_replacement_cheat_risk\": 1029\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/no-hardcoded-reality.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/no-hardcoded-reality.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/no-hardcoded-reality.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/no-hardcoded-reality.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/no-hardcoded-reality.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/no-hardcoded-reality.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/no-hardcoded-reality.spec.ts` (do `git add scripts/pulse/__tests__/no-hardcoded-reality.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/no-hardcoded-reality.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/no-hardcoded-reality.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/no-hardcoded-reality.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-behavior-graph",
      "title": "Liquefy scripts/pulse/behavior-graph.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/behavior-graph.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1838 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/behavior-graph.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_regex_surface_risk\": 66,\n  \"hardcoded_const_declaration_risk\": 230,\n  \"hardcoded_literal_surface_risk\": 262,\n  \"hardcoded_numeric_surface_risk\": 113,\n  \"hardcoded_boolean_surface_risk\": 19,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_fixed_literal_decision_risk\": 29,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"hardcoded_replacement_cheat_risk\": 1112\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/behavior-graph.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/behavior-graph.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/behavior-graph.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/behavior-graph'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/behavior-graph.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/behavior-graph.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/behavior-graph.ts` (do `git add scripts/pulse/behavior-graph.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/behavior-graph.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/behavior-graph.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/behavior-graph.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-chaos-engine",
      "title": "Liquefy scripts/pulse/chaos-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/chaos-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1730 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/chaos-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 166,\n  \"hardcoded_literal_surface_risk\": 288,\n  \"hardcoded_regex_surface_risk\": 35,\n  \"hardcoded_numeric_surface_risk\": 56,\n  \"hardcoded_fixed_literal_decision_risk\": 10,\n  \"hardcoded_boolean_surface_risk\": 2,\n  \"hardcoded_numeric_decision_literal_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 1171\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/chaos-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/chaos-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/chaos-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/chaos-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/chaos-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/chaos-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/chaos-engine.ts` (do `git add scripts/pulse/chaos-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/chaos-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/chaos-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/chaos-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-dod-engine",
      "title": "Liquefy scripts/pulse/dod-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/dod-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1682 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/dod-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 1682\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/dod-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/dod-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/dod-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/dod-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/dod-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/dod-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/dod-engine.ts` (do `git add scripts/pulse/dod-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/dod-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/dod-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/dod-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-certification",
      "title": "Liquefy scripts/pulse/certification.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/certification.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1629 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/certification.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 53,\n  \"hardcoded_literal_surface_risk\": 131,\n  \"hardcoded_numeric_surface_risk\": 48,\n  \"hardcoded_fixed_literal_decision_risk\": 6,\n  \"hardcoded_boolean_surface_risk\": 8,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_gate_profile_threshold_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 1380\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/certification.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/certification.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/certification.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/certification'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/certification.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/certification.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/certification.ts` (do `git add scripts/pulse/certification.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/certification.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/certification.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/certification.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-otel-runtime",
      "title": "Liquefy scripts/pulse/otel-runtime.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/otel-runtime.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1587 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/otel-runtime.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 220,\n  \"hardcoded_literal_surface_risk\": 263,\n  \"hardcoded_numeric_surface_risk\": 104,\n  \"hardcoded_boolean_surface_risk\": 15,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_regex_surface_risk\": 4,\n  \"hardcoded_numeric_decision_literal_risk\": 4,\n  \"hardcoded_replacement_cheat_risk\": 970\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/otel-runtime.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/otel-runtime.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/otel-runtime.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/otel-runtime'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/otel-runtime.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/otel-runtime.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/otel-runtime.ts` (do `git add scripts/pulse/otel-runtime.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/otel-runtime.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/otel-runtime.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/otel-runtime.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-execution-harness-core",
      "title": "Liquefy scripts/pulse/execution-harness-core.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/execution-harness-core.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1515 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/execution-harness-core.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 158,\n  \"hardcoded_literal_surface_risk\": 283,\n  \"hardcoded_regex_surface_risk\": 59,\n  \"hardcoded_numeric_surface_risk\": 84,\n  \"hardcoded_boolean_surface_risk\": 62,\n  \"hardcoded_replacement_cheat_risk\": 869\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/execution-harness-core.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/execution-harness-core.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/execution-harness-core.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/execution-harness-core'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/execution-harness-core.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/execution-harness-core.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/execution-harness-core.ts` (do `git add scripts/pulse/execution-harness-core.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/execution-harness-core.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/execution-harness-core.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/execution-harness-core.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-api-fuzzer",
      "title": "Liquefy scripts/pulse/api-fuzzer.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/api-fuzzer.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1509 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/api-fuzzer.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 154,\n  \"hardcoded_literal_surface_risk\": 218,\n  \"hardcoded_regex_surface_risk\": 45,\n  \"hardcoded_numeric_surface_risk\": 119,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_boolean_surface_risk\": 34,\n  \"hardcoded_numeric_decision_literal_risk\": 26,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_decision_regex_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 900\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/api-fuzzer.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/api-fuzzer.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/api-fuzzer.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/api-fuzzer'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/api-fuzzer.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/api-fuzzer.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/api-fuzzer.ts` (do `git add scripts/pulse/api-fuzzer.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/api-fuzzer.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/api-fuzzer.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/api-fuzzer.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-contract-tester",
      "title": "Liquefy scripts/pulse/contract-tester.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/contract-tester.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1491 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/contract-tester.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 195,\n  \"hardcoded_literal_surface_risk\": 267,\n  \"hardcoded_regex_surface_risk\": 29,\n  \"hardcoded_boolean_surface_risk\": 15,\n  \"hardcoded_numeric_surface_risk\": 50,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_predeclared_authority_context_risk\": 3,\n  \"hardcoded_numeric_decision_literal_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 924\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/contract-tester.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/contract-tester.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/contract-tester.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/contract-tester'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/contract-tester.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/contract-tester.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/contract-tester.ts` (do `git add scripts/pulse/contract-tester.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/contract-tester.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/contract-tester.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/contract-tester.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-observability-coverage",
      "title": "Liquefy scripts/pulse/observability-coverage.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/observability-coverage.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1473 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/observability-coverage.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 153,\n  \"hardcoded_literal_surface_risk\": 238,\n  \"hardcoded_regex_surface_risk\": 29,\n  \"hardcoded_numeric_surface_risk\": 13,\n  \"hardcoded_boolean_surface_risk\": 5,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_fixed_literal_decision_risk\": 14,\n  \"hardcoded_replacement_cheat_risk\": 1018\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/observability-coverage.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/observability-coverage.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/observability-coverage.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/observability-coverage'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/observability-coverage.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/observability-coverage.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/observability-coverage.ts` (do `git add scripts/pulse/observability-coverage.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/observability-coverage.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/observability-coverage.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/observability-coverage.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-runtime-fusion",
      "title": "Liquefy scripts/pulse/runtime-fusion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/runtime-fusion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1446 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/runtime-fusion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 257,\n  \"hardcoded_numeric_surface_risk\": 157,\n  \"hardcoded_regex_surface_risk\": 7,\n  \"hardcoded_numeric_decision_literal_risk\": 1,\n  \"hardcoded_boolean_surface_risk\": 12,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 1010\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/runtime-fusion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/runtime-fusion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/runtime-fusion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/runtime-fusion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/runtime-fusion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/runtime-fusion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/runtime-fusion.ts` (do `git add scripts/pulse/runtime-fusion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/runtime-fusion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/runtime-fusion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/runtime-fusion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-execution-matrix",
      "title": "Liquefy scripts/pulse/execution-matrix.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/execution-matrix.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1284 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/execution-matrix.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 230,\n  \"hardcoded_predeclared_authority_context_risk\": 7,\n  \"hardcoded_numeric_surface_risk\": 74,\n  \"hardcoded_const_declaration_risk\": 72,\n  \"hardcoded_boolean_surface_risk\": 13,\n  \"hardcoded_replacement_cheat_risk\": 888\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/execution-matrix.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/execution-matrix.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/execution-matrix.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/execution-matrix'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/execution-matrix.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/execution-matrix.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/execution-matrix.ts` (do `git add scripts/pulse/execution-matrix.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/execution-matrix.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/execution-matrix.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/execution-matrix.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-perfectness-test",
      "title": "Liquefy scripts/pulse/perfectness-test.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/perfectness-test.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1279 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/perfectness-test.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 102,\n  \"hardcoded_literal_surface_risk\": 213,\n  \"hardcoded_numeric_surface_risk\": 80,\n  \"hardcoded_numeric_decision_literal_risk\": 11,\n  \"hardcoded_boolean_surface_risk\": 13,\n  \"hardcoded_predeclared_authority_context_risk\": 6,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"hardcoded_gate_profile_threshold_risk\": 9,\n  \"hardcoded_fixed_literal_decision_risk\": 6,\n  \"hardcoded_replacement_cheat_risk\": 837\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/perfectness-test.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/perfectness-test.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/perfectness-test.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/perfectness-test'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/perfectness-test.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/perfectness-test.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/perfectness-test.ts` (do `git add scripts/pulse/perfectness-test.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/perfectness-test.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/perfectness-test.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/perfectness-test.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-dataflow-engine",
      "title": "Liquefy scripts/pulse/dataflow-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/dataflow-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1247 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/dataflow-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 174,\n  \"hardcoded_regex_surface_risk\": 24,\n  \"hardcoded_literal_surface_risk\": 134,\n  \"hardcoded_numeric_surface_risk\": 89,\n  \"hardcoded_boolean_surface_risk\": 17,\n  \"hardcoded_fixed_literal_decision_risk\": 5,\n  \"hardcoded_replacement_cheat_risk\": 804\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/dataflow-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/dataflow-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/dataflow-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/dataflow-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/dataflow-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/dataflow-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/dataflow-engine.ts` (do `git add scripts/pulse/dataflow-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/dataflow-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/dataflow-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/dataflow-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-artifacts",
      "title": "Liquefy scripts/pulse/artifacts.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/artifacts.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1214 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/artifacts.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 1214\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/artifacts.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/artifacts.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/artifacts.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/artifacts'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/artifacts.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/artifacts.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/artifacts.ts` (do `git add scripts/pulse/artifacts.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/artifacts.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/artifacts.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/artifacts.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-scenario-engine",
      "title": "Liquefy scripts/pulse/scenario-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/scenario-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1201 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/scenario-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 102,\n  \"hardcoded_numeric_surface_risk\": 59,\n  \"hardcoded_literal_surface_risk\": 219,\n  \"hardcoded_regex_surface_risk\": 7,\n  \"hardcoded_fixed_literal_decision_risk\": 4,\n  \"hardcoded_boolean_surface_risk\": 3,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"hardcoded_replacement_cheat_risk\": 804\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/scenario-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/scenario-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/scenario-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/scenario-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/scenario-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/scenario-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/scenario-engine.ts` (do `git add scripts/pulse/scenario-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/scenario-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/scenario-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/scenario-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-artifacts-directive",
      "title": "Liquefy scripts/pulse/artifacts.directive.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/artifacts.directive.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1199 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/artifacts.directive.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 63,\n  \"hardcoded_literal_surface_risk\": 251,\n  \"hardcoded_fixed_literal_decision_risk\": 11,\n  \"hardcoded_numeric_surface_risk\": 69,\n  \"hardcoded_numeric_decision_literal_risk\": 11,\n  \"hardcoded_boolean_surface_risk\": 13,\n  \"hardcoded_predeclared_authority_context_risk\": 11,\n  \"hardcoded_regex_surface_risk\": 10,\n  \"hardcoded_fixed_boolean_decision_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 758\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/artifacts.directive.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/artifacts.directive.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/artifacts.directive.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/artifacts.directive'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/artifacts.directive.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/artifacts.directive.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/artifacts.directive.ts` (do `git add scripts/pulse/artifacts.directive.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/artifacts.directive.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/artifacts.directive.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/artifacts.directive.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-source-root-detector",
      "title": "Liquefy scripts/pulse/source-root-detector.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/source-root-detector.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1134 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/source-root-detector.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 113,\n  \"hardcoded_literal_surface_risk\": 184,\n  \"hardcoded_regex_surface_risk\": 37,\n  \"hardcoded_numeric_surface_risk\": 19,\n  \"hardcoded_boolean_surface_risk\": 11,\n  \"hardcoded_fixed_literal_decision_risk\": 2,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 767\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/source-root-detector.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/source-root-detector.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/source-root-detector.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/source-root-detector'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/source-root-detector.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/source-root-detector.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/source-root-detector.ts` (do `git add scripts/pulse/source-root-detector.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/source-root-detector.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/source-root-detector.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/source-root-detector.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-execution-matrix-spec",
      "title": "Liquefy scripts/pulse/__tests__/execution-matrix.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/execution-matrix.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1096 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/execution-matrix.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 35,\n  \"hardcoded_literal_surface_risk\": 298,\n  \"hardcoded_predeclared_authority_context_risk\": 54,\n  \"hardcoded_numeric_surface_risk\": 204,\n  \"hardcoded_numeric_decision_literal_risk\": 16,\n  \"hardcoded_boolean_surface_risk\": 84,\n  \"hardcoded_fixed_boolean_decision_risk\": 11,\n  \"fixed_capability_id_collection\": 1,\n  \"hardcoded_fixed_literal_decision_risk\": 1,\n  \"hardcoded_gate_profile_threshold_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 391\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/execution-matrix.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/execution-matrix.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/execution-matrix.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/execution-matrix.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/execution-matrix.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/execution-matrix.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/execution-matrix.spec.ts` (do `git add scripts/pulse/__tests__/execution-matrix.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/execution-matrix.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/execution-matrix.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/execution-matrix.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-scope-engine",
      "title": "Liquefy scripts/pulse/scope-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/scope-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1082 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/scope-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 1082\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/scope-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/scope-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/scope-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/scope-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/scope-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/scope-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/scope-engine.ts` (do `git add scripts/pulse/scope-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/scope-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/scope-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/scope-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-flows-index",
      "title": "Liquefy scripts/pulse/flows/index.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/flows/index.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1070 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/flows/index.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 102,\n  \"hardcoded_literal_surface_risk\": 172,\n  \"hardcoded_regex_surface_risk\": 6,\n  \"hardcoded_fixed_literal_decision_risk\": 14,\n  \"hardcoded_boolean_surface_risk\": 69,\n  \"hardcoded_fixed_boolean_decision_risk\": 5,\n  \"hardcoded_numeric_surface_risk\": 59,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_numeric_decision_literal_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 638\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/flows/index.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/flows/index.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/flows/index.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/flows/index'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/flows/index.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/flows/index.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/flows/index.ts` (do `git add scripts/pulse/flows/index.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/flows/index.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/flows/index.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/flows/index.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-continuous-daemon",
      "title": "Liquefy scripts/pulse/continuous-daemon.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/continuous-daemon.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1065 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/continuous-daemon.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 113,\n  \"hardcoded_const_declaration_risk\": 4,\n  \"hardcoded_regex_surface_risk\": 4,\n  \"hardcoded_numeric_surface_risk\": 25,\n  \"hardcoded_replacement_cheat_risk\": 919\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/continuous-daemon.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/continuous-daemon.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/continuous-daemon.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/continuous-daemon'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/continuous-daemon.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/continuous-daemon.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/continuous-daemon.ts` (do `git add scripts/pulse/continuous-daemon.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/continuous-daemon.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/continuous-daemon.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/continuous-daemon.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-index",
      "title": "Liquefy scripts/pulse/index.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/index.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1039 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/index.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 71,\n  \"hardcoded_literal_surface_risk\": 119,\n  \"hardcoded_predeclared_authority_context_risk\": 3,\n  \"hardcoded_numeric_surface_risk\": 41,\n  \"hardcoded_fixed_literal_decision_risk\": 10,\n  \"hardcoded_boolean_surface_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 793\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/index.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/index.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/index.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/index'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/index.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/index.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/index.ts` (do `git add scripts/pulse/index.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/index.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/index.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/index.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-dynamic-reality-kernel",
      "title": "Liquefy scripts/pulse/dynamic-reality-kernel.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/dynamic-reality-kernel.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **1034 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/dynamic-reality-kernel.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_numeric_surface_risk\": 43,\n  \"hardcoded_const_declaration_risk\": 46,\n  \"hardcoded_literal_surface_risk\": 401,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_fixed_literal_decision_risk\": 17,\n  \"hardcoded_regex_surface_risk\": 73,\n  \"hardcoded_boolean_surface_risk\": 6,\n  \"hardcoded_replacement_cheat_risk\": 446\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/dynamic-reality-kernel.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/dynamic-reality-kernel.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/dynamic-reality-kernel.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/dynamic-reality-kernel'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/dynamic-reality-kernel.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/dynamic-reality-kernel.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/dynamic-reality-kernel.ts` (do `git add scripts/pulse/dynamic-reality-kernel.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/dynamic-reality-kernel.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/dynamic-reality-kernel.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/dynamic-reality-kernel.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-autonomy-loop",
      "title": "Liquefy scripts/pulse/autonomy-loop.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/autonomy-loop.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **981 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/autonomy-loop.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 39,\n  \"hardcoded_literal_surface_risk\": 42,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_numeric_surface_risk\": 13,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_boolean_surface_risk\": 4,\n  \"hardcoded_replacement_cheat_risk\": 875\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/autonomy-loop.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/autonomy-loop.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/autonomy-loop.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/autonomy-loop'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/autonomy-loop.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/autonomy-loop.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/autonomy-loop.ts` (do `git add scripts/pulse/autonomy-loop.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/autonomy-loop.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/autonomy-loop.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/autonomy-loop.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-ui-crawler",
      "title": "Liquefy scripts/pulse/ui-crawler.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/ui-crawler.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **977 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/ui-crawler.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 977\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/ui-crawler.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/ui-crawler.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/ui-crawler.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/ui-crawler'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/ui-crawler.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/ui-crawler.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/ui-crawler.ts` (do `git add scripts/pulse/ui-crawler.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/ui-crawler.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/ui-crawler.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/ui-crawler.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-types",
      "title": "Liquefy scripts/pulse/types.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/types.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **965 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/types.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 965\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/types.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/types.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/types.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/types'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/types.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/types.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/types.ts` (do `git add scripts/pulse/types.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/types.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/types.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/types.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-adapters-external-sources-orchestrator",
      "title": "Liquefy scripts/pulse/adapters/external-sources-orchestrator.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/adapters/external-sources-orchestrator.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **938 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/adapters/external-sources-orchestrator.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 70,\n  \"hardcoded_literal_surface_risk\": 296,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_boolean_surface_risk\": 7,\n  \"hardcoded_regex_surface_risk\": 6,\n  \"hardcoded_numeric_surface_risk\": 40,\n  \"hardcoded_predeclared_authority_context_risk\": 3,\n  \"hardcoded_numeric_decision_literal_risk\": 15,\n  \"hardcoded_replacement_cheat_risk\": 494\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/adapters/external-sources-orchestrator.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/adapters/external-sources-orchestrator.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/adapters/external-sources-orchestrator.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/adapters/external-sources-orchestrator'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/adapters/external-sources-orchestrator.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/adapters/external-sources-orchestrator.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/adapters/external-sources-orchestrator.ts` (do `git add scripts/pulse/adapters/external-sources-orchestrator.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/adapters/external-sources-orchestrator.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/adapters/external-sources-orchestrator.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/adapters/external-sources-orchestrator.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-ast-graph",
      "title": "Liquefy scripts/pulse/ast-graph.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/ast-graph.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **908 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/ast-graph.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 116,\n  \"hardcoded_regex_surface_risk\": 13,\n  \"hardcoded_literal_surface_risk\": 118,\n  \"hardcoded_numeric_surface_risk\": 20,\n  \"hardcoded_fixed_literal_decision_risk\": 5,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_boolean_surface_risk\": 22,\n  \"hardcoded_replacement_cheat_risk\": 612\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/ast-graph.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/ast-graph.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/ast-graph.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/ast-graph'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/ast-graph.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/ast-graph.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/ast-graph.ts` (do `git add scripts/pulse/ast-graph.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/ast-graph.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/ast-graph.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/ast-graph.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-no-hardcoded-reality-spec-companion",
      "title": "Liquefy scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **887 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 605,\n  \"hardcoded_const_declaration_risk\": 90,\n  \"hardcoded_boolean_surface_risk\": 81,\n  \"hardcoded_numeric_surface_risk\": 70,\n  \"hardcoded_predeclared_authority_context_risk\": 28,\n  \"hardcoded_fixed_literal_decision_risk\": 3,\n  \"hardcoded_sql_reality_table_risk\": 1,\n  \"hardcoded_path_decision_risk\": 3,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"fixed_flow_id_collection\": 1\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/no-hardcoded-reality.spec.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts` (do `git add scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/no-hardcoded-reality.spec.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-product-vision",
      "title": "Liquefy scripts/pulse/product-vision.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/product-vision.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **876 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/product-vision.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_boolean_surface_risk\": 5,\n  \"hardcoded_numeric_surface_risk\": 32,\n  \"hardcoded_const_declaration_risk\": 80,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_literal_surface_risk\": 33,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 724\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/product-vision.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/product-vision.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/product-vision.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/product-vision'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/product-vision.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/product-vision.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/product-vision.ts` (do `git add scripts/pulse/product-vision.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/product-vision.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/product-vision.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/product-vision.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-runtime-fusion-spec",
      "title": "Liquefy scripts/pulse/__tests__/runtime-fusion.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/runtime-fusion.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **865 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/runtime-fusion.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 27,\n  \"hardcoded_literal_surface_risk\": 327,\n  \"hardcoded_boolean_surface_risk\": 11,\n  \"hardcoded_numeric_surface_risk\": 89,\n  \"hardcoded_numeric_decision_literal_risk\": 31,\n  \"hardcoded_predeclared_authority_context_risk\": 16,\n  \"fixed_capability_id_collection\": 6,\n  \"fixed_flow_id_collection\": 2,\n  \"hardcoded_path_decision_risk\": 7,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 347\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/runtime-fusion.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/runtime-fusion.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/runtime-fusion.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/runtime-fusion.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/runtime-fusion.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/runtime-fusion.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/runtime-fusion.spec.ts` (do `git add scripts/pulse/__tests__/runtime-fusion.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/runtime-fusion.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/runtime-fusion.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/runtime-fusion.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-parsers-ui-parser",
      "title": "Liquefy scripts/pulse/parsers/ui-parser.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/parsers/ui-parser.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **853 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/parsers/ui-parser.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 83,\n  \"hardcoded_literal_surface_risk\": 173,\n  \"hardcoded_numeric_surface_risk\": 69,\n  \"hardcoded_boolean_surface_risk\": 5,\n  \"hardcoded_replacement_cheat_risk\": 523\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/parsers/ui-parser.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/parsers/ui-parser.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/parsers/ui-parser.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/parsers/ui-parser'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/parsers/ui-parser.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/parsers/ui-parser.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/parsers/ui-parser.ts` (do `git add scripts/pulse/parsers/ui-parser.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/parsers/ui-parser.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/parsers/ui-parser.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/parsers/ui-parser.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-daemon",
      "title": "Liquefy scripts/pulse/daemon.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/daemon.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **823 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/daemon.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 51,\n  \"hardcoded_literal_surface_risk\": 116,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_boolean_surface_risk\": 6,\n  \"hardcoded_numeric_surface_risk\": 11,\n  \"hardcoded_fixed_literal_decision_risk\": 1,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_numeric_decision_literal_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 635\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/daemon.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/daemon.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/daemon.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/daemon'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/daemon.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/daemon.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/daemon.ts` (do `git add scripts/pulse/daemon.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/daemon.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/daemon.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/daemon.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-external-adapters-spec",
      "title": "Liquefy scripts/pulse/__tests__/external-adapters.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/external-adapters.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **816 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/external-adapters.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 38,\n  \"hardcoded_literal_surface_risk\": 253,\n  \"hardcoded_boolean_surface_risk\": 48,\n  \"hardcoded_numeric_surface_risk\": 131,\n  \"hardcoded_predeclared_authority_context_risk\": 34,\n  \"hardcoded_numeric_decision_literal_risk\": 35,\n  \"hardcoded_fixed_literal_decision_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 275\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/external-adapters.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/external-adapters.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/external-adapters.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/external-adapters.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/external-adapters.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/external-adapters.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/external-adapters.spec.ts` (do `git add scripts/pulse/__tests__/external-adapters.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/external-adapters.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/external-adapters.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/external-adapters.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-artifacts-autonomy",
      "title": "Liquefy scripts/pulse/artifacts.autonomy.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/artifacts.autonomy.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **813 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/artifacts.autonomy.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 85,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_literal_surface_risk\": 184,\n  \"hardcoded_numeric_surface_risk\": 51,\n  \"hardcoded_fixed_literal_decision_risk\": 13,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_boolean_surface_risk\": 22,\n  \"hardcoded_replacement_cheat_risk\": 455\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/artifacts.autonomy.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/artifacts.autonomy.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/artifacts.autonomy.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/artifacts.autonomy'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/artifacts.autonomy.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/artifacts.autonomy.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/artifacts.autonomy.ts` (do `git add scripts/pulse/artifacts.autonomy.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/artifacts.autonomy.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/artifacts.autonomy.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/artifacts.autonomy.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-safety-sandbox",
      "title": "Liquefy scripts/pulse/safety-sandbox.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/safety-sandbox.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **806 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/safety-sandbox.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 80,\n  \"hardcoded_literal_surface_risk\": 186,\n  \"hardcoded_regex_surface_risk\": 14,\n  \"hardcoded_boolean_surface_risk\": 22,\n  \"hardcoded_numeric_surface_risk\": 11,\n  \"hardcoded_fixed_literal_decision_risk\": 1,\n  \"hardcoded_predeclared_authority_context_risk\": 3,\n  \"hardcoded_replacement_cheat_risk\": 489\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/safety-sandbox.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/safety-sandbox.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/safety-sandbox.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/safety-sandbox'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/safety-sandbox.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/safety-sandbox.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/safety-sandbox.ts` (do `git add scripts/pulse/safety-sandbox.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/safety-sandbox.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/safety-sandbox.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/safety-sandbox.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-capability-model",
      "title": "Liquefy scripts/pulse/capability-model.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/capability-model.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **779 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/capability-model.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 63,\n  \"hardcoded_numeric_surface_risk\": 22,\n  \"hardcoded_boolean_surface_risk\": 2,\n  \"hardcoded_const_declaration_risk\": 77,\n  \"hardcoded_replacement_cheat_risk\": 615\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/capability-model.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/capability-model.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/capability-model.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/capability-model'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/capability-model.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/capability-model.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/capability-model.ts` (do `git add scripts/pulse/capability-model.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/capability-model.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/capability-model.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/capability-model.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-resolved-manifest",
      "title": "Liquefy scripts/pulse/resolved-manifest.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/resolved-manifest.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **775 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/resolved-manifest.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 43,\n  \"hardcoded_literal_surface_risk\": 43,\n  \"hardcoded_numeric_surface_risk\": 10,\n  \"hardcoded_fixed_literal_decision_risk\": 23,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"hardcoded_boolean_surface_risk\": 4,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_fixed_boolean_decision_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 645\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/resolved-manifest.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/resolved-manifest.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/resolved-manifest.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/resolved-manifest'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/resolved-manifest.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/resolved-manifest.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/resolved-manifest.ts` (do `git add scripts/pulse/resolved-manifest.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/resolved-manifest.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/resolved-manifest.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/resolved-manifest.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-product-model",
      "title": "Liquefy scripts/pulse/product-model.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/product-model.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **755 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/product-model.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 80,\n  \"hardcoded_numeric_surface_risk\": 51,\n  \"hardcoded_numeric_decision_literal_risk\": 10,\n  \"hardcoded_literal_surface_risk\": 85,\n  \"hardcoded_fixed_literal_decision_risk\": 20,\n  \"hardcoded_boolean_surface_risk\": 1,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 505\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/product-model.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/product-model.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/product-model.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/product-model'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/product-model.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/product-model.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/product-model.ts` (do `git add scripts/pulse/product-model.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/product-model.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/product-model.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/product-model.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-dod-engine-companion",
      "title": "Liquefy scripts/pulse/__companions__/dod-engine.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/dod-engine.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **728 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/dod-engine.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 125,\n  \"hardcoded_const_declaration_risk\": 116,\n  \"hardcoded_numeric_surface_risk\": 54,\n  \"hardcoded_regex_surface_risk\": 11,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_boolean_surface_risk\": 12,\n  \"hardcoded_replacement_cheat_risk\": 406\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/dod-engine.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/dod-engine.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/dod-engine.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/dod-engine.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/dod-engine.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/dod-engine.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/dod-engine.companion.ts` (do `git add scripts/pulse/__companions__/dod-engine.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/dod-engine.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/dod-engine.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/dod-engine.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-property-tester-companion",
      "title": "Liquefy scripts/pulse/__companions__/property-tester.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/property-tester.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **712 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/property-tester.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 525,\n  \"hardcoded_numeric_surface_risk\": 114,\n  \"hardcoded_boolean_surface_risk\": 26,\n  \"hardcoded_predeclared_authority_context_risk\": 5,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 40\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/property-tester.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/property-tester.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/property-tester.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/property-tester.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/property-tester.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/property-tester.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/property-tester.companion.ts` (do `git add scripts/pulse/__companions__/property-tester.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/property-tester.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/property-tester.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/property-tester.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-self-trust",
      "title": "Liquefy scripts/pulse/self-trust.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/self-trust.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **702 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/self-trust.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 177,\n  \"hardcoded_boolean_surface_risk\": 51,\n  \"hardcoded_numeric_surface_risk\": 49,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_fixed_boolean_decision_risk\": 22,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_numeric_decision_literal_risk\": 7,\n  \"hardcoded_replacement_cheat_risk\": 386\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/self-trust.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/self-trust.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/self-trust.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/self-trust'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/self-trust.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/self-trust.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/self-trust.ts` (do `git add scripts/pulse/self-trust.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/self-trust.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/self-trust.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/self-trust.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-capability-product-dynamic-spec",
      "title": "Liquefy scripts/pulse/__tests__/capability-product-dynamic.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/capability-product-dynamic.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **674 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/capability-product-dynamic.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 135,\n  \"hardcoded_predeclared_authority_context_risk\": 62,\n  \"hardcoded_numeric_surface_risk\": 110,\n  \"hardcoded_boolean_surface_risk\": 42,\n  \"hardcoded_fixed_boolean_decision_risk\": 8,\n  \"hardcoded_numeric_decision_literal_risk\": 25,\n  \"hardcoded_const_declaration_risk\": 5,\n  \"hardcoded_replacement_cheat_risk\": 287\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/capability-product-dynamic.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/capability-product-dynamic.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/capability-product-dynamic.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/capability-product-dynamic.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/capability-product-dynamic.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/capability-product-dynamic.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/capability-product-dynamic.spec.ts` (do `git add scripts/pulse/__tests__/capability-product-dynamic.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/capability-product-dynamic.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/capability-product-dynamic.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/capability-product-dynamic.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-regression-guard-spec",
      "title": "Liquefy scripts/pulse/__tests__/regression-guard.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/regression-guard.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **673 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/regression-guard.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_predeclared_authority_context_risk\": 6,\n  \"hardcoded_numeric_surface_risk\": 130,\n  \"hardcoded_numeric_decision_literal_risk\": 36,\n  \"hardcoded_gate_profile_threshold_risk\": 14,\n  \"hardcoded_boolean_surface_risk\": 72,\n  \"hardcoded_literal_surface_risk\": 75,\n  \"hardcoded_const_declaration_risk\": 75,\n  \"hardcoded_replacement_cheat_risk\": 265\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/regression-guard.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/regression-guard.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/regression-guard.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/regression-guard.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/regression-guard.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/regression-guard.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/regression-guard.spec.ts` (do `git add scripts/pulse/__tests__/regression-guard.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/regression-guard.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/regression-guard.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/regression-guard.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-structural-memory",
      "title": "Liquefy scripts/pulse/structural-memory.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/structural-memory.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **634 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/structural-memory.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 84,\n  \"hardcoded_literal_surface_risk\": 70,\n  \"hardcoded_numeric_surface_risk\": 63,\n  \"hardcoded_numeric_decision_literal_risk\": 4,\n  \"hardcoded_regex_surface_risk\": 4,\n  \"hardcoded_fixed_literal_decision_risk\": 8,\n  \"hardcoded_boolean_surface_risk\": 11,\n  \"hardcoded_replacement_cheat_risk\": 390\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/structural-memory.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/structural-memory.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/structural-memory.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/structural-memory'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/structural-memory.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/structural-memory.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/structural-memory.ts` (do `git add scripts/pulse/structural-memory.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/structural-memory.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/structural-memory.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/structural-memory.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-manifest",
      "title": "Liquefy scripts/pulse/manifest.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/manifest.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **633 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/manifest.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 35,\n  \"hardcoded_literal_surface_risk\": 248,\n  \"hardcoded_numeric_surface_risk\": 17,\n  \"hardcoded_fixed_literal_decision_risk\": 15,\n  \"hardcoded_boolean_surface_risk\": 1,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_regex_surface_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 314\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/manifest.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/manifest.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/manifest.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/manifest'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/manifest.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/manifest.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/manifest.ts` (do `git add scripts/pulse/manifest.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/manifest.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/manifest.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/manifest.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-command-graph",
      "title": "Liquefy scripts/pulse/command-graph.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/command-graph.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **632 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/command-graph.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 76,\n  \"hardcoded_literal_surface_risk\": 132,\n  \"hardcoded_regex_surface_risk\": 22,\n  \"hardcoded_fixed_literal_decision_risk\": 1,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_numeric_surface_risk\": 39,\n  \"hardcoded_boolean_surface_risk\": 5,\n  \"hardcoded_replacement_cheat_risk\": 356\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/command-graph.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/command-graph.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/command-graph.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/command-graph'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/command-graph.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/command-graph.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/command-graph.ts` (do `git add scripts/pulse/command-graph.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/command-graph.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/command-graph.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/command-graph.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-artifacts-report",
      "title": "Liquefy scripts/pulse/artifacts.report.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/artifacts.report.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **625 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/artifacts.report.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 40,\n  \"hardcoded_numeric_surface_risk\": 80,\n  \"hardcoded_literal_surface_risk\": 165,\n  \"hardcoded_fixed_literal_decision_risk\": 4,\n  \"hardcoded_boolean_surface_risk\": 6,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 322\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/artifacts.report.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/artifacts.report.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/artifacts.report.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/artifacts.report'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/artifacts.report.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/artifacts.report.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/artifacts.report.ts` (do `git add scripts/pulse/artifacts.report.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/artifacts.report.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/artifacts.report.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/artifacts.report.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-runtime-fusion-companion",
      "title": "Liquefy scripts/pulse/__companions__/runtime-fusion.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/runtime-fusion.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **612 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/runtime-fusion.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 149,\n  \"hardcoded_boolean_surface_risk\": 12,\n  \"hardcoded_numeric_surface_risk\": 106,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 342\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/runtime-fusion.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/runtime-fusion.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/runtime-fusion.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/runtime-fusion.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/runtime-fusion.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/runtime-fusion.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/runtime-fusion.companion.ts` (do `git add scripts/pulse/__companions__/runtime-fusion.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/runtime-fusion.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/runtime-fusion.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/runtime-fusion.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-behavior-graph-companion",
      "title": "Liquefy scripts/pulse/__companions__/behavior-graph.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/behavior-graph.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **605 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/behavior-graph.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 171,\n  \"hardcoded_literal_surface_risk\": 235,\n  \"hardcoded_regex_surface_risk\": 57,\n  \"hardcoded_numeric_surface_risk\": 113,\n  \"hardcoded_predeclared_authority_context_risk\": 3,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"hardcoded_boolean_surface_risk\": 17,\n  \"hardcoded_fixed_literal_decision_risk\": 6\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/behavior-graph.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/behavior-graph.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/behavior-graph.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/behavior-graph.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/behavior-graph.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/behavior-graph.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/behavior-graph.companion.ts` (do `git add scripts/pulse/__companions__/behavior-graph.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/behavior-graph.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/behavior-graph.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/behavior-graph.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-path-coverage-engine",
      "title": "Liquefy scripts/pulse/path-coverage-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/path-coverage-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **604 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/path-coverage-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 54,\n  \"hardcoded_literal_surface_risk\": 126,\n  \"hardcoded_fixed_literal_decision_risk\": 16,\n  \"hardcoded_boolean_surface_risk\": 12,\n  \"hardcoded_numeric_surface_risk\": 16,\n  \"hardcoded_fixed_boolean_decision_risk\": 1,\n  \"hardcoded_regex_surface_risk\": 5,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 372\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/path-coverage-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/path-coverage-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/path-coverage-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/path-coverage-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/path-coverage-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/path-coverage-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/path-coverage-engine.ts` (do `git add scripts/pulse/path-coverage-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/path-coverage-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/path-coverage-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/path-coverage-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-authority-engine",
      "title": "Liquefy scripts/pulse/authority-engine.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/authority-engine.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **583 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/authority-engine.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 78,\n  \"hardcoded_literal_surface_risk\": 49,\n  \"hardcoded_numeric_surface_risk\": 29,\n  \"hardcoded_numeric_decision_literal_risk\": 7,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_boolean_surface_risk\": 13,\n  \"hardcoded_replacement_cheat_risk\": 400\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/authority-engine.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/authority-engine.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/authority-engine.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/authority-engine'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/authority-engine.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/authority-engine.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/authority-engine.ts` (do `git add scripts/pulse/authority-engine.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/authority-engine.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/authority-engine.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/authority-engine.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-parsers-api-parser",
      "title": "Liquefy scripts/pulse/parsers/api-parser.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/parsers/api-parser.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **583 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/parsers/api-parser.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 108,\n  \"hardcoded_literal_surface_risk\": 80,\n  \"hardcoded_numeric_surface_risk\": 83,\n  \"hardcoded_regex_surface_risk\": 38,\n  \"hardcoded_fixed_literal_decision_risk\": 12,\n  \"hardcoded_boolean_surface_risk\": 6,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 255\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/parsers/api-parser.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/parsers/api-parser.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/parsers/api-parser.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/parsers/api-parser'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/parsers/api-parser.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/parsers/api-parser.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/parsers/api-parser.ts` (do `git add scripts/pulse/parsers/api-parser.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/parsers/api-parser.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/parsers/api-parser.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/parsers/api-parser.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-parsers-facade-detector",
      "title": "Liquefy scripts/pulse/parsers/facade-detector.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/parsers/facade-detector.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **579 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/parsers/facade-detector.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_numeric_surface_risk\": 44,\n  \"hardcoded_literal_surface_risk\": 277,\n  \"hardcoded_boolean_surface_risk\": 7,\n  \"hardcoded_replacement_cheat_risk\": 251\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/parsers/facade-detector.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/parsers/facade-detector.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/parsers/facade-detector.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/parsers/facade-detector'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/parsers/facade-detector.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/parsers/facade-detector.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/parsers/facade-detector.ts` (do `git add scripts/pulse/parsers/facade-detector.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/parsers/facade-detector.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/parsers/facade-detector.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/parsers/facade-detector.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-context-broadcast",
      "title": "Liquefy scripts/pulse/context-broadcast.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/context-broadcast.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **572 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/context-broadcast.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 68,\n  \"hardcoded_numeric_surface_risk\": 32,\n  \"hardcoded_numeric_decision_literal_risk\": 1,\n  \"hardcoded_regex_surface_risk\": 4,\n  \"hardcoded_literal_surface_risk\": 132,\n  \"hardcoded_fixed_literal_decision_risk\": 4,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_boolean_surface_risk\": 3,\n  \"hardcoded_replacement_cheat_risk\": 324\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/context-broadcast.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/context-broadcast.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/context-broadcast.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/context-broadcast'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/context-broadcast.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/context-broadcast.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/context-broadcast.ts` (do `git add scripts/pulse/context-broadcast.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/context-broadcast.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/context-broadcast.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/context-broadcast.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__tests__-dod-real-status-spec",
      "title": "Liquefy scripts/pulse/__tests__/dod-real-status.spec.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__tests__/dod-real-status.spec.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **571 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__tests__/dod-real-status.spec.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 21,\n  \"hardcoded_literal_surface_risk\": 142,\n  \"hardcoded_numeric_surface_risk\": 78,\n  \"hardcoded_boolean_surface_risk\": 61,\n  \"hardcoded_predeclared_authority_context_risk\": 34,\n  \"hardcoded_numeric_decision_literal_risk\": 16,\n  \"hardcoded_fixed_boolean_decision_risk\": 6,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 212\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__tests__/dod-real-status.spec.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__tests__/dod-real-status.spec.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__tests__/dod-real-status.spec.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__tests__/dod-real-status.spec'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__tests__/dod-real-status.spec.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__tests__/dod-real-status.spec.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__tests__/dod-real-status.spec.ts` (do `git add scripts/pulse/__tests__/dod-real-status.spec.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__tests__/dod-real-status.spec.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__tests__/dod-real-status.spec.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__tests__/dod-real-status.spec.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-execution-matrix-spec-companion",
      "title": "Liquefy scripts/pulse/__companions__/execution-matrix.spec.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/execution-matrix.spec.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **569 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/execution-matrix.spec.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 34,\n  \"hardcoded_numeric_surface_risk\": 191,\n  \"hardcoded_predeclared_authority_context_risk\": 45,\n  \"hardcoded_literal_surface_risk\": 231,\n  \"hardcoded_boolean_surface_risk\": 49,\n  \"hardcoded_fixed_boolean_decision_risk\": 8,\n  \"hardcoded_numeric_decision_literal_risk\": 9,\n  \"hardcoded_fixed_literal_decision_risk\": 1,\n  \"hardcoded_gate_profile_threshold_risk\": 1\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/execution-matrix.spec.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/execution-matrix.spec.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/execution-matrix.spec.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/execution-matrix.spec.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/execution-matrix.spec.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/execution-matrix.spec.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/execution-matrix.spec.companion.ts` (do `git add scripts/pulse/__companions__/execution-matrix.spec.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/execution-matrix.spec.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/execution-matrix.spec.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/execution-matrix.spec.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-autonomy-loop-unit-ranking",
      "title": "Liquefy scripts/pulse/autonomy-loop.unit-ranking.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/autonomy-loop.unit-ranking.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **560 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/autonomy-loop.unit-ranking.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 57,\n  \"hardcoded_boolean_surface_risk\": 10,\n  \"hardcoded_literal_surface_risk\": 52,\n  \"hardcoded_fixed_literal_decision_risk\": 36,\n  \"hardcoded_numeric_surface_risk\": 85,\n  \"hardcoded_numeric_decision_literal_risk\": 10,\n  \"hardcoded_fixed_boolean_decision_risk\": 3,\n  \"hardcoded_replacement_cheat_risk\": 307\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/autonomy-loop.unit-ranking.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/autonomy-loop.unit-ranking.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/autonomy-loop.unit-ranking.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/autonomy-loop.unit-ranking'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/autonomy-loop.unit-ranking.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/autonomy-loop.unit-ranking.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/autonomy-loop.unit-ranking.ts` (do `git add scripts/pulse/autonomy-loop.unit-ranking.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/autonomy-loop.unit-ranking.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/autonomy-loop.unit-ranking.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/autonomy-loop.unit-ranking.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-regression-guard",
      "title": "Liquefy scripts/pulse/regression-guard.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/regression-guard.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **556 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/regression-guard.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 65,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_literal_surface_risk\": 125,\n  \"hardcoded_numeric_surface_risk\": 24,\n  \"hardcoded_boolean_surface_risk\": 12,\n  \"hardcoded_numeric_decision_literal_risk\": 4,\n  \"hardcoded_fixed_literal_decision_risk\": 4,\n  \"hardcoded_replacement_cheat_risk\": 321\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/regression-guard.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/regression-guard.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/regression-guard.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/regression-guard'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/regression-guard.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/regression-guard.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/regression-guard.ts` (do `git add scripts/pulse/regression-guard.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/regression-guard.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/regression-guard.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/regression-guard.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-browser-stress-tester-live-artifacts",
      "title": "Liquefy scripts/pulse/browser-stress-tester/live-artifacts.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/browser-stress-tester/live-artifacts.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **550 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/browser-stress-tester/live-artifacts.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 75,\n  \"hardcoded_literal_surface_risk\": 59,\n  \"hardcoded_regex_surface_risk\": 23,\n  \"hardcoded_fixed_literal_decision_risk\": 10,\n  \"hardcoded_numeric_surface_risk\": 38,\n  \"hardcoded_numeric_decision_literal_risk\": 4,\n  \"hardcoded_boolean_surface_risk\": 8,\n  \"hardcoded_fixed_boolean_decision_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 331\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/browser-stress-tester/live-artifacts.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/browser-stress-tester/live-artifacts.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/browser-stress-tester/live-artifacts.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/browser-stress-tester/live-artifacts'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/browser-stress-tester/live-artifacts.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/browser-stress-tester/live-artifacts.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/browser-stress-tester/live-artifacts.ts` (do `git add scripts/pulse/browser-stress-tester/live-artifacts.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/browser-stress-tester/live-artifacts.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/browser-stress-tester/live-artifacts.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/browser-stress-tester/live-artifacts.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-execution-harness",
      "title": "Liquefy scripts/pulse/execution-harness.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/execution-harness.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **545 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/execution-harness.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 545\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/execution-harness.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/execution-harness.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/execution-harness.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/execution-harness'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/execution-harness.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/execution-harness.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/execution-harness.ts` (do `git add scripts/pulse/execution-harness.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/execution-harness.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/execution-harness.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/execution-harness.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-codebase-truth",
      "title": "Liquefy scripts/pulse/codebase-truth.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/codebase-truth.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **544 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/codebase-truth.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 33,\n  \"hardcoded_literal_surface_risk\": 52,\n  \"hardcoded_fixed_literal_decision_risk\": 22,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_numeric_surface_risk\": 52,\n  \"hardcoded_numeric_decision_literal_risk\": 5,\n  \"hardcoded_boolean_surface_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 377\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/codebase-truth.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/codebase-truth.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/codebase-truth.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/codebase-truth'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/codebase-truth.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/codebase-truth.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/codebase-truth.ts` (do `git add scripts/pulse/codebase-truth.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/codebase-truth.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/codebase-truth.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/codebase-truth.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-runtime-evidence-probes",
      "title": "Liquefy scripts/pulse/runtime-evidence.probes.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/runtime-evidence.probes.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **544 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/runtime-evidence.probes.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 53,\n  \"hardcoded_literal_surface_risk\": 117,\n  \"hardcoded_boolean_surface_risk\": 31,\n  \"hardcoded_numeric_surface_risk\": 27,\n  \"hardcoded_regex_surface_risk\": 1,\n  \"hardcoded_fixed_literal_decision_risk\": 2,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_replacement_cheat_risk\": 311\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/runtime-evidence.probes.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/runtime-evidence.probes.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/runtime-evidence.probes.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/runtime-evidence.probes'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/runtime-evidence.probes.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/runtime-evidence.probes.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/runtime-evidence.probes.ts` (do `git add scripts/pulse/runtime-evidence.probes.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/runtime-evidence.probes.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/runtime-evidence.probes.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/runtime-evidence.probes.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-whatsapp-service-companion",
      "title": "Liquefy scripts/pulse/__companions__/whatsapp.service.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/whatsapp.service.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **530 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/whatsapp.service.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 530\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/whatsapp.service.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/whatsapp.service.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/whatsapp.service.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/whatsapp.service.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/whatsapp.service.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/whatsapp.service.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/whatsapp.service.companion.ts` (do `git add scripts/pulse/__companions__/whatsapp.service.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/whatsapp.service.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/whatsapp.service.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/whatsapp.service.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-parser-registry",
      "title": "Liquefy scripts/pulse/parser-registry.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/parser-registry.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **529 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/parser-registry.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 84,\n  \"hardcoded_regex_surface_risk\": 8,\n  \"hardcoded_literal_surface_risk\": 96,\n  \"hardcoded_numeric_surface_risk\": 23,\n  \"hardcoded_boolean_surface_risk\": 4,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_numeric_decision_literal_risk\": 1,\n  \"hardcoded_fixed_literal_decision_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 311\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/parser-registry.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/parser-registry.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/parser-registry.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/parser-registry'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/parser-registry.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/parser-registry.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/parser-registry.ts` (do `git add scripts/pulse/parser-registry.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/parser-registry.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/parser-registry.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/parser-registry.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-external-signals",
      "title": "Liquefy scripts/pulse/external-signals.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/external-signals.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **527 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/external-signals.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 45,\n  \"hardcoded_numeric_surface_risk\": 37,\n  \"hardcoded_numeric_decision_literal_risk\": 9,\n  \"hardcoded_literal_surface_risk\": 79,\n  \"hardcoded_fixed_literal_decision_risk\": 18,\n  \"hardcoded_predeclared_authority_context_risk\": 3,\n  \"hardcoded_boolean_surface_risk\": 5,\n  \"hardcoded_replacement_cheat_risk\": 331\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/external-signals.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/external-signals.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/external-signals.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/external-signals'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/external-signals.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/external-signals.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/external-signals.ts` (do `git add scripts/pulse/external-signals.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/external-signals.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/external-signals.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/external-signals.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-real-sandbox",
      "title": "Liquefy scripts/pulse/real-sandbox.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/real-sandbox.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **527 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/real-sandbox.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 56,\n  \"hardcoded_literal_surface_risk\": 117,\n  \"hardcoded_regex_surface_risk\": 8,\n  \"hardcoded_numeric_surface_risk\": 34,\n  \"hardcoded_boolean_surface_risk\": 18,\n  \"hardcoded_fixed_boolean_decision_risk\": 4,\n  \"hardcoded_fixed_literal_decision_risk\": 2,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 287\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/real-sandbox.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/real-sandbox.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/real-sandbox.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/real-sandbox'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/real-sandbox.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/real-sandbox.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/real-sandbox.ts` (do `git add scripts/pulse/real-sandbox.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/real-sandbox.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/real-sandbox.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/real-sandbox.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-api-fuzzer-companion",
      "title": "Liquefy scripts/pulse/__companions__/api-fuzzer.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/api-fuzzer.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **508 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/api-fuzzer.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 108,\n  \"hardcoded_literal_surface_risk\": 218,\n  \"hardcoded_regex_surface_risk\": 30,\n  \"hardcoded_numeric_surface_risk\": 92,\n  \"hardcoded_boolean_surface_risk\": 31,\n  \"hardcoded_numeric_decision_literal_risk\": 18,\n  \"hardcoded_predeclared_authority_context_risk\": 2,\n  \"hardcoded_fixed_literal_decision_risk\": 9\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/api-fuzzer.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/api-fuzzer.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/api-fuzzer.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/api-fuzzer.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/api-fuzzer.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/api-fuzzer.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/api-fuzzer.companion.ts` (do `git add scripts/pulse/__companions__/api-fuzzer.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/api-fuzzer.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/api-fuzzer.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/api-fuzzer.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-runtime-fusion-spec-companion",
      "title": "Liquefy scripts/pulse/__companions__/runtime-fusion.spec.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/runtime-fusion.spec.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **507 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/runtime-fusion.spec.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 322,\n  \"hardcoded_const_declaration_risk\": 25,\n  \"hardcoded_numeric_surface_risk\": 88,\n  \"hardcoded_numeric_decision_literal_risk\": 31,\n  \"hardcoded_predeclared_authority_context_risk\": 16,\n  \"fixed_capability_id_collection\": 6,\n  \"fixed_flow_id_collection\": 2,\n  \"hardcoded_boolean_surface_risk\": 8,\n  \"hardcoded_path_decision_risk\": 7,\n  \"hardcoded_regex_surface_risk\": 2\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/runtime-fusion.spec.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/runtime-fusion.spec.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/runtime-fusion.spec.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/runtime-fusion.spec.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/runtime-fusion.spec.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/runtime-fusion.spec.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/runtime-fusion.spec.companion.ts` (do `git add scripts/pulse/__companions__/runtime-fusion.spec.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/runtime-fusion.spec.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/runtime-fusion.spec.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/runtime-fusion.spec.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-cert-gate-multi-cycle",
      "title": "Liquefy scripts/pulse/cert-gate-multi-cycle.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/cert-gate-multi-cycle.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **506 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/cert-gate-multi-cycle.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 61,\n  \"hardcoded_numeric_surface_risk\": 48,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_literal_surface_risk\": 93,\n  \"hardcoded_boolean_surface_risk\": 14,\n  \"hardcoded_regex_surface_risk\": 4,\n  \"hardcoded_fixed_boolean_decision_risk\": 2,\n  \"hardcoded_numeric_decision_literal_risk\": 12,\n  \"hardcoded_fixed_literal_decision_risk\": 10,\n  \"hardcoded_replacement_cheat_risk\": 258\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/cert-gate-multi-cycle.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/cert-gate-multi-cycle.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/cert-gate-multi-cycle.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/cert-gate-multi-cycle'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/cert-gate-multi-cycle.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/cert-gate-multi-cycle.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/cert-gate-multi-cycle.ts` (do `git add scripts/pulse/cert-gate-multi-cycle.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/cert-gate-multi-cycle.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/cert-gate-multi-cycle.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/cert-gate-multi-cycle.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-production-proof",
      "title": "Liquefy scripts/pulse/production-proof.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/production-proof.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **500 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/production-proof.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 68,\n  \"hardcoded_literal_surface_risk\": 133,\n  \"hardcoded_numeric_surface_risk\": 19,\n  \"hardcoded_boolean_surface_risk\": 15,\n  \"hardcoded_fixed_literal_decision_risk\": 4,\n  \"hardcoded_predeclared_authority_context_risk\": 4,\n  \"hardcoded_numeric_decision_literal_risk\": 4,\n  \"hardcoded_replacement_cheat_risk\": 253\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/production-proof.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/production-proof.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/production-proof.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/production-proof'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/production-proof.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/production-proof.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/production-proof.ts` (do `git add scripts/pulse/production-proof.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/production-proof.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/production-proof.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/production-proof.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-graph",
      "title": "Liquefy scripts/pulse/graph.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/graph.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **499 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/graph.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_regex_surface_risk\": 13,\n  \"hardcoded_literal_surface_risk\": 67,\n  \"hardcoded_const_declaration_risk\": 75,\n  \"hardcoded_boolean_surface_risk\": 7,\n  \"hardcoded_numeric_surface_risk\": 29,\n  \"hardcoded_fixed_literal_decision_risk\": 8,\n  \"hardcoded_numeric_decision_literal_risk\": 3,\n  \"hardcoded_predeclared_authority_context_risk\": 1,\n  \"hardcoded_replacement_cheat_risk\": 296\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/graph.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/graph.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/graph.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/graph'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/graph.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/graph.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/graph.ts` (do `git add scripts/pulse/graph.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/graph.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/graph.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/graph.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-convergence-plan-companion",
      "title": "Liquefy scripts/pulse/__companions__/convergence-plan.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/convergence-plan.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **498 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/convergence-plan.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_numeric_surface_risk\": 78,\n  \"hardcoded_literal_surface_risk\": 414,\n  \"hardcoded_predeclared_authority_context_risk\": 6\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/convergence-plan.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/convergence-plan.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/convergence-plan.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/convergence-plan.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/convergence-plan.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/convergence-plan.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/convergence-plan.companion.ts` (do `git add scripts/pulse/__companions__/convergence-plan.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/convergence-plan.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/convergence-plan.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/convergence-plan.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-otel-runtime-companion",
      "title": "Liquefy scripts/pulse/__companions__/otel-runtime.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/otel-runtime.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **496 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/otel-runtime.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_const_declaration_risk\": 183,\n  \"hardcoded_literal_surface_risk\": 193,\n  \"hardcoded_fixed_literal_decision_risk\": 7,\n  \"hardcoded_numeric_surface_risk\": 92,\n  \"hardcoded_regex_surface_risk\": 4,\n  \"hardcoded_numeric_decision_literal_risk\": 4,\n  \"hardcoded_boolean_surface_risk\": 13\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/otel-runtime.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/otel-runtime.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/otel-runtime.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/otel-runtime.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/otel-runtime.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/otel-runtime.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/otel-runtime.companion.ts` (do `git add scripts/pulse/__companions__/otel-runtime.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/otel-runtime.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/otel-runtime.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/otel-runtime.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-cross-artifact-consistency-check-comparators",
      "title": "Liquefy scripts/pulse/cross-artifact-consistency-check/comparators.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/cross-artifact-consistency-check/comparators.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **490 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/cross-artifact-consistency-check/comparators.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_literal_surface_risk\": 103,\n  \"hardcoded_const_declaration_risk\": 55,\n  \"hardcoded_numeric_surface_risk\": 22,\n  \"hardcoded_fixed_literal_decision_risk\": 5,\n  \"hardcoded_boolean_surface_risk\": 7,\n  \"hardcoded_replacement_cheat_risk\": 298\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/cross-artifact-consistency-check/comparators.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/cross-artifact-consistency-check/comparators.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/cross-artifact-consistency-check/comparators.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/cross-artifact-consistency-check/comparators'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/cross-artifact-consistency-check/comparators.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/cross-artifact-consistency-check/comparators.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/cross-artifact-consistency-check/comparators.ts` (do `git add scripts/pulse/cross-artifact-consistency-check/comparators.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/cross-artifact-consistency-check/comparators.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/cross-artifact-consistency-check/comparators.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/cross-artifact-consistency-check/comparators.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    },
    {
      "id": "liquefy-__companions__-flow-engine-global-companion",
      "title": "Liquefy scripts/pulse/__companions__/flow-engine-global.companion.ts",
      "prompt": "# Combined Recon+Apply Worker — `scripts/pulse/__companions__/flow-engine-global.companion.ts`\n\n## Mission (self-contained — you have no memory of prior sessions)\n\nPULSE is a verification machine inside this repo. Goal: **PULSE itself must be 100% dynamic with ZERO hardcode.** The auditor `scripts/pulse/no-hardcoded-reality-audit.ts` is **GOVERNANCE-LOCKED — never touch it under any circumstance**. The auditor reports **485 findings** in your assigned file.\n\nYour assigned file: `scripts/pulse/__companions__/flow-engine-global.companion.ts`\n\n## Finding mix in your file\n\n```json\n{\n  \"hardcoded_replacement_cheat_risk\": 485\n}\n```\n\n## Token economy mandate (Daniel is non-technical, no human reviewer)\n\nYou are the SOLE worker for this file. Daniel cannot validate your work. Your output reaches production. Therefore:\n- Apply patches ONLY when truly dynamic (kernel-derived, AST-derived, runtime-evidence-derived).\n- Skip \"fake-dynamic\" patches (lookup table wrapping literal, string-construction of identifier, function returning a literal).\n- Never lower the auditor count by deleting code that has runtime behavior.\n- Never break imports. Always validate with smoke.\n\n## Liquefaction toolkit\n\nRead in order:\n1. `scripts/pulse/dynamic-reality-kernel.ts` — full primitive list (~50-65 `discover*`/`derive*` functions, including AST-derivation meta `deriveStringUnionMembersFromTypeContract(filePath, typeName)` and many union-label wrappers like `discoverConvergenceUnitKindLabels()`, `discoverHarnessExecutionStatusLabels()`, etc).\n2. `scripts/pulse/no-hardcoded-reality-audit.ts` lines **79–220** — allowed-context kernel-grammar tokens (your patches must be in functions whose context contains these tokens, e.g. `evidence`, `kernel`, `ast`, `schema`, `structural`, `grammar`, `token`, `gate`, `type`).\n3. `docs/ai/PULSE_NO_HARDCODED_REALITY_DEBT_GUIDE.md` — patterns that worked / failed before.\n4. `scripts/pulse/__companions__/flow-engine-global.companion.ts` in full.\n5. If your file is truncated/companion-split, also read its companion at `scripts/pulse/__companions__/<basename>.companion.ts` — restoring main from companion FIRST (preserve all exports) is acceptable in this task.\n\n## Workflow (do all steps; stop only on uncatchable error)\n\n### Step A — Establish baseline\n```sh\n./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"const m=require('./scripts/pulse/no-hardcoded-reality-audit'); const r=m.auditPulseNoHardcodedReality(process.cwd()); let c=0,k={}; for(const f of r.findings) if (f.filePath==='scripts/pulse/__companions__/flow-engine-global.companion.ts') { c++; k[f.kind]=(k[f.kind]||0)+1; } console.log(JSON.stringify({file:'scripts/pulse/__companions__/flow-engine-global.companion.ts', count:c, byKind:k}, null, 2));\"\n```\n\nSave the count as BEFORE.\n\n### Step B — Identify and apply patches (iterate up to 3 cycles)\n\nFor each cycle:\n1. Identify ~5-15 strong patch opportunities. Prefer:\n   - **literals/regex** that match a kernel `discover*/derive*` function output → replace with kernel call\n   - **`isSameState(x, 'literal')`** where `'literal'` is a member of a TS type-union → use `discoverXxxLabels().has(x)`\n   - **numeric thresholds** with derivable origin → use `deriveUnitValue()`/`deriveZeroValue()`/AST contract\n   - **filename literals** like `'PULSE_CERTIFICATE.json'` → `discoverAllObservedArtifactFilenames().certificate`\n2. For each patch, edit the file (preserve exports!).\n3. Add kernel imports at top (deduped).\n4. After 5-15 patches, smoke-import:\n   ```sh\n   ./backend/node_modules/.bin/ts-node --transpile-only --project scripts/pulse/tsconfig.json -e \"require('./scripts/pulse/__companions__/flow-engine-global.companion'); console.log('IMPORT_OK');\"\n   ```\n   Must print `IMPORT_OK`. If error, REVERT just the bad patches by editing them out (NOT `git restore`); re-smoke. If you cannot recover after 2 attempts, STOP that cycle.\n5. Re-run auditor for THIS file:\n   ```sh\n   (same auditor command as Step A)\n   ```\n6. If count went DOWN: continue (Step C if cycle limit hit, else next cycle from Step B).\n7. If count went UP: surgical-revert the just-applied patches (edit file, undo those exact edits — do NOT use git). Re-smoke. Mark cycle as net-zero.\n8. If count unchanged after 3 patches in a cycle: stop iterating.\n\n### Step C — Commit (only if final count is strictly LESS than BEFORE)\n```sh\ngit add scripts/pulse/__companions__/flow-engine-global.companion.ts\ngit commit -m \"refactor(pulse-liquefy): replace hardcoded with kernel discovery in $(basename scripts/pulse/__companions__/flow-engine-global.companion.ts)\"\n```\nDo NOT use `--no-verify`. If husky/lint-staged complains about UNRELATED files (Onda 0 work, etc.), STAGE ONLY `scripts/pulse/__companions__/flow-engine-global.companion.ts` (do `git add scripts/pulse/__companions__/flow-engine-global.companion.ts` not `git add -A`). If hooks still fail on unrelated files, that's blocker — report and don't force commit.\n\n### Step D — Final report\nAppend a single fenced JSON block at end of your response:\n\n```json\n{\n  \"file\": \"scripts/pulse/__companions__/flow-engine-global.companion.ts\",\n  \"auditorBefore\": <number>,\n  \"auditorAfter\": <number>,\n  \"patchesApplied\": <number>,\n  \"patchesSkippedFakeDynamic\": <number>,\n  \"cyclesRun\": <number>,\n  \"importSmoke\": \"ok|failed\",\n  \"commit\": \"<sha|null>\",\n  \"blockers\": [{\"kind\": \"<text>\", \"reason\": \"<text>\"}],\n  \"kernelExtensionsRequested\": [\"<new function name + signature>\"]\n}\n```\n\n## Hard constraints\n\n- Edit ONLY `scripts/pulse/__companions__/flow-engine-global.companion.ts` (and its kernel import line at top). No other PULSE file.\n- NEVER edit `scripts/pulse/no-hardcoded-reality-audit.ts`.\n- NEVER edit `scripts/pulse/dynamic-reality-kernel.ts` (a separate Wave K subagent owns kernel changes — request extensions in your output instead).\n- Do NOT use `: any`, `@ts-ignore`, `eslint-disable`, `biome-ignore`, `NOSONAR`, `codacy:disable`.\n- NEVER `git restore`, `git checkout --`, `git reset --hard`, `git stash`, `--no-verify`.\n- Stage ONLY your file when committing (`git add scripts/pulse/__companions__/flow-engine-global.companion.ts`, not `-A`).\n- Watch for active PULSE daemon: `pgrep -af 'ts-node.*scripts/pulse/index\\.ts.*(--watch|--continuous|--daemon)' | grep -v opencode`. Abort if found (the daemon overwrites files).\n- If the kernel lacks a primitive you need to liquefy a finding, list it under `kernelExtensionsRequested` (with desired name + signature + intended source-of-truth like \"AST union members of `type X` in `types.X.ts`\"). Do NOT invent fake-dynamic to avoid the request.\n- Time budget: complete all 3 cycles or report partial in <30 minutes. Hard stop if you exceed.\n"
    }
  ]
}