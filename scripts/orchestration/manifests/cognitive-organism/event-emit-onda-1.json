{
  "runId": "cognitive-event-emit-2026-05-14T00-27-15-867",
  "concurrency": 8,
  "staggerMs": 8000,
  "timeoutSec": 0,
  "dir": "/Users/danielpenin/whatsapp_saas",
  "skipPermissions": true,
  "tasks": [
    {
      "id": "UTP-EVENT-EMIT-CHECKOUT",
      "title": "UTP-EVENT-EMIT-CHECKOUT — emit canonical events for checkout",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-CHECKOUT\n\n**Surface**: checkout\n**Surface dir hint**: `backend/src/checkout/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.cart.created`\n  - `commerce.cart.abandoned`\n  - `commerce.cart.checkout_initiated`\n  - `commerce.payment.initiated`\n  - `commerce.payment.approved`\n  - `commerce.payment.declined`\n  - `commerce.payment.refunded`\n  - `commerce.payment.charged_back`\n\n**Controller hint**: checkout / payment intent / Stripe webhook handlers (search backend/src for `payment_intent`, `charge.succeeded`, `cart.created`)\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/checkout-emitter/checkout-event-emitter.service.ts` that:\n\n1. Receives the existing `checkout / payment intent / Stripe webhook handlers (search backend/src for `payment_intent`, `charge.succeeded`, `cart.created`)` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/checkout-emitter/checkout-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/checkout-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/checkout/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/checkout-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/checkout/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-CRM",
      "title": "UTP-EVENT-EMIT-CRM — emit canonical events for crm",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-CRM\n\n**Surface**: crm\n**Surface dir hint**: `backend/src/crm/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.crm.stage_changed`\n  - `commerce.crm.owner_assigned`\n  - `commerce.crm.next_step_defined`\n  - `commerce.crm.deal_won`\n  - `commerce.crm.deal_lost`\n  - `commerce.lead.objection_raised`\n\n**Controller hint**: CRM stage transitions, deal lifecycle (search for `pipeline`, `deal`, `lead.stage`)\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/crm-emitter/crm-event-emitter.service.ts` that:\n\n1. Receives the existing `CRM stage transitions, deal lifecycle (search for `pipeline`, `deal`, `lead.stage`)` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/crm-emitter/crm-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/crm-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/crm/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/crm-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/crm/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-WHATSAPP",
      "title": "UTP-EVENT-EMIT-WHATSAPP — emit canonical events for whatsapp",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-WHATSAPP\n\n**Surface**: whatsapp\n**Surface dir hint**: `backend/src/whatsapp/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.whatsapp.message_received`\n  - `commerce.whatsapp.message_read`\n  - `commerce.whatsapp.message_replied`\n  - `commerce.whatsapp.handoff_to_human`\n  - `commerce.whatsapp.conversation_resumed`\n  - `commerce.whatsapp.session_lifecycle`\n\n**Controller hint**: WhatsApp inbound (WAHA + Meta), reply, handoff, session lifecycle (search for `whatsapp`, `meta` webhook handlers)\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/whatsapp-emitter/whatsapp-event-emitter.service.ts` that:\n\n1. Receives the existing `WhatsApp inbound (WAHA + Meta), reply, handoff, session lifecycle (search for `whatsapp`, `meta` webhook handlers)` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/whatsapp-emitter/whatsapp-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/whatsapp-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/whatsapp/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/whatsapp-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/whatsapp/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-CAMPAIGN",
      "title": "UTP-EVENT-EMIT-CAMPAIGN — emit canonical events for campaign",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-CAMPAIGN\n\n**Surface**: campaign\n**Surface dir hint**: `backend/src/campaigns/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.campaign.clicked`\n  - `commerce.campaign.conversion_associated`\n  - `commerce.campaign.audience_reached`\n  - `commerce.campaign.creative_swapped`\n  - `commerce.campaign.performance_drop_detected`\n\n**Controller hint**: Campaign click handlers, attribution, fadiga detector (search for `campaign`, `creative`, `utm`)\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/campaign-emitter/campaign-event-emitter.service.ts` that:\n\n1. Receives the existing `Campaign click handlers, attribution, fadiga detector (search for `campaign`, `creative`, `utm`)` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/campaign-emitter/campaign-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/campaign-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/campaigns/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/campaign-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/campaigns/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-MEMBER",
      "title": "UTP-EVENT-EMIT-MEMBER — emit canonical events for member-area",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-MEMBER\n\n**Surface**: member-area\n**Surface dir hint**: `backend/src/member-area/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.member_area.enrolled`\n  - `commerce.member_area.progressed`\n  - `commerce.member_area.dropped_out`\n  - `commerce.affiliate.performance_measured`\n  - `commerce.affiliate.commission_calculated`\n\n**Controller hint**: Member-area enrollment + affiliate accounting (search for `member`, `affiliate`)\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/member-area-emitter/member-area-event-emitter.service.ts` that:\n\n1. Receives the existing `Member-area enrollment + affiliate accounting (search for `member`, `affiliate`)` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/member-area-emitter/member-area-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/member-area-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/member-area/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/member-area-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/member-area/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-KYC",
      "title": "UTP-EVENT-EMIT-KYC — emit canonical events for kyc",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-KYC\n\n**Surface**: kyc\n**Surface dir hint**: `backend/src/kyc/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.kyc.document_submitted`\n  - `commerce.kyc.approved`\n  - `commerce.kyc.rejected`\n\n**Controller hint**: KYC document lifecycle (search for `kyc`)\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/kyc-emitter/kyc-event-emitter.service.ts` that:\n\n1. Receives the existing `KYC document lifecycle (search for `kyc`)` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/kyc-emitter/kyc-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/kyc-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/kyc/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/kyc-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/kyc/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-POSTSALE",
      "title": "UTP-EVENT-EMIT-POSTSALE — emit canonical events for post-sale",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-POSTSALE\n\n**Surface**: post-sale\n**Surface dir hint**: `backend/src/post-sale/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n  - `commerce.post_sale.delivery_completed`\n  - `commerce.post_sale.activation_started`\n  - `commerce.post_sale.first_value_obtained`\n  - `commerce.post_sale.satisfaction_signal_observed`\n  - `commerce.post_sale.testimonial_requested`\n  - `commerce.post_sale.repurchase_window_opened`\n  - `commerce.post_sale.churn_risk_detected`\n  - `commerce.post_sale.win_back_window_opened`\n\n**Controller hint**: Post-sale lifecycle. If no post-sale module exists, CREATE one at backend/src/post-sale/ as additive infrastructure (no existing controller change). The spec proves emitter shape.\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/post-sale-emitter/post-sale-event-emitter.service.ts` that:\n\n1. Receives the existing `Post-sale lifecycle. If no post-sale module exists, CREATE one at backend/src/post-sale/ as additive infrastructure (no existing controller change). The spec proves emitter shape.` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/post-sale-emitter/post-sale-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/post-sale-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/post-sale/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/post-sale-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/post-sale/\n\nThen plan, then implement, then validate, then EXIT.\n"
    },
    {
      "id": "UTP-EVENT-EMIT-AUDIT",
      "title": "UTP-EVENT-EMIT-AUDIT — emit canonical events for event-emit-audit",
      "prompt": "# COGNITIVE ORGANISM — Onda 1 EVENT-EMIT subagent\n\nYou are an OpenCode V4 Pro subagent dispatched by the orchestrator (Claude\nOpus 4.7) for the Kloel Cognitive Organism mission.\n\n## MANDATORY PRE-READ (in order)\n\n1. `docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md` — full canonical plan.\n2. `docs/contracts/pci/MANIFEST.md` — PCI manifest (frozen contracts).\n3. `docs/contracts/pci/01-event-taxonomy.md` — event taxonomy (your authority).\n4. `docs/contracts/pci/05-universal-conventions.md` — truthMode / provenance / valence.\n5. `docs/contracts/pci/06-b17-surfaces.md` — surface→event mapping (your domain).\n6. `scripts/decomp/cognitive-organism-subagent-delegation-rules.md` — operational rules.\n7. `CLAUDE.md` — project laws.\n\nIf any file is missing or its checksum mismatches `docs/contracts/pci/CHECKSUMS.txt`, STOP and report.\n\n## ABSOLUTE PROHIBITIONS\n\n- NO touch on frontend/**, *.tsx, *.vue, e2e/**, frontend-admin/**.\n- NO change to existing HTTP controllers/routes/DTOs (additive only).\n- NO touch on CLAUDE.md, AGENTS.md, ops/**, .husky/**, eslint.config.mjs,\n  ai-models.ts, scripts/pulse/no-hardcoded-reality-audit.ts,\n  docs/contracts/pci/**, docs/plans/KLOEL_COGNITIVE_ORGANISM_PLAN.md.\n- NO bypass of Codacy: forbidden suppression comments and skip tags\n  (biome-ignore, @ts-ignore, eslint-disable, NOSONAR, [skip ci], etc.).\n- NO `git restore` (any flag).\n- NO invent event names outside PCI.1.\n- NO `prismaAny` in NEW code (existing usage may stay).\n- NO `Math.random()`, `localStorage`, hardcoded fake data in production paths.\n- NO behavioral instruction strings (\"você é\", \"act as\", etc.) anywhere.\n\n## REQUIRED VALIDATION (before declaring complete)\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='<your contract spec path>' --no-coverage\n```\n\nExit 0 required on all three.\n\n## DELIVERABLE FORMAT\n\nWhen done, print to stdout (in this exact format):\n\n```\nEXIT <utp-id>\nfiles_added:\n  - <path>\nfiles_modified:\n  - <path>\ngates_status:\n  prompt-leakage: PASS|FAIL\n  evidence-provenance: PASS|FAIL\nvalidation:\n  pci_validate: PASS|FAIL\n  tsc: PASS|FAIL\n  contract_spec: PASS|FAIL  (count: <N>)\nadjacency_conflicts: <none|list>\npci_divergences: <none|list>\nfollow_ups: <none|list>\n```\n\n\n## YOUR UTP: UTP-EVENT-EMIT-AUDIT\n\n**Surface**: event-emit-audit\n**Surface dir hint**: `backend/src/kloel/spine/` (verify with `ls` — adjust if the real dir differs).\n\n**Canonical events you MUST emit on the spine** (PCI.1 + PCI.6):\n\n\n**Controller hint**: NO event emission — instead, build an auditor that scans the in-memory spine and reports per-surface coverage (which canonical event names from PCI.6 were observed in the last N events). Output ratio events:transitions per surface. Build at backend/src/kloel/spine/spine-coverage-auditor.service.ts with a focused spec.\n\n## SPINE EMITTER CONTRACT\n\nThere is no shared spine module yet. Create one at\n`backend/src/kloel/spine/spine-emitter.service.ts` if absent (additive,\nnot destructive). Define:\n\n- `SpineEmitterService` with method `emit(event: SpineEventInput): Promise<SpineEventRef>` where the input carries `eventName`, `workspaceId`, `entityRef`, `payload`, `truthMode`, `provenance`, optional `correlationId`. The service stamps eventId (ULID-like), timestamp/occurredAt, valence (auto via ValenceTaggerService from MIND), provenance environment, and persists to an in-memory ring buffer (last 5000 events) with an exported `recentEvents()` accessor. Persistence to a Prisma-backed table is out of scope for this UTP — the in-memory ring is sufficient because downstream consumers (LOCAL-IDENT, INSIGHT, etc.) read via `recentEvents()` for now.\n\nIf `SpineEmitterService` already exists when you arrive, USE IT (do not duplicate).\n\n## YOUR DELIVERABLE\n\nCreate `backend/src/kloel/event-emit-audit-emitter/event-emit-audit-event-emitter.service.ts` that:\n\n1. Receives the existing `NO event emission — instead, build an auditor that scans the in-memory spine and reports per-surface coverage (which canonical event names from PCI.6 were observed in the last N events). Output ratio events:transitions per surface. Build at backend/src/kloel/spine/spine-coverage-auditor.service.ts with a focused spec.` business call sites (locate them with `grep -rn`).\n2. Adds a side-effect call to `SpineEmitterService.emit(...)` AFTER the existing business effect succeeds (NOT before — preserve real outcome semantics).\n3. NEVER touches the existing controller/service/DTO signature.\n4. NEVER throws inside the emitter — catch and log via NestJS Logger; surface emission must NOT break the business path.\n5. Maps every transition listed above to its canonical event with the correct truthMode (`observed` for confirmed events from external systems, `inferred` for classifier outputs).\n\nWire the new emitter via DI in the appropriate existing module (additive imports only). If the surface module doesn't exist, create one minimal module file that imports the emitter.\n\n## CONTRACT SPEC (mandatory)\n\nCreate `backend/src/kloel/event-emit-audit-emitter/event-emit-audit-event-emitter.service.spec.ts` that proves:\n\n1. For each transition in the list, calling the emitter produces an event on the spine with the correct `eventName`, `workspaceId`, `entityRef`, `truthMode`, and `provenance.source`.\n2. The emitter does NOT throw when the spine is full (ring buffer overflows oldest).\n3. The emitter does NOT throw when payload is malformed — it logs and returns.\n4. ValenceTaggerService runs over emitted events and tags terminals.\n5. Two consecutive emissions with different correlationIds produce two distinct events on the spine.\n\nUse the in-memory `SpineEmitterService` directly in tests (no Nest DI bootstrap required) — pass dependencies in the constructor.\n\n## VALIDATION COMMANDS YOU MUST RUN\n\n```sh\nnode scripts/pci/validate.mjs --strict\ncd backend && npx tsc --noEmit\ncd backend && npx jest --testPathPatterns='kloel/event-emit-audit-emitter/' --no-coverage\n```\n\nAll three must exit 0.\n\n## EDITABLE SET (read-only outside this set)\n\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/spine/**`\n  - `backend/src/kloel/event-emit-audit-emitter/**`\n\nYou MAY read any file in the repo. You MAY NOT edit any file outside this set.\n\nBegin by running:\n  cd /Users/danielpenin/whatsapp_saas\n  cat docs/contracts/pci/01-event-taxonomy.md\n  cat docs/contracts/pci/06-b17-surfaces.md\n  cat scripts/decomp/cognitive-organism-subagent-delegation-rules.md\n  ls backend/src/kloel/spine/\n\nThen plan, then implement, then validate, then EXIT.\n"
    }
  ]
}