{
  "concurrency": 5,
  "timeoutSec": 3000,
  "dir": "/Users/danielpenin/whatsapp_saas",
  "skipPermissions": true,
  "tasks": [
    {
      "id": "wiring-decision-outcome-emission",
      "title": "Wire DecisionOutcomeService into production flow (P11 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md. Never git restore.\n\nMISSION: Close the P11 wiring gap. DecisionOutcomeService.recordDecision/closeOutcome/recordEvent currently have ZERO external callers — the DecisionOutcome table is never populated and DecisionOutcomeEvent is always empty. Wire it into the real production flow.\n\nCONTEXT: Read backend/src/kloel/decision-outcome.service.ts to understand the API. Then trace where decisions are MADE (commercial-decision-orchestrator.service.ts.executeOrchestration) and where INBOUND messages arrive (search backend/src for 'whatsapp.message.received', WhatsappInboundProcessor, or the controller that handles webhooks).\n\nFILES YOU OWN:\n- backend/src/kloel/commercial-decision-orchestrator.service.ts — after building PredecidedActions in executeOrchestration, call decisionOutcomeService.recordDecision({workspaceId, decisionType: 'omnichannel', chosenAction: <primary action tool>, baselineAction, outcomeKey: stableInboundKey-based, expectedWindow: 24, contextSnapshot: {channel, contactId, concept}}). Inject DecisionOutcomeService.\n- backend/src/whatsapp/whatsapp-webhook.processor.ts OR whatever processes inbound. On inbound message received, call decisionOutcomeService.recordEvent({workspaceId, eventType: 'inbound.received', eventKey: messageId, correlation: {contactId, channel: 'whatsapp'}}). Same for instagram-webhook, email-inbox, etc — find all inbound entry points.\n- backend/src/kloel/kloel.module.ts — wire DecisionOutcomeService into providers if missing.\n\nDO NOT EDIT:\n- scripts/pulse/*\n- DTOs already consumed by frontend\n\nVALIDATION:\n  cd backend && npx tsc --noEmit 2>&1 | head -20\n  cd backend && npx jest --testPathPatterns='decision-outcome|commercial-decision-orchestrator' --no-coverage 2>&1 | tail -15\n\nCOMMITS (one per integration point):\n- feat(kloel): record decision in orchestrator at action build (P11 wiring)\n- feat(inbox): emit inbound.received event for outcome attribution\n\nFINAL REPORT: list of inbound entry points wired, sample DecisionOutcome row shape, sample DecisionOutcomeEvent row shape."
    },
    {
      "id": "wiring-global-prior-feedback",
      "title": "Wire KloelGlobalPrior.recordObservation feedback loop (P6.4 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md. Never git restore.\n\nMISSION: Close the P6.4 wiring gap. KloelGlobalPriorService.recordObservation has ZERO callers — the global prior table never grows, MindPolicyService cold-start mix is a no-op.\n\nNOTE: depends on wiring-decision-outcome-emission task. If DecisionOutcomeService.closeOutcome is not yet wired by that parallel task, still complete what you can — wire from worker silent-24h-resolver where outcomes already close atomically.\n\nFILES TO READ:\n- backend/src/kloel/decision-outcome.service.ts\n- backend/src/kloel/kloel-global-prior.service.ts\n- worker/processors/silent-24h-resolver.processor.ts\n- worker/processors/decision-outcome-resolver.ts\n- worker/prisma/schema.prisma (verify KloelGlobalPrior already synced)\n\nFILES YOU OWN:\n- backend/src/kloel/decision-outcome.service.ts — inject KloelGlobalPriorService. After closeOutcome successfully updates a row, read back the closed row (channel from contextSnapshot, decisionType + chosenAction from row), call globalPrior.recordObservation(channel, decisionType, chosenAction, wonVsBaseline ?? false). Best-effort: if globalPrior fails, log + continue.\n- worker/processors/silent-24h-resolver.processor.ts — at the atomic-claim win point (after prisma.decisionOutcome.updateMany succeeds), also call prisma.kloelGlobalPrior.upsert via the same shape KloelGlobalPriorService uses. Extract channel from decision.contextSnapshot. Best-effort.\n- worker/processors/decision-outcome-resolver.ts (if exists) — same treatment.\n\nDO NOT EDIT:\n- scripts/pulse/*\n- backend/src/kloel/mind-policy.service.ts (already consumes globalPrior correctly)\n\nVALIDATION:\n  cd backend && npx tsc --noEmit 2>&1 | head -10\n  cd worker && npx tsc --noEmit 2>&1 | head -10\n  cd worker && npx vitest run test/silent-24h-resolver.spec.ts 2>&1 | tail -10\n\nCOMMITS:\n- feat(kloel): feed closed outcomes into kloel-global-prior (P6.4)\n- feat(worker): silent-24h-resolver records global prior observation\n\nFINAL REPORT: sample upsert call, observation count delta after 1 closeOutcome run."
    },
    {
      "id": "wiring-runtime-tracer-production",
      "title": "Wire RuntimeConversationTracer into real production flow (P12 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md. Never git restore.\n\nMISSION: RuntimeConversationTracerService is @Injectable but NEVER registered in any module and NEVER consumed by the real orchestrator. Wire it into production.\n\nFILES TO READ:\n- backend/src/kloel/runtime-conversation-tracer.service.ts\n- backend/src/kloel/runtime-conversation.e2e-runtime.spec.ts (shows the 12 step instrumentation pattern)\n- backend/src/kloel/commercial-decision-orchestrator.service.ts (the orchestrateInbound method that needs steps wired)\n- backend/src/kloel/kloel.module.ts\n\nFILES YOU OWN:\n- backend/src/kloel/runtime-conversation-tracer.service.ts — refactor: instead of a single in-process in-memory trace shared across all conversations, store traces per (workspaceId, contactId, correlationId). Use a Map<string, TracerEvent[]> with capped retention (last 100 traces, ~1h TTL via timestamp). Methods: record({workspaceId, contactId, correlationId, kind, detail}), getTrace({workspaceId, contactId, correlationId}).\n- backend/src/kloel/commercial-decision-orchestrator.service.ts — inject RuntimeConversationTracerService. At each of the 12 steps in orchestrateInbound + executeOrchestration, call tracer.record(...) with the canonical step kind (step1_inbox_recorded through step12_evidence_consultable). Use input.contactId + the inboundKey as the correlationId.\n- backend/src/admin/runtime-trace/runtime-trace.controller.ts (new) — GET /admin/runtime/trace?workspaceId=&contactId=&correlationId= returns the trace JSON. Use admin auth guards (look at existing admin controllers for the pattern).\n- backend/src/kloel/kloel.module.ts — register RuntimeConversationTracerService + RuntimeTraceController.\n- backend/src/kloel/runtime-conversation-tracer.service.spec.ts — update for new API.\n\nDO NOT EDIT:\n- scripts/pulse/*\n- runtime-conversation.e2e-runtime.spec.ts (test fixture, will keep using old API; if its tests break under new API, update them to match)\n\nVALIDATION:\n  cd backend && npx tsc --noEmit 2>&1 | head -10\n  cd backend && npx jest --testPathPatterns='runtime-conversation' --no-coverage 2>&1 | tail -15\n\nCOMMITS:\n- refactor(kloel): tracer scoped per workspace+contact+correlationId (P12 closure)\n- feat(orchestrator): emit 12-step trace records during orchestrateInbound\n- feat(admin): runtime trace query endpoint\n\nFINAL REPORT: trace storage shape, sample 12-step trace for one inbound, endpoint response."
    },
    {
      "id": "wiring-identity-verification-path",
      "title": "Wire ChannelIdentifier.verifiedAt setter so cross-channel match activates (P12 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md. Never git restore.\n\nMISSION: ContactIdentityResolver.findCrossChannelMatch only matches identifiers where verifiedAt IS NOT NULL — but production code NEVER sets verifiedAt. Cross-channel unification is currently inactive. Wire the verification setter so the resolver can actually unify identities.\n\nFILES TO READ:\n- backend/src/contacts/contact-identity-resolver.service.ts\n- backend/src/contacts/channel-identifier.service.ts (if exists)\n- backend/src/auth/* (find any existing phone/email verification path — login OTP, magic link, email confirm)\n- backend/prisma/schema.prisma — ChannelIdentifier model\n\nFILES YOU OWN:\n- backend/src/contacts/channel-identifier.service.ts (or new identity-verification.service.ts) — add markVerified({workspaceId, channel, value}) method that updates verifiedAt = new Date().\n- backend/src/auth/phone-verification.service.ts (if exists; if not, add minimal version) — on OTP success, call markVerified for the phone via WHATSAPP channel.\n- backend/src/auth/email-verification.service.ts (or similar) — on email confirm, call markVerified via EMAIL channel.\n- backend/src/admin/contacts/admin-contact-verify.controller.ts (new) — POST /admin/contacts/:contactId/verify-channel body {channel, value} that operators can call to manually mark verified. Use admin auth guards.\n- backend/src/contacts/contacts.module.ts — register new service + controller.\n- spec files for new code.\n\nDO NOT EDIT:\n- scripts/pulse/*\n- Existing auth flows beyond adding the markVerified call.\n\nVALIDATION:\n  cd backend && npx tsc --noEmit 2>&1 | head -10\n  cd backend && npx jest --testPathPatterns='channel-identifier|admin-contact-verify|phone-verification|email-verification' --no-coverage 2>&1 | tail -15\n\nCOMMITS:\n- feat(contacts): channel-identifier markVerified setter (P12 closure)\n- feat(auth): phone/email verification persists ChannelIdentifier.verifiedAt\n- feat(admin): operator endpoint to mark contact channel verified\n\nFINAL REPORT: where verifiedAt is now set, sample row before/after, admin endpoint response."
    },
    {
      "id": "wiring-meta-multi-channel-schema",
      "title": "MetaConnection allows multiple channels per workspace (P8.2 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md. Never git restore.\n\nMISSION: MetaConnection.workspaceId is @unique, blocking simultaneous Meta connections for WhatsApp + Instagram + Facebook on the same workspace. The latest connection overwrites the previous, regardless of channel. Fix the schema and all consumers.\n\nFILES TO READ:\n- backend/prisma/schema.prisma (MetaConnection model around line 3409)\n- backend/src/meta/meta-auth.controller.ts (upsert by workspaceId)\n- backend/src/meta/meta-connection-state.service.ts (forWorkspace assumes single row)\n- backend/src/meta/meta-whatsapp.service.ts (resolveConnection)\n- All other consumers: grep for prisma.metaConnection\n\nFILES YOU OWN:\n- backend/prisma/schema.prisma — remove `@unique` from workspaceId; add `@@unique([workspaceId, channel])`. If `channel` is currently nullable, decide: keep nullable + use first non-null result OR require non-null with backfill migration.\n- backend/prisma/migrations/<ts>_meta_connection_per_channel/migration.sql — drop old unique index, add new compound unique. If existing rows have channel=null, backfill to 'unknown' so the new constraint doesn't break.\n- backend/src/meta/meta-auth.controller.ts — upsert by (workspaceId, channel) tuple instead of workspaceId alone.\n- backend/src/meta/meta-connection-state.service.ts — refactor forWorkspace to findMany by workspaceId, then aggregate {instagram, facebook, whatsapp} from N rows.\n- backend/src/meta/meta-whatsapp.service.ts — resolveConnection takes channel parameter, finds the right row.\n- All other consumers updated accordingly.\n- specs updated.\n- worker/prisma/schema.prisma synced (regenerate worker prisma client at the end).\n\nDO NOT EDIT:\n- scripts/pulse/*\n- Frontend code consuming the meta status (response shape from meta-connection-state.service should remain compatible).\n\nVALIDATION:\n  cd backend && npx prisma generate && npx prisma validate\n  cd backend && npx tsc --noEmit 2>&1 | head -10\n  cd backend && npx jest --testPathPatterns='meta-auth|meta-connection-state|meta-whatsapp' --no-coverage 2>&1 | tail -15\n  cd worker && npx prisma generate --schema=prisma/schema.prisma\n  cd worker && npx tsc --noEmit 2>&1 | head -10\n\nCOMMITS:\n- chore(prisma): MetaConnection per-channel uniqueness (P8.2 closure)\n- refactor(meta): consumers use (workspaceId, channel) lookup\n- chore(worker/prisma): sync MetaConnection per-channel\n\nFINAL REPORT: schema diff, sample 3-row state (whatsapp + instagram + facebook all connected on one workspace), test count."
    },
    {
      "id": "wiring-mailbox-microsoft-imap-send",
      "title": "Microsoft + IMAP/SMTP mailbox senders complete (P8.4 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md. Never git restore.\n\nMISSION: Microsoft OAuth + IMAP/SMTP mailbox services have OAuth and credential storage but ZERO send-message methods. Adding email-marketing or transactional emails through those mailboxes silently fails. Implement send paths that also append unsubscribe footer + List-Unsubscribe header (parity with gmail-oauth path at mailbox-gmail-oauth.service.ts:368-410).\n\nFILES TO READ:\n- backend/src/marketing/mailbox-gmail-oauth.service.ts (reference: sendMessageFromMailbox method with footer + List-Unsubscribe)\n- backend/src/marketing/mailbox-microsoft-oauth.service.ts (currently no send)\n- backend/src/marketing/mailbox-imap-smtp.service.ts (currently no send)\n- backend/src/common/utils/unsubscribe-footer.util.ts\n\nFILES YOU OWN:\n- backend/src/marketing/mailbox-microsoft-oauth.service.ts — add sendMessageFromMailbox(workspaceId, {toEmail, subject, html, proactive}) using Microsoft Graph API (POST /me/sendMail with the access token). Mirror the suppression + footer + List-Unsubscribe behavior from gmail-oauth.\n- backend/src/marketing/mailbox-imap-smtp.service.ts — add sendMessageFromMailbox(...) using nodemailer's SMTP transport built from stored creds (decrypt with mailbox-token-crypto). Same footer + suppression + headers.\n- backend/src/marketing/mailbox-gmail-oauth.service.spec.ts as reference for spec shape — write microsoft + imap-smtp send specs.\n- backend/src/marketing/marketing.module.ts — ensure all senders exported with the same interface so a consumer can pick any.\n\nDO NOT EDIT:\n- scripts/pulse/*\n- The Gmail mailbox service (already correct).\n\nVALIDATION:\n  cd backend && npx tsc --noEmit 2>&1 | head -10\n  cd backend && npx jest --testPathPatterns='mailbox-microsoft|mailbox-imap' --no-coverage 2>&1 | tail -15\n\nCOMMITS:\n- feat(marketing): microsoft mailbox send via graph api with footer (P8.4)\n- feat(marketing): imap-smtp send via nodemailer with footer (P8.4)\n\nFINAL REPORT: API surface (sendMessageFromMailbox signature), sample message with footer + header, test count per sender."
    },
    {
      "id": "wiring-apple-button-into-login",
      "title": "AppleSignInButton wired into kloel-auth-screen (P8.1 closure)",
      "prompt": "You are an OpenCode V4 Pro subagent. Repo: /Users/danielpenin/whatsapp_saas. Read CLAUDE.md (preserve visual contract). Never git restore.\n\nMISSION: AppleSignInButton.tsx exists with diagnostic gating, but the live login screen at frontend/src/components/kloel/auth/kloel-auth-screen.tsx still renders its own inline Apple button via onAppleClick={handleApple}. The gating is in dead code. Wire AppleSignInButton into the real login flow.\n\nFILES TO READ:\n- frontend/src/components/login/AppleSignInButton.tsx\n- frontend/src/hooks/useAppleDiagnostic.ts\n- frontend/src/components/kloel/auth/kloel-auth-screen.tsx (look for handleApple + social buttons section)\n- frontend/src/components/kloel/auth/kloel-auth-screen.social-buttons.test.tsx (if exists)\n\nFILES YOU OWN:\n- frontend/src/components/kloel/auth/kloel-auth-screen.tsx — replace the inline Apple button in the social buttons section with <AppleSignInButton onClick={handleApple} isLoading={...} />. Preserve all other social buttons (Google, etc) unchanged.\n- frontend/src/components/kloel/auth/kloel-auth-screen.social-buttons.test.tsx — adjust to mock useAppleDiagnostic; verify button renders only when ready=true.\n\nVISUAL CONTRACT: preserve all colors, fonts, border-radius (must stay <=6px), no emojis, no SVG icons changes (AppleSignInButton already uses AppleIcon from kloel-auth-screen.icons.tsx).\n\nDO NOT EDIT:\n- scripts/pulse/*\n- Any other auth flow (Google login, magic link, etc).\n\nVALIDATION:\n  cd frontend && npx tsc --noEmit 2>&1 | head -10\n  cd frontend && npx vitest run src/components/kloel/auth/kloel-auth-screen.social-buttons.test.tsx 2>&1 | tail -10\n  cd frontend && npx vitest run src/hooks/useAppleDiagnostic.test.tsx 2>&1 | tail -5\n\nCOMMITS:\n- refactor(frontend/auth): kloel-auth-screen consumes AppleSignInButton (P8.1 closure)\n\nFINAL REPORT: before/after snippet of the social-buttons section, mock test pattern."
    }
  ]
}
