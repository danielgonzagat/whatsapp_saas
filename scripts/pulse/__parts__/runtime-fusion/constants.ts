// ─── Constants ───────────────────────────────────────────────────────────────

export let EXTERNAL_SIGNAL_STATE_FILE = 'PULSE_EXTERNAL_SIGNAL_STATE.json';
export let RUNTIME_TRACES_FILE = 'PULSE_RUNTIME_TRACES.json';
export let FUSION_OUTPUT_FILE = 'PULSE_RUNTIME_FUSION.json';

export let DYNAMIC_SIGNAL_SEMANTICS_NOTE =
  'Dynamic signal semantics derived from source capability, observed payload, runtime baseline, trend, impact, and blast-radius hints; legacy labels are weak calibration only.';
